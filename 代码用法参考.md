# 铁锈战争 MOD 代码用法参考（按实际用法 v3）

> 分两个维度：**调用方**（属性，每个代码接收什么值）与 **值来源**（可被调用的表达式/函数）。
> 分类以**自动补全(snippets)的权威类型标注**为基准，并标注每个代码的**可用节**。
> 核心区分：**单位标记**(填表达式) vs **单位类型**(填单位名) vs **单位标签**(填字符串标签) vs **消息标签**(带标签发送消息)。

## 值类型速查

| 值类型                              | 说明                                                              |
| ----------------------------------- | ----------------------------------------------------------------- |
| 调用单位标记                        | 属性的值填「单位标记表达式」，如`setCustomTarget1: self.父单位` |
| 单位类型引用                        | 属性的值填「单位类型名」，如`UI中显示的单位: heavyTank`         |
| 单位标签                            | 填字符串标签，如`tags: 海, 对空`、`临时标签添加: buff`        |
| 消息标签                            | 仅`带标签发送消息`，配合 `自动触发事件: 新消息(需标签=...)`   |
| 事件触发                            | `自动触发事件` + 全部触发条件（含 withActionTag / withTag）     |
| 逻辑判断表                          | 填 if/then/else 条件逻辑                                          |
| 数值/价格/布尔/文字                 | 基本值类型                                                        |
| 效果/声音/抛射体/炮塔/动画/贴花引用 | 填资源 ID                                                         |

## 标签辨析：单位标签 / 消息标签 / 行动标签

**三者不是同一个东西，但共享同一套字符串标签系统**（同命名空间，值可互用）：

| 类型               | 权威类型        | 用于                                                                 | 例子                                                                        |
| ------------------ | --------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| **单位标签** | `tags`        | 单位身上的标签，用于统计/过滤/检索                                   | `tags: 海, 对空`、`临时标签添加: buff`、`接近单位(需标签="对地威胁")` |
| **消息标签** | `Message Tag` | 仅`带标签发送消息`，配合 `自动触发事件: 新消息(需标签=...)` 回调 | `带标签发送消息: hitZone` → `自动触发事件: 新消息(需标签="hitZone")`   |
| **行动标签** | —（事件参数）  | 仅自动触发事件`queueItemAdded(withActionTag=...)`，用于队列计数    | `自动触发事件: 队列添加项目(withActionTag="actionFire")`                  |

**关键点**：

- 单位标签作用在「单位」上（`tags:` 属性定义、`自身有标签` 检查、`接近单位`/`队伍中此单位数量` 过滤）。
- 消息标签作用在「消息」上（发送方 `带标签发送消息` 打标签，接收方 `新消息(需标签)` 匹配回调）。
- 二者值都是字符串，可互换（同一个标签既可以是单位标签也可以是消息标签），但**用途语义不同**。

# 一、值来源（可调用的表达式 / 函数）

> 这些是**可被调用**的值来源，可直接填入支持对应类型的属性值位置，支持 `.` 链式续写。

## 单位标记表达式（17 个）

### 当前动作目标（`thisActionTarget`）

**返回值类型** `unit / marker`

当前目标或定位的位置。
fireTurretXAtGround: mainGun (thisActionTarget==Marker with ground location)
alsoTriggerAction: x (thisActionTarget==Same as original action)
[turret]onShoot_triggerActions: x  (thisActionTarget==Target that was shot at)
takeResources_triggerActionIfAnyCollected: x (thisActionTarget==Target with resources)
addWaypoint_triggerActionIfMatched: x (thisActionTarget == Marker for move/Target for attack, etc. Note: use addWaypoint_maxTime:0 if you want to search only)

### 父单位（`parent`）

**返回值类型** `unit`

传输器或附件父级。 （注：单元在无连接槽位时运输时处于暂停状态）
Eg autoTrigger: if parent.energy > 100

```ini
自动触发: if 父单位.能量 > 100
```

### 附件（`attachment`）

**返回值类型** `unit`

（参数：[slot(槽位)]、[withTag(带有标签)]）
self.attachment(withTag='x').lastDamagedBy.getAsMarker()

```ini
setCustomTarget2: self.附属(需标签='x').最后伤害源.获取为标记()
```

### 攻击中（`attacking`）

**返回值类型** `unit`

当前正在攻击的目标，可能不是当前的路径点目标。
if attacking.tags(includes='bug') and attacking.hp < 20

```ini
锁定: if 攻击中.tags(有标签='bug') and 攻击中.hp < 20
```

### 活动路径点目标（`activeWaypointTarget`）

**返回值类型** `unit`

当前活动航路点目标。 包括攻击、运输、修理等。
if distanceBetween(self, activeWaypointTarget) < 100

```ini
可用: if 间距(self, 活动路径点目标) < 100
```

### 获取绝对偏移（`getOffsetAbsolute`）

**返回值类型** `marker`

([x],[y],[height]) 返回具有绝对偏移量的标记（-y 是北，+x 是东）

```ini
添加路径点来自参考: unitref 获取绝对偏移(self.x, self.y+1000, self.z)
```

### 获取为标记（`getAsMarker`）

**返回值类型** `marker`

在一个单位现在所在的位置创建一个临时标记。 标记的创建速度非常快，不再需要时会自动删除。 不链接到任何单位并且在单位死亡时仍然存在，并且在源移动时保持不变。

```ini
最后伤害源.获取为标记()
```

### 获取相对偏移（`getOffsetRelative`）

**返回值类型** `marker`

([x],[y],[height],[dirOffset]) 返回具有相对偏移量的标记。 (y+ 是向前)
self.getOffsetRelative(y=100).nearestUnit(withinRange=70, withTag='mouse') != null

```ini
self.获取相对偏移(y=100).接近单位(范围内=70, 需标签='mouse') != null
```

### 接近单位（`nearestUnit`）

**返回值类型** `unit`

(withinRange=500, withTag='x',relation='any') 搜索一个距离自己最近的单位（不推荐在 autoTrigger 中进行检查）

```ini
setCustomTarget1: 接近单位(需标签="derrick")
```

### 空单位（`nullUnit`）

**返回值类型** `unit`

返回一个空单位引用，用于比较

```ini
更多锁定2: if self.父单位 == 空单位
```

### 全局检查第一个单位（`globalSearchForFirstUnit`）

**返回值类型** `unit`

(withTag=x,relation=any) - 返回找到的与过滤器匹配的第一个（也是最旧的）单元。 慢，避免在 autoTrigger 检查中使用

```ini
self.全局检查第一个单位(需标签='gameController', 关系='中立')
```

### 事件来源（`eventSource`）

**返回值类型** `event`

根据来自 autoTriggerOnEvent 的事件设置“目标”（基本上是 thisActionTarget 但对于事件）

```ini
text: Event: %{事件来源}
```

### 运输（`transporting`）

**返回值类型** `unit`

（参数：[slot]）
if self.transporting(slot=0).hasResources(gold=100)

```ini
自动触发: if self.运输(slot=0).hasResources(gold=100)
```

### 自定义目标1（`customTarget1`）

**返回值类型** `unit`

自定义内存，默认为创建该单位的单位。
if parent.customTarget1 == self

```ini
锁定: if 父单位.自定义目标1 == self
```

### 自定义目标2（`customTarget2`）

**返回值类型** `unit`

自定义内存，默认为空

```ini
fireTurretXAtGround_withPosition: 自定义目标2
```

### 最后伤害源（`lastDamagedBy`）

**返回值类型** `unit`

最后一个攻击这个的单位。

```ini
teleportTo: 最后伤害源
```

### createMarker(x, y, [height], [teamId], [dir])

**返回值类型** `marker`

```ini
指定攻击目标: 创建标记(self.x + 500, self.y + 500)
```

---

## 数值表达式（30 个）

### 速度（`self.speed()`）

**返回值类型** `float`

Current unit speed（目前的单位速度）

```ini
更多锁定: if 自身速度 < 2
```

### 自身队伍id（`self.teamId()`）

**返回值类型** `int`

返回单位或标记的团队 ID。 从0开始。

```ini
switchToteam: 最后伤害源.teamId()
```

### 自身建造数量（`self.builtAmount()`）

**返回值类型** `float`

(似乎不包括使用autoTrigger建造的数量)How much this unit is built.  1  when complete. Note only events trigger on incomplete units, not autoTrigger（这个单位建造了多少。  1  完成后。仅注意不完整单位触发的事件，）

```ini
列表图像数量: int(12 * (自身建造数量 * 0.1))
```

### 自身角度（`self.dir()`）

**返回值类型** `float`

返回单位的方向

```ini
描述: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{自身角度}
```

### 自身运输数量（`self.transportingCount()`）

**返回值类型** `int`

载具运输单位数量(parameters: greaterThan, lessThan,equalTo,empty)（参数：大于，小于，等于，为空）

```ini
text: Load - %{自身运输数量()}
```

### 自身资源价格（`self.priceCredits()`）

**返回值类型** `float`

返回单位的资源价格

```ini
用逻辑添加资源: 资金 = 攻击中.priceCredits
```

### 自身资源类型（`self.resource.RESOURCE_TYPE`）

**返回值类型** `float`

此为快捷方式，可直接使用，原代码为self.resource(type='RESOURCE_TYPE')

```ini
用逻辑添加资源: hp += 自身资源.gold
```

### 自身最大移动速度（`self.maxMoveSpeed()`）

**返回值类型** `float`

自身最大移动速度

```ini
自动触发: if 内存.defaultSpeed < 自身最大移动速度
```

### cos(angle)

**返回值类型** `float`

```ini
cos(60) = 0.5
```

### direction(x1, y1, x2, y2)

**返回值类型** `float`

```ini
direction(self.x, self.y, 攻击中.x, 攻击中.y)
```

### directionBetween(unit1, unit2)

**返回值类型** `float`

```ini
之间方向(self, 最后伤害源)
```

### distance(x1, y1, x2, y2)

**返回值类型** `float`

计算两点之间的距离

```ini
锁定: if 距离(self.x, self.y, 当前动作目标.x, 当前动作目标.y) > 350
```

### distanceBetween(unit1, unit2)

**返回值类型** `float`

返回两个单位之间的距离

```ini
锁定: if 间距(self, 内存.antenna) > 1000
```

### distanceBetweenSquared(unit1, unit2)

**返回值类型** `float`

返回两个单位之间的平方距离。 比 distanceBetween 快一点

```ini
锁定: if distanceBetweenSquared(self, 内存.antenna) > 100
```

### distanceSquared(x1, y1, x2, y2)

**返回值类型** `float`

返回两点之距离差的平方。 比distance快，因为少了开方这一步。

```ini
锁定: if 距离方差(self.x, self.y, 当前动作目标.x, 当前动作目标.y) > 19
```

### game.mapHeight()

**返回值类型** `float`

检测正在游戏的地图高度

```ini
指定攻击目标: 创建标记(rnd(0, game.mapWidth), rnd(0, game.mapHeight))
```

### game.mapWidth()

**返回值类型** `float`

检测正在游戏的地图宽度

```ini
指定攻击目标: 创建标记(rnd(0, game.mapWidth), rnd(0, game.mapHeight))
```

### int(x)

**返回值类型** `int`

从数字中删除小数位。

```ini
列表图像数量: int(12 * (自身建造数量 * 0.1))
```

### length(string)

**返回值类型** `int`

以数字形式返回字符串或者值类型是字符串的逻辑长度

```ini
length(内存.sms)
```

### max(num1, num2)

**返回值类型** `float`

```ini
max(5, 10) == 10
```

### memory.NAME.size

**返回值类型** `int`

```ini
设置单位内存: squadSize = 内存.squad.size
```

### min(num1, num2)

**返回值类型** `float`

```ini
damage = min(自身血量, 自身能量)
```

### rnd(min, max)

**返回值类型** `float`

```ini
rnd(666, 888)
```

### self.id()

**返回值类型** `int`

返回自身的单位id，每个单位的id都不一样，如果id小的死了，后面生成的单位就会占用那个id

```ini
设置单位内存: UID = self.id()
```

### self.x()

**返回值类型** `float`

```ini
描述: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{自身角度}
```

### self.y()

**返回值类型** `float`

```ini
描述: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{自身角度}
```

### self.z()

**返回值类型** `float`

```ini
描述: [Position]\n[X]: %{self.x}\n[Y]: %{self.y}\n[Z]: %{self.z}\n[D]: %{自身角度}
```

### sin(angle)

**返回值类型** `float`

```ini
sin(10) = 0.173
```

### squareRoot(num)

**返回值类型** `float`

返回一个数的平方根

```ini
平方根(self.x + self.y)
```

### thisActionIndex

**返回值类型** `int`

```ini
alsoTriggerActionRepeat: thisActionIndex
```

---

## 布尔表达式（74 个）

### !=

**返回值类型** `bool`

如果两边的数字、单位、字符串、布尔值不同，则返回真

```ini
可见性: if 内存.message != 父单位.读取单位内存(name="stateMessage", type="string")
```

### <

**返回值类型** `bool`

如果左边的数字较小则返回真

```ini
锁定: if 自身弹药 < 1
```

### <=

**返回值类型** `bool`

如果左边的数字小于或等于右边，则返回真

```ini
需要条件: if 队伍中此单位数量(需标签="fish") >= 10
```

### ==

**返回值类型** `bool`

如果两边的数字、单位、字符串、布尔值相同，则返回真

```ini
隐藏: if 内存.tail == 0
```

### >

**返回值类型** `bool`

如果右侧的数字较小则返回真

```ini
自动触发: if 自身在水中 and 自身能量>=1
```

### >=

**返回值类型** `bool`

如果右侧的数字小于或等于右边，则返回真

```ini
需要条件: if 队伍中此单位数量(需标签="fish") <= 11
```

### 存活时间（`self.timeAlive()`）

**返回值类型** `float / bool`

单位存活时间。参数：几秒内，几秒后。(parameters:withinSeconds=X,laterThanSeconds=X)

```ini
锁定: if 存活时间() > 100
```

### 敌对中立拥有此单位数量（`numberOfUnitsInAggressiveTeam()`）

**返回值类型** `int / bool`

特殊的“对所有人都有侵略性”的队伍,这与numberOfUnitsInEnemyTeam不同.(参数:withTag, greaterThan, lessThan, withinRange, incompleteBuildings, factoryQueue

```ini
可见性: if 内存.deadzone.敌对中立拥有此单位数量() >= 30
```

### 敌人有此单位数量（`self.numberOfUnitsInEnemyTeam()`）

**返回值类型** `int / bool`

```ini
自动触发: if self.敌人有此单位数量(范围内=500) > 0
```

### 队伍获胜（`self.teamVictory()`）

**返回值类型** `bool`

队伍获胜

```ini
自动触发: if 队伍获胜()
```

### 队伍失败（`self.teamDefeatedTech()`）

**返回值类型** `bool`

队伍失败

```ini
自动触发: if 攻击中.teamDefeatedTech()
```

### 队伍无单位（`self.teamWipedOut()`）

**返回值类型** `bool`

(ps:都没单位了哪个单位检测队伍没单位?所以应该是只能由禁止受到攻击的单位(此单位不计入总数)来判断)

```ini
自动触发: if 队伍无单位()
```

### 队伍中此单位数量（`self.numberOfUnitsInTeam()`）

**返回值类型** `int / bool`

```ini
锁定: if 队伍中此单位数量(需标签='techUnlockBuilding', 少于=5)
```

### 高度（`self.height()`）

**返回值类型** `float / bool`

如果自身在特定高度则执行。参数:greaterThan,lessThan,equalTo,empty,full(参数，超过，少于，等于，空，满))

```ini
图像缩放: 1 + (自身高度 * 0.1)
```

### 护盾（`self.shield()`）

**返回值类型** `float / bool`

自身护盾数值。(参数:greaterThan,lessThan,equalTo,empty,full)参数：大于，小于，等于，空，满

```ini
可用: if 自身护盾() == 0
```

### 护盾值（`self.maxShield()`）

**返回值类型** `float / bool`

自身最大护盾

```ini
更多锁定2: if 自身能量 <= self.护盾值()/2
```

### 假（`false`）

**返回值类型** `bool`

不满足/非/错/假/0

```ini
自动触发: 假
```

### 盟友有此单位数量（`self.numberOfUnitsInAllyNotOwnTeam()`）

**返回值类型** `int / bool`

盟友拥有此单位的数量。(参数:withTag,greaterThan,lessThan,withinRange,incompleteBuildings,factoryQueue)

```ini
可用: if self.盟友有此单位数量() > 100
```

### 能量（`self.energy()`）

**返回值类型** `float / bool`

自身能量数值。参数:greaterThan,lessThan,equalTo,empty,full(参数，超过，少于，等于，空，满)

```ini
可见性: if 自身能量() > 50
```

### 全图中此单位数量（`numberOfUnitsInAllTeams()`）

**返回值类型** `int / bool`

所有队伍拥有单位数量。(参数:withTag,greaterThan,lessThan,withinRange,incompleteBuildings,factoryQueue)

```ini
text: Birds: %{全图中此单位数量(需标签="bird")}
```

### 生命值（`self.maxHp()`）

**返回值类型** `float / bool`

自身最大血量条件。

```ini
用逻辑设置资源: sauce = self.生命值 * 自身能量
```

### 拥有标志（`self.hasFlag()`）

**返回值类型** `bool`

自身拥有标志。可用通过增加资源和价格修改。（参数：id = 0-31），使用addResources在action中更改此vaule

```ini
自动触发: if 自身有标志(id=1)
```

### 有活动的路径点（`self.hasActiveWaypoint()`）

**返回值类型** `bool`

单位当前活动的路径点为此类型。类型=路径点类型。（[type = WAYPOINT_TYPE]）

```ini
text: Waypoint - %{self.有活动的路径点(type='移动')}
```

### 有全局标签（`self.globalTeamTags()`）

**返回值类型** `bool`

有全局标签，(parameters: includes)参数:包括

```ini
自动触发: if 有全局标签(有标签='animal')
```

### 真（`true`）

**返回值类型** `bool`

满足/是/对/为真/1，不建议使用1或0，不是所有代码都支持。

```ini
自动触发: 真
```

### 中立有此单位数量（`numberOfUnitsInNeutralTeam()`）

**返回值类型** `int / bool`

中立队伍拥有单位数量。(参数:withTag,greaterThan,lessThan,withinRange,incompleteBuildings,factoryQueue)

```ini
可见性: if 内存.deadzone.中立有此单位数量() >= 10
```

### 资源（`self.resource()`）

**返回值类型** `float / bool`

检查单个资源条件。（参数：greaterThan,lessThan,equalTo）参数，超过，少于，等于

```ini
可用: 自身资源(type=gold) >= 10
```

### 自身弹药（`self.ammo()`）

**返回值类型** `int / bool`

自身弹药条件。参数:greaterThan,lessThan,equalTo,empty,full(参数，超过，少于，等于，空，满)

```ini
锁定: if 自身弹药 < 1
```

### 自身弹药包括队列（`self.ammoIncludingQueued()`）

**返回值类型** `int / bool`

自身弹药数量，包括还在队列中的。参数:greaterThan,lessThan,equalTo,empty,full(参数：大于，小于，等于，空，满)

```ini
锁定: if 自身弹药包括队列(少于=12)
```

### 自身弹药空（`self.isAmmoEmpty()`）

**返回值类型** `bool`

自身弹药为空。self.ammo(empty=true)的快捷方式

```ini
更多锁定: 自身弹药空()
```

### 自身到最高速度（`self.isAtTopSpeed()`）

**返回值类型** `bool`

单位已经到达单位所能到达的最大速度

```ini
自动触发: if 自身到最高速度()
```

### 自身队列量（`self.queueSize()`）

**返回值类型** `int / bool`

自身队列数量。(参数:greaterThan,lessThan,equalTo,empty,full)参数：大于，小于，等于，空，满

```ini
锁定: if 自身队列量() >= 4
```

### 自身队伍无单位（`self.noUnitInTeam()`）

**返回值类型** `bool`

自身队伍单位数量不满足。(参数:withTag，withinRange，incompleteBuildings，factoryQueue)numberOfUnitsInTeam的别名

```ini
锁定: if 自身队伍无单位(需标签="infected")
```

### 自身队伍有单位（`self.hasUnitInTeam()`）

**返回值类型** `bool`

自身队伍拥有单位数量。(参数:withTag，withinRange，incompleteBuildings，factoryQueue)numberOfUnitsInTeam的别名

```ini
锁定: if 自身队伍有单位(需标签="fish")
```

### 自身建造完成（`self.completed()`）

**返回值类型** `bool`

(比如建造者造了一半的建筑就是0,建造完成就是1)Shortcut for checking built is（是检查已建成的快捷方式）

```ini
自动触发: if 自身建造完成()
```

### 自身能量包括队列（`self.energyIncludingQueued()`）

**返回值类型** `float / bool`

自身能量数值，包括还在队列中的。(参数:greaterThan,lessThan,equalTo,empty,full)参数：大于，小于，等于，空，满

```ini
可见性: if 自身能量包括队列()
```

### 自身能量空（`self.isEnergyEmpty()`）

**返回值类型** `bool`

自身能量为空。self.energy(empty=true)的快捷方式。

```ini
可用: if 自身能量空()
```

### 自身能量满（`self.isEnergyFull()`）

**返回值类型** `bool`

自身能量为满。self.energy(full=true)的快捷方式

```ini
锁定: if 自身能量满()
```

### 自身杀敌数（`self.kills()`）

**返回值类型** `int / bool`

自身杀敌数量。(参数:greaterThan,lessThan,equalTo)（参数：大于，小于，等于）

```ini
设置单位内存: score = 自身杀敌数()*3
```

### 自身受到伤害（`self.hasTakenDamage()`）

**返回值类型** `float / bool`

单位受到伤害。参数：几秒内，几秒后。(parameters:withinSeconds=X,laterThanSeconds=X)

```ini
可用: if 自身受到伤害(几秒内=1)
```

### 自身属于AI控制（`self.isControlledByAI()`）

**返回值类型** `bool`

单位归属于AI所属方控制。

```ini
text: Robot? %{选择(自身属于AI控制(), "Yes", "No")}
```

### 自身血量（`self.hp()`）

**返回值类型** `float / bool`

自身血量条件。参数:greaterThan,lessThan,equalTo,empty,full(超过，少于，等于，空，满)

```ini
用逻辑添加资源: 能量 += 选择(自身血量 > 自身能量, 10, 3)
```

### 自身有标签（`self.tags()`）

**返回值类型** `bool`

自身拥有标签。等同self.hasTags()

```ini
自动触发: if 自身有标签(有标签='fish')
```

### 自身有父单位（`self.hasParent()`）

**返回值类型** `bool`

用于子单位和载员逻辑。有父单位时执行。可选参数:需要标签。withTag ="unit"

```ini
可见性: if 自身有父单位
```

### 自身有资源（`self.hasResources()`）

**返回值类型** `bool`

可以检查多种资源（所有价格参数）

```ini
可用 自身有资源(资金=1, 能量=2)
```

### 自身有子单位（`self.numberOfAttachedUnits()`）

**返回值类型** `int / bool`

自身有子单位数量。（withTag,greaterThan,lessThan,equalTo）参数:标签，大于、小于、等于

```ini
text: Cores - %{自身有子单位()}
```

### 自身运输单位有标签（`self.transportingUnitWithTags()`）

**返回值类型** `bool`

自身运输单位有标签(parameters: includes)参数:包括

```ini
需要条件: if 自身运输单位有标签(有标签='human')
```

### 自身在地块类型上（`self.isOverPassableTile()`）

**返回值类型** `bool`

如果自身在这类通行地块上则执行，类型：无、陆地、建筑、空中、水面、两栖、跨悬崖、跨水和悬崖
NONE、LAND、BUILDING、AIR、WATER、HOVER、OVER_CLIFF、OVER_CLIFF_WATER

```ini
更多锁定: if 自身在地块类型上(type=跨悬崖和水面)
```

### 自身在地面高度（`self.isAtGroundHeight()`）

**返回值类型** `bool`

单位在地面,触发范围是-1~4，如果您单位高度在不断变化时超过此值的话，此条不一定触发，因为铁锈执行频率默认是1s.
等同于self.gound()或self.ground()

```ini
自动触发: if 当前动作目标.isAtGroundHeight()
```

### 自身在攻击（`self.isAttacking()`）

**返回值类型** `bool`

单位正在攻击。

```ini
设置单位状态: 移动速度=选择(自身在攻击, 4, 2)
```

### 自身在开阔地带（`self.isOverOpenLand()`）

**返回值类型** `bool`

如果自身在开阔地带则执行。self.isOverPassableTile（type ='LAND'）的快捷方式

```ini
隐藏: if not 自身在开阔地带()
```

### 自身在水上（`self.isOverwater()`）

**返回值类型** `bool`

如果自身投影在水面这种地形则执行。

```ini
锁定: if not 自身在水上()
```

### 自身在水下（`self.isUnderwater()`）

**返回值类型** `bool`

单位在水下，深水单位如潜艇。

```ini
自动触发: if 自身在水下()
```

### 自身在水中（`self.isInWater()`）

**返回值类型** `bool`

如果自身在水中则执行。等同于self.inwater()。

```ini
锁定: if not 自身在水中()
```

### 自身在天上（`self.isFlying()`）

**返回值类型** `bool`

如果单位在天上飞行，则执行。

```ini
可用: if 自身在天上()
```

### 自身在悬崖上（`self.isOverClift()`）

**返回值类型** `bool`

如果自身在悬崖上则执行。等同self.isOverCliff()

```ini
锁定: if not 自身在悬崖上()
```

### 自身在液体上（`self.isOverLiquid()`）

**返回值类型** `bool`

如果自身在液体则执行。岩浆算为液体。

```ini
锁定: if not 自身在液体上()
```

### 自身在移动（`self.isMoving()`）

**返回值类型** `bool`

如果自身在移动则执行。

```ini
锁定: if 自身在移动()
```

### 自身在中立队伍（`self.isOnNeutralTeam()`）

**返回值类型** `bool`

自身在中立队伍。

### 自身资源大于（`self.isResourceLargerThan`）

**返回值类型** `bool`

self.isResourceLargerThan

```ini
自动触发: 自身资源大于(source=oil, compareTarget=sauce, byMoreThan=100, multiplyTargetBy=0.2)
```

### 自身自定义计时器（`self.customTimer()`）

**返回值类型** `float / bool`

自身自定义计时器。参数：几秒内，几秒后。(parameters:withinSeconds=X,laterThanSeconds=X)

```ini
自动触发: if 自身自定义计时器() > 120
```

### 最后转换时间（`self.lastConverted()`）

**返回值类型** `float / bool`

单位最后转换时间。参数：几秒内，几秒后。(parameters:withinSeconds=X,laterThanSeconds=X)

```ini
更多锁定2: if 最后转换时间()
```

### and

**返回值类型** `bool`

需要and两边条件都满足执行。

```ini
自动触发: if 自身在水中 and 自身能量>=1
```

### game.nukesEnabled()

**返回值类型** `bool`

如果在此游戏的设置中启用了核武器，则返回 true。

```ini
可用: if not game.nukesEnabled()
```

### if

**返回值类型** `LogicBoolean`

所有逻辑布尔值使用if开始，除非仅使用true/false

```ini
可用: if 自身有标志(id=1)
```

### memory.NAME.contains(query)

**返回值类型** `bool`

```ini
自动触发: if 内存.savedCoord.contains(最后伤害源.自定义目标1.自定义目标1)
```

### not

**返回值类型** `bool`

将逻辑结果取反。

```ini
if not 自身在液体上
```

### or

**返回值类型** `bool`

or两边条件满足一个即可执行。

```ini
自动触发: if (自身能量>=2 or 自身弹药>=1) and 自身在天上
```

### self.isEnergyRecharging()

**返回值类型** `bool`

自身正在充能

```ini
text: Reload %{选择(self.isEnergyRecharging(), "", "[ready"}
```

### self.isInMap()

**返回值类型** `bool`

判断自身是否在地图内：地图外指那些黑色的边缘外面

```ini
自动触发: if not self.isInMap()
```

### self.isReversing()

**返回值类型** `bool`

逻辑布尔值

```ini
更多锁定2: if self.isReversing()
```

### self.maxEnergy()

**返回值类型** `float / bool`

自身最大能量

```ini
锁定: if 内存.kills < self.maxEnergy()
```

### self.numberOfQueuedWaypoints()

**返回值类型** `int / bool`

小括号里写路径点种类(parameters: greaterThan, lessThan,equalTo,empty,full)（参数：大于，小于，等于，为空，为满）

```ini
text: Waypoint Queue - %{self.numberOfQueuedWaypoints(type='移动攻击') >= 2}
```

### self.numberOfUnitsInAllyTeam()

**返回值类型** `int / bool`

```ini
描述: TEAM %{self.teamName}\n\nTank Population: %{self.numberOfUnitsInAllyTeam()}
```

---

## 文字表达式（7 个）

### debug(logicBoolean)

**返回值类型** `string`

返回一个文本字符串，帮助解释当前结果的原因。 可以查看嵌套逻辑、比较和运算符。

```ini
描述: [DEBUG] %{debug(self.x > self.y)}
```

### lowercase(string)

**返回值类型** `string`

给字符串中的所有大写字母进行小写处理

```ini
lowercase("Spaghetti Macaroni Lasagna") -> "spaghetti macaroni lasagna"
```

### self.playerName()

**返回值类型** `string`

返回游戏中玩家的昵称，类型为字符串

```ini
发送战争快报至所有: %{self.playerName()} 销毁 %{当前动作目标.playerName()}'s %{当前动作目标}
```

### self.teamName()

**返回值类型** `string`

返回自身队伍的名称，一般情况下，铁锈默认的都是ABCDEF

```ini
发送消息至所有: %{攻击中.teamName()} dominated %{当前动作目标.teamName()}
```

### str(x)

**返回值类型** `string`

将数字、单位、布尔值、逻辑显示值等转换为字符串

```ini
str(自身能量)+'x' == '100x'
```

### substring(text,start,end)

**返回值类型** `string`

例如 substring('hello',0,2) == 'he'

```ini
substring('hello',0,2) == 'he'
```

### uppercase(string)

**返回值类型** `string`

给字符串中的所有小写字母进行大写处理

```ini
uppercase("fish dish wish") -> "FISH DISH WISH"
```

---

## 通用表达式（9 个）

### -

**返回值类型** `same type`

两个数字相减所得值

```ini
设置单位内存: stockpile = 自身弹药 - 自定义目标2.资源.reserve
```

### *

**返回值类型** `same type`

两个数相乘所得值

```ini
设置单位内存: reserveCash = 内存.population * 自身资源.资金
```

### /

**返回值类型** `same type`

两个数字相除所得值

```ini
设置单位内存: damageMultiplierBuffer = 内存.population / 内存.death
```

### %

**返回值类型** `same type`

将两个数字相除并取余数部分作为计算所得值

```ini
设置单位内存: isEvenX = 选择(self.x % 2, 真, 假)
```

### +

**返回值类型** `same type`

添加两个数字或连接两个字符串

```ini
设置单位内存: stockpile = 自身弹药 + 自定义目标1.资源.reserve
```

### 读取单位内存（`readUnitMemory`）

**返回值类型** `all arguments`

(name:string, type:string{boolean,unit,float,string}, [default])

```ini
self.读取单位内存('ammoType', type='string'), 父单位.读取单位内存(''attachmentArray", type="unit[ ]", index=5), 攻击中.读取单位内存("wishlist", type="string[ ]")[12]
```

### eventData(name, type, [default])

**返回值类型** `any type`

```ini
text: %{eventData(name="msg", type="string")}
```

### memory.NAME

**返回值类型** `self only`

使用当前defineUnitMemory以及@memory类型读取自身的快捷方式。 不能在其他单元上使用，为此使用 readUnitMemory。 不要调用自身。

```ini
自动触发: if 内存.experience > 100
```

### select(bool, textA, textB)

**返回值类型** `any`

如果布尔值为真则返回 textA 否则返回 textB

```ini
设置单位内存: isEvenX = 选择(self.x % 2, 真, 假)
```

---

# 二、调用方（属性，每个代码接收什么值）

> 每个属性标注「接收值类型」+「可用节」。

## 调用单位标记（12 个）

### 创建时生成单位

**英文** `onCreateSpawnUnitOf` · **类型** `unit ref` · **权威类型** `unit ref` · **可用节** `附属`

此创造时也产生此子单位。

```ini
创建时生成单位
```

### 发送消息到

**英文** `sendMessageTo` · **类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

填单位/单位引用

```ini
发送消息到: unitref 接近单位(范围内=100, 需标签="fish").
```

### 绘制线条

**英文** `drawLineTo` · **类型** `marker ref` · **权威类型** `marker ref` · **可用节** `贴花`

Draws a line to this location affected by color and width（在此位置绘制一条受颜色和宽度影响的线）

```ini
绘制线条: 攻击中.自定义目标2
```

### 建造时临时转换为

**英文** `whenBuilding_temporarilyConvertTo` · **类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

准备此操作时临时转换为另一个单位。原始单位的操作将保留。

```ini
建造时临时转换为: cannon_uberState
```

### 提取资源包括引用

**英文** `takeResources_includeReference` · **类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

提取资源包括指定单位。

```ini
提取资源包括引用: self.最后伤害源
```

### 添加路径点来自参考

**英文** `addWaypoint_target_fromReference` · **类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

添加路径点来自单位信息引用。

```ini
添加路径点来自参考: self.内存.lastDock
```

### 中心位置

**英文** `basePosition` · **类型** `marker` · **权威类型** `marker` · **可用节** `贴花`

unit or marker to draw from as the base（作为基础绘制的单位或标记）

```ini
中心位置: self
```

### 转换成

**英文** `convertTo` · **类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

将您的单位转换为另一个单位。许多属性被保留。

```ini
转换成: fishLevel2
```

### setCustomTarget1

**类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

建议填单位引用

```ini
setCustomTarget1: self.父单位
```

### setCustomTarget2

**类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

建议填单位引用

```ini
setCustomTarget2: 最后伤害源
```

### teleportTo

**类型** `unit ref/marker` · **权威类型** `unit ref/marker` · **可用节** `行动`

传送到(填单位参考)

```ini
teleportTo: 内存.lastLocation
```

### transportTargetNow

**类型** `unit ref` · **权威类型** `unit ref` · **可用节** `行动`

主动装运目标(填单位参考)

```ini
transportTargetNow: 最后伤害源
```

---

## 单位类型引用（12 个）

### 产生单位

**英文** `spawnUnits` · **类型** `unitref` · **权威类型** `unitref` · **可用节** `行动`

填单位名，可用ini代码表里面的操作。例如mechBunker*1(falling=true)

```ini
eg: 产生单位: heavyTank, tank*5, hoverTank(偏移量x=10)
```

### 从单元添加描述

**英文** `descriptionAddFromUnit` · **类型** `unitRef/unitType` · **权威类型** `unitRef/unitType` · **可用节** `行动`

从指定单位添加单位描述。

```ini
从单元添加描述: builder
```

### 从单元添加名称

**英文** `textAddUnitName` · **类型** `unitRef/unitType` · **权威类型** `unitRef/unitType` · **可用节** `行动`

从指定单位添加名称。

```ini
 eg: 从单元添加名称: unitRef self.附属(slot="1")
```

### 从单元添加属性

**英文** `descriptionAddUnitStats` · **类型** `unitRef/unitType` · **权威类型** `unitRef/unitType` · **可用节** `行动`

从指定单位添加详细信息描述，比如攻击力，攻击范围。

```ini
从单元添加属性: unitRef self.父单位()
```

### 附属添加单位

**英文** `attachments_addNewUnits` · **类型** `unit types` · **权威类型** `unit types` · **可用节** `行动`

添加单位作为其子单位，需要定义附属位置。

```ini
附属添加单位: coreDefense*3
```

### 界面显示单位

**英文** `guiBuildUnit` · **类型** `unitRef/unitType` · **权威类型** `unitRef/unitType` · **可用节** `行动`

将action图标和鼠标指针改为指定单位，这就像您用建造者去造建筑，会受地形影响，可为action或路径点提供坐标。

```ini
界面显示单位: placeholderUnit
```

### 生产单位

**英文** `produceUnits` · **类型** `unitref` · **权威类型** `unitref` · **可用节** `行动`

类似spawnUnits，但是单位像正常生产一样，并获得路径的移动。1.13只对非建筑物生成的单位有效。

```ini
生产单位: builder*4, plasmaTank*8
```

### 添加单位到载具

**英文** `addUnitsIntoTransport` · **类型** `unitTypes` · **权威类型** `unitTypes` · **可用节** `行动`

将单位添加到运输槽中，在添加之前会检查空间是否够。

```ini
添加单位到载具: tank*3, heavyTank(neutralTeam=真)
```

### 添加路径点单位类型

**英文** `addWaypoint_unitType` · **类型** `unitTypes` · **权威类型** `unitTypes` · **可用节** `行动`

用于让当前单位去建造指定单位。仅用于addWaypoint_type:build

```ini
添加路径点单位类型: 炮塔
```

### 指定攻击目标

**英文** `fireTurretXAtGround_withTarget` · **类型** `unitref` · **权威类型** `unitref` · **可用节** `行动`

炮塔瞄准指示的单位或标记的位置发射

```ini
指定攻击目标: 最后伤害源
```

### alsoTriggerOrQueueActionWithTarget

**类型** `unitref` · **权威类型** `unitref` · **可用节** `行动`

更改其他触发动作的目标，默认为当前动作的目标。影响诸如 fireTurretXAtGround、spawnUnits、thisActionTarget()等。

```ini
alsoTriggerOrQueueActionWithTarget: 最后伤害源
```

### UI中显示的单位

**英文** `unitShownInUI` · **类型** `unitRef/unitType` · **权威类型** `unitRef/unitType` · **可用节** `行动`

使用此单位作为动作图标。

```ini
eg: UI中显示的单位: unitRef self.运输(slot=0)  or  UI中显示的单位: heavyTank
```

---

## 单位标签（15 个）

### 不攻击带特定标签单位

**英文** `canOnlyAttackUnitsWithoutTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `攻击` `炮塔`

不能攻击带特定标签的单位

```ini
不攻击带特定标签单位: corrosive, magnetic
```

### 检索标签

**英文** `searchTags` · **类型** `tag list` · **权威类型** `tag list` · **可用节** `放置规则`

搜索至少包含这些标签之一的任何单位

```ini
检索标签: factory
```

### 拦截抛射体需有标签

**英文** `interceptProjectiles_withTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `炮塔`

拦截具有此标签的抛射体，目前铁锈用于反核武器。

```ini
拦截抛射体需有标签: nuke
```

### 临时标签删除

**英文** `temporarilyRemoveTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `行动`

从单位上删除标签，直到将其转换或重置。

```ini
临时标签删除: fullJuice, emptySauce
```

### 临时标签添加

**英文** `temporarilyAddTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `行动`

将标签添加到单位，直到转换或重置。

```ini
临时标签添加: emptyJuice, fullSauce
```

### 提取资源标签

**英文** `takeResources_excludeUnitsWithoutTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `行动`

提取资源，但排除没有此标签的单位。

```ini
提取资源标签: truck, tanker
```

### 添加路径点检索标签

**英文** `addWaypoint_target_nearestUnit_tagged` · **类型** `tags` · **权威类型** `tags` · **可用节** `行动`

添加的路径点目标为靠近有此标签的单位。

```ini
添加路径点检索标签: assault, mechanized
```

### 添加全局标签

**英文** `addGlobalTeamTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `行动`

为玩家的团队添加此全局标签。与self.globalTeamTags()一起使用可创建解锁和升级。

```ini
添加全局标签: upgrade_energized, upgrade_research2
```

### 添加指定标签的随机单位

**英文** `addWaypoint_target_randomUnit_tagged` · **类型** `tags` · **权威类型** `tags` · **可用节** `行动`

添加路径点单位为指定标签的随机单位

```ini
添加指定标签的随机单位: chargers, strikers
```

### 修正除此标签

**英文** `mutatorX_ifUnitWithoutTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `抛射体`

伤害修正除此标签外有效。

```ini
mutator1_ifUnitWithoutTags: strongArmour
```

### 修正需要标签

**英文** `mutatorX_ifUnitWithTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `抛射体`

伤害修正所需标签，仅对携带标签单位有效。否则为原始伤害。

```ini
mutator1_ifUnitWithTags: infantry
```

### 移除全局标签

**英文** `removeGlobalTeamTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `行动`

从玩家队伍中删除此全局标签。

```ini
移除全局标签: buff_immune
```

### 只攻击带特定标签单位

**英文** `canOnlyAttackUnitsWithTags` · **类型** `tags` · **权威类型** `tags` · **可用节** `攻击` `炮塔`

只能攻击带特定标签的单位

```ini
只攻击带特定标签单位: metallic, ceramic
```

### 重新瞄准在飞行时针对标签

**英文** `retargetingInFlightSearchOnlyTags` · **类型** `tag ref` · **权威类型** `tag ref` · **可用节** `抛射体`

只针对具有这些标签的单位进行重定向。

```ini
重新瞄准在飞行时针对标签: lightArmor
```

### tags

**可按节区分类型** · **权威类型** `tags` · **可用节** `行动` `核心` `抛射体`

- **行动**：`tags`
- **核心**：`string(s)`
- **抛射体**：`tags`

模组标签，方便和其他模组进行区分

```ini
tags: actionFire
```

---

## 消息标签（1 个）

### 带标签发送消息

**英文** `sendMessageWithTags` · **类型** `Message Tag` · **权威类型** `Message Tag` · **可用节** `行动`

填指定标签

```ini
带标签发送消息: hitZone
```

---

## 逻辑判断表（33 个）

### 不可见

**英文** `invisible` · **可按节区分类型** · **权威类型** `LogicBoolean` · **可用节** `抛射体` `炮塔`

- **抛射体**：`bool`
- **炮塔**：`LogicBoolean`

隐藏抛射体图像。

```ini
不可见: 真
```

### 额外图标显示条件

**英文** `iconExtraIsVisible` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

设置图标什么时候可显示。

```ini
额外图标显示条件: if 自身资源.ammo < 1
```

### 更多锁定

**英文** `isLockedAlt` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动` `可建造`

另一个原因被锁定。用于显示不同的消息。

```ini
更多锁定: if numberOfUnitsInGame(需标签="factory") < 2
```

### 更多锁定2

**英文** `isLockedAlt2` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动` `可建造`

另一个原因被锁定。用于显示更多锁定的原因。

```ini
更多锁定2: if 接近单位(需标签="explosive", 范围内="500", 关系="己方").hp() < 10
```

### 攻击条件

**英文** `canAttackCondition` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `炮塔`

攻击需要满足条件。

```ini
攻击条件: if not self.flying
```

### 关联抛射体条件

**英文** `altProjectileCondition` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `炮塔`

用于altProjectile

```ini
关联抛射体条件: if 自身血量() < 100
```

### 界面闪烁

**英文** `isGuiBlinking` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动` `可建造`

此行为在界面中闪烁。

```ini
界面闪烁: 真
```

### 可攻击表面单位

**英文** `canAttackLandUnits` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `攻击` `炮塔`

可以攻击表面单位（包括陆地和水面）

```ini
可攻击表面单位: 真
```

### 可攻击空中单位

**英文** `canAttackFlyingUnits` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `攻击` `炮塔`

可以攻击空中单位，覆盖[attack]的设定。

```ini
可攻击空中单位: 假
```

### 可攻击水下单位

**英文** `canAttackUnderwaterUnits` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `攻击` `炮塔`

可以攻击水下单位

```ini
可攻击水下单位: 假
```

### 可见性

**英文** `isVisible` · **可按节区分类型** · **权威类型** `logic` · **可用节** `行动` `附属` `可建造` `贴花` `图像`

- **行动**：`LogicBoolean`
- **附属**：`bool`
- **可建造**：`LogicBoolean`
- **贴花**：`LogicBoolean`
- **图像**：`logic`

表明该帖花是否可以被我们看见(可以使用逻辑)

```ini
可见性: 真
```

### 可以攻击非接触水单位

**英文** `canAttackNotTouchingWaterUnits` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `攻击` `炮塔`

可以攻击非接触水单位，默认为true。如果是false，则只能攻击与水接触的单位，不能攻击岸上。用于鱼雷逻辑。也可以根据炮塔需要设置。

```ini
可以攻击非接触水单位: 假
```

### 可用

**英文** `isActive` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

默认为true。如果为false，则会禁用操作，并在用户界面中以红色显示。

```ini
可用: 真
```

### 空闲扫描条件

**英文** `idleSweepCondition` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `炮塔`

炮塔闲时扫描的条件，比如单位移动但没攻击时炮塔确实是闲置的，但这时可能不该旋转。

```ini
空闲扫描条件: if 自身血量() > 400
```

### 列表图像起始图像

**英文** `stack_indexStart` · **类型** `logicNumber` · **权威类型** `logicNumber` · **可用节** `贴花`

logic int (Starting image of the image stack)（动态整数（图像堆栈的起始图像））

```ini
列表图像起始图像: 0
```

### 列表图像数量

**英文** `stack_indexCount` · **类型** `logicNumber` · **权威类型** `logicNumber` · **可用节** `贴花`

logic int (Number of images to draw. Could be set to 1 to use stack_indexStart as an image picker)（动态整数（要绘制的图像数。可以设置为1以使用堆栈索引开始作为图像选择器））

```ini
列表图像数量: 10
```

### 锁定

**英文** `isLocked` · **可按节区分类型** · **权威类型** `bool` · **可用节** `行动` `可建造` `核心`

- **行动**：`LogicBoolean`
- **可建造**：`LogicBoolean`
- **核心**：`bool`

默认为false。如果true则禁用操作，则会显示一个锁定文本。可用作禁核或者科技树，或是条件限制。

```ini
锁定: if 自身资源.ammo < 1
```

### 图像X缩放

**英文** `imageScaleX` · **类型** `logicNumber` · **权威类型** `logicNumber` · **可用节** `贴花`

图像X缩放

```ini
图像X缩放: 1
```

### 图像Y缩放

**英文** `imageScaleY` · **类型** `logicNumber` · **权威类型** `logicNumber` · **可用节** `贴花`

图像Y缩放

```ini
图像Y缩放: (自身血量/self.生命值)*100
```

### 显示所有操作来源

**英文** `showAllActionsFrom` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `附属`

当被选中时，在父单元列表中显示所有附加单位的动作

```ini
显示所有操作来源
```

### 需要条件

**英文** `requireConditional` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

如果结果为false，则跳过此操作所有结果。

```ini
需要条件: if 自身资源.质量 < 300
```

### 也执行队列或需执行条件

**英文** `alsoTriggerOrQueueActionConditional` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

将此操作添加到序列。如果条件不满足(false)则忽略alsoQueueAction。默认true.

```ini
也执行队列或需执行条件: 假
```

### 载具可以卸载单位

**英文** `transportUnitsCanUnloadUnits` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `核心`

载具卸载单位需要满足条件，默认为“ if not self.isOverLiquid() and not self.isMoving()”(不在液体上且没有移动)

```ini
载具可以卸载单位: 假
```

### 载具内单位和载具一同死亡

**英文** `transportUnitsKillOnDeath` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `核心`

默认为true。如果载具死亡，则其中单位也死亡。

```ini
载具内单位和载具一同死亡: if 自身在液体上()
```

### 重置自定义计时器

**英文** `resetCustomTimer` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

重置自定义计时器，与self.customTimer()

```ini
重置自定义计时器: 真
```

### 资源取得条件

**英文** `generation_active` · **类型** `logicBoolean` · **权威类型** `logicBoolean` · **可用节** `核心`

获取资源条件。可用于受损时无法产出。

```ini
资源取得条件: if not 自身血量(少于=100)
```

### 自动触发

**英文** `autoTrigger` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

如果为true，则立即触发此操作的效果（忽略价格，isActive，isVisible，buildSpeed等）

```ini
自动触发: if self.overWater(), 自动触发: if 自身自定义计时器(几秒后=5)
```

### AI被禁用

**英文** `ai_isDisabled` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

默认为false。使用此操作停止AI使用此动作。

```ini
AI被禁用: 假
```

### AI高优先级

**英文** `ai_isHighPriority` · **类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `行动`

AI将优先执行此动作。

```ini
AI高优先级: 真
```

### alsoTriggerActionRepeat

**类型** `logicNumber` · **权威类型** `logicNumber` · **可用节** `行动`

logic number (Repeats the alsoTriggerAction call, index changed on each repeat) - Useful to create loops or work with arrays
（动态数字）（每次重复时改变索引 用于创建循环或处理数组）

```ini
alsoTriggerActionRepeat: 5
```

### builtFrom__isLocked

**类型** `LogicBoolean` · **权威类型** `LogicBoolean` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_isLocked

```ini
builtFrom_1_isLocked: if 自身血量(少于=100)
```

### setHeight

**类型** `logicNumber` · **权威类型** `logicNumber` · **可用节** `行动`

新增设置高度，可以使用逻辑

```ini
setHeight: 父单位.高度+5
```

### switchToTeam

**类型** `logicNumber` · **权威类型** `logicNumber` · **可用节** `行动`

切换到小队id。从0开始。(中立-1，敌对中立-2)

```ini
switchToTeam: 2, switchToTeam: 最后伤害源.teamId
```

---

## 数值（303 个）

### 摆动幅度

**英文** `wobbleAmplitude` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

抛射体飞行中摆动的剧烈程度。

```ini
摆动幅度: 10
```

### 摆动频率

**英文** `wobbleFrequency` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

抛射体飞行中摆动的频率。

```ini
摆动频率: 4
```

### 半径

**英文** `radius` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

单位的实际碰擦体积，也是实际的可选择范围。其光圈在displayRadius覆盖时真实体积不变。

```ini
半径: 20
```

### 波束偏移频率

**英文** `beamImageOffsetRate` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

波束图像的移动频率。

```ini
波束偏移频率: 1
```

### 产生机会

**英文** `spawnChance` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

抛射体产生几率，默认为1

```ini
产生机会: 1
```

### 充能时能量恢复速度

**英文** `energyRegenWhenRecharging` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

能量恢复是持续的，如果您设置了energyNeedsToRechargeToFull，那么攻击时按energyRegen恢复，耗尽时的灰条按此处设定值恢复。

```ini
充能时能量恢复速度: 0.1
```

### 初始非制导垂直速度

**英文** `initialUnguidedSpeedHeight` · **可用节** `抛射体`

设置抛射体和地面间的垂直速度，利用gravity制造出平滑的抛物线。比较迷，填1左右，重力可以不填。

```ini
初始非制导垂直速度: 30
```

### 初始缩放倍数

**英文** `scaleFrom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

效果初始时缩放倍数，用于效果从小到大缩放，或者反向缩放。

```ini
初始缩放倍数: 4
```

### 创建时高度偏移

**英文** `startingHeightOffset` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

单位在创建时高度。负数上升比较慢。

```ini
创建时高度偏移: 40
```

### 从载具中删除单位数

**英文** `deleteNumUnitsFromTransport` · **类型** `int` · **权威类型** `int` · **可用节** `行动`

从载具中删除指定数量单位。

```ini
从载具中删除单位数: 2
```

### 单位护盾显示半径

**英文** `shieldRenderRadius` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

护盾绘制半径，默认值比半径大一点。可以设置在单位上显示更大或更小的护盾圈。

```ini
单位护盾显示半径: 12
```

### 单位选择时显示圆圈

**英文** `displayRadius` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

修改选择单位时显示的绿色圆圈，不更改实际碰撞(radius)和可选择范围。

```ini
单位选择时显示圆圈: 20
```

### 弹道垂直飞行高度

**英文** `ballistic_delaymove_height` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

弹道导抛射体发射初期效果，决定弹道一开始先升高多少，再斜向爬升到最高过度。

```ini
弹道垂直飞行高度: 20
```

### 弹道高度

**英文** `ballistic_height` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

弹道最终高度。

```ini
弹道高度: 10
```

### 淡入时间

**英文** `fadeInTime` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

设置淡入效果时间，从透明到不透明。透明度值从0%到100%。

```ini
淡入时间: 2s
```

### 到达高度

**英文** `targetHeight` · **可按节区分类型** · **权威类型** `float` · **可用节** `腿` `运动`

- **腿**：`int`
- **运动**：`float`

最终高度

```ini
到达高度: 10
```

### 到达速度

**英文** `targetSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

加速到这个速度

```ini
到达速度: 5
```

### 倒车速度比率

**英文** `reverseSpeedPercentage` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

0.6默认值。超过0.4会在短距离（以40％的速度）时反转。 如果设置为1前进后退则不转弯。 填0时禁止倒退，必须旋转。

```ini
倒车速度比率: 0
```

### 动画_类型_结束

**英文** `animation_TYPE_end` · **类型** `int` · **可用节** `图像`

结束帧，必须更大然后开始

```ini
animation_moving_end: 3
```

### 动画_类型_开始

**英文** `animation_TYPE_start` · **类型** `int` · **可用节** `图像`

已弃用，请改用[animation]部分

```ini
animation_moving_start: 0
```

### 动画_类型_速度

**英文** `animation_TYPE_speed` · **类型** `float` · **可用节** `图像`

动画每一帧的延迟。较大的值会导致动画变慢

### 动画_类型_缩放结束

**英文** `animation_TYPE_scale_end` · **类型** `float` · **可用节** `图像`

比例单位图像。默认值为1。适用于生物单位或呼吸效果。

### 动画_类型_缩放开始

**英文** `animation_TYPE_scale_start` · **类型** `float` · **可用节** `图像`

比例单位图像。默认值为1。适用于生物单位或呼吸效果。

### 动画结束帧

**英文** `animateFrameEnd` · **类型** `int` · **权威类型** `int` · **可用节** `效果`

动画在哪一帧结束。

```ini
动画结束帧: 3
```

### 动画开始帧

**英文** `animateFrameStart` · **类型** `int` · **权威类型** `int` · **可用节** `效果`

动画从哪一帧开始,第一帧编号为0

```ini
动画开始帧: 0
```

### 动画帧重放

**英文** `animateFramePingPong` · **类型** `int` · **权威类型** `bool` · **可用节** `效果`

动画正序播放完毕后再倒序播放一次。

```ini
动画帧重放: 真
```

### 队伍色相

**英文** `teamColorRatio` · **权威类型** `float` · **可用节** `效果` `抛射体`

填0-1在图像上叠加阵营色，1为完全为阵营色。

```ini
队伍色相: 1
```

### 多向动画朝向

**英文** `direction_starting` · **类型** `float` · **权威类型** `float` · **可用节** `动画`

覆盖[graphics] animation_direction_starting，第一帧的方向，取决于您的素材。

```ini
多向动画朝向: 0
```

### 多向动画度数

**英文** `direction_units` · **类型** `float` · **权威类型** `float` · **可用节** `动画`

播放此动画时，覆盖[graphics] animation_direction_units。多向动画度数	45个代表8个方向，90个代表4个方向的动画。

```ini
多向动画度数: 45
```

### 多向动画x向

**英文** `direction_strideX` · **类型** `int` · **权威类型** `int` · **可用节** `动画`

覆盖[graphics] animation_direction_strideX，动画帧取值在方向改变时偏移。一般填0

```ini
多向动画x向: 20
```

### 多向动画y向

**英文** `direction_strideY` · **类型** `int` · **权威类型** `int` · **可用节** `动画`

覆盖[graphics] animation_direction_strideY，动画帧在方向改变Y轴偏移偏移。与frame_height一起使用。一般填1

```ini
多向动画y向: 50
```

### 额外建造距离

**英文** `extraBuildRangeWhenBuildingThis` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

此单位额外的建造距离，可以使建造者在更远的地方建造它，多用于水上建筑。

```ini
额外建造距离: 90
```

### 范围半径

**英文** `areaRadius` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

范围伤害区域大小，此区域内敌方单位都将受到损伤，默认向边缘递减。数值类型为像素，大小算法为半径，铁锈内地块一格长为20像素。

```ini
范围半径: 100
```

### 范围扩展时间

**英文** `areaExpandTime` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

将区域伤害变为爆炸波向边缘扩散。效果如核抛射体。

```ini
范围扩展时间: 1s
```

### 范围伤害

**英文** `areaDamage` · **类型** `int` · **权威类型** `int` · **可用节** `抛射体`

范围伤害值，此区域内敌方单位都将受到损伤，默认向边缘递减。

```ini
范围伤害: 10
```

### 范围伤害忽略区域

**英文** `areaIgnoreUnitsCloserThan` · **类型** `int` · **权威类型** `int` · **可用节** `抛射体`

小于此范围的单位不会受到攻击影响。需要比areaRadius小，否则造不成伤害。

```ini
范围伤害忽略区域: 10
```

### 附属删除单位数

**英文** `attachments_deleteNumUnits` · **类型** `int` · **权威类型** `int` · **可用节** `行动`

删除单位数量

```ini
附属删除单位数: 3
```

### 复位角度

**英文** `resetAngle` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

复位角度。

```ini
复位角度: float
```

### 高度

**英文** `height` · **类型** `float` · **权威类型** `float` · **可用节** `附属` `炮塔`

子单位高度。

```ini
高度
```

### 高度浮动

**英文** `targetHeightDrift` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

默认0，空军1.5.单位高度上下浮动高度。

```ini
高度浮动: 1
```

### 高度改变速率

**英文** `heightChangeRate` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

单位改变高度的频率，无论是转换还是浮动

```ini
高度改变速率: 3
```

### 高度绘制偏移

**英文** `image_offsetH` · **类型** `int` · **权威类型** `int` · **可用节** `图像`

图像在高度上的偏移

```ini
高度绘制偏移: 25
```

### 高度偏移

**英文** `hOffset` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `效果`

- **贴花**：`int`
- **效果**：`float`

height offset（高度偏移）

```ini
高度偏移: 10
```

### 高度速度

**英文** `hSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

高度移动速度。正数向上，负数向下。

```ini
高度速度: 1
```

### 工厂速度乘数

**英文** `nanoFactorySpeed` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为1。如果此单位是工厂，则乘以创建单位的buildSpeed值。用于加速或者减速建造。

```ini
工厂速度乘数: 1.2
```

### 攻击间隔

**英文** `shootDelay` · **类型** `float` · **权威类型** `float` · **可用节** `攻击`

开火间隔，也可以在每个转塔上使用延迟

```ini
攻击间隔: 21, 攻击间隔: 5s
```

### 攻击距离

**英文** `maxAttackRange` · **类型** `float` · **权威类型** `float` · **可用节** `攻击`

最大攻击距离。（会乘以globalScale，但不建议使用globalScale）

```ini
攻击距离: 300
```

### 后坐力大小

**英文** `recoilOffset` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

开火后向前或向后推动炮塔，以产生后坐力。填像素。

```ini
后坐力大小: 10
```

### 后坐力恢复

**英文** `recoilReturnTime` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

开火后回到默认位置所需时间。

```ini
后坐力恢复: 0.5s
```

### 后坐力速度

**英文** `recoilOutTime` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

开火后到达偏移位置的时间

```ini
后坐力速度: 1s
```

### 护盾恢复速度

**英文** `shieldRegen` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

此数值决定每帧增加护盾值，游戏内一秒为60帧，所以不要写太大。可以写负值。

```ini
护盾恢复速度: 0.15
```

### 护盾伤害乘数

**英文** `shieldDamageMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

默认为1。对护盾伤害乘数。如0对护盾不造成伤害，2对护盾造成双倍伤害

```ini
护盾伤害乘数: 1.4
```

### 护盾损伤乘数

**英文** `hullDamageMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

可以用来制造只影响护盾的电磁脉冲武器。0忽略单位，只伤害护盾

```ini
护盾损伤乘数: 2
```

### 护盾消失速度

**英文** `shieldDeflectionDisplayRate` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为4。数值越大消失越快。

```ini
护盾消失速度: 3
```

### 护盾值

**英文** `maxShield` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

单位最大护盾值，默认生成时即为此值。如果设置了startShieldAtZero:true，则初始为0.

```ini
护盾值: 500
```

### 护盾阻挡乘数

**英文** `shieldDefectionMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

护盾阻挡乘数，填0则可无视护盾，同时对单位和盾造成伤害。

```ini
护盾阻挡乘数: 1.55
```

### 回收价格

**英文** `reclaimPrice` · **类型** `int` · **权威类型** `resources` · **可用节** `核心`

自定义资源，类似价格。对于自定义资源很有用。

```ini
回收价格: gold=1000
```

### 回收距离

**英文** `nanoRangeForReclaim` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

此单位回收距离。

```ini
回收距离: 60
```

### 回收速率

**英文** `resourceRate` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

回收速度。每帧回收血量。与canReclaimResources一起使用。允许其他队伍回收该单位。通常与中立队伍配合使用。使用价格来设置获取哪些资源。

```ini
回收速率: 100
```

### 回收效果频率

**英文** `reclaimEffectRate` · **类型** `int` · **权威类型** `int` · **可用节** `图像`

回收效果每隔多少帧产生一次。

```ini
回收效果频率: 20
```

### 绘制大小

**英文** `drawSize` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

缩放抛射体大小。默认为1

```ini
绘制大小: 1.5
```

### 绘制角度偏移

**英文** `drawDirOffset` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

绘制角度偏移。

```ini
绘制角度偏移: 73
```

### 基地内没有时优先级

**英文** `noneInBaseExtraPriority` · **类型** `float` · **权威类型** `float` · **可用节** `AI`

如果AI基地中不存在此单位，则增加其优先级。

```ini
基地内没有时优先级: 2
```

### 激光防御能量需求

**英文** `laserDefenceEnergyUse` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔启用激光防御拦截敌方抛射体。还应在core中设置energyMax。

```ini
激光防御能量需求: 0.25
```

### 激光拦截耗能

**英文** `deflectionPower` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

默认值为1。此抛射体被激光防御摧毁所需的能量。 -1为禁止拦截。（对特殊武器如火焰）

```ini
激光拦截耗能: -1
```

### 加速度

**英文** `moveAccelerationSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

定义单位加速到最大速度的加速度。

```ini
加速度: 0.07
```

### 减速度

**英文** `moveDecelerationSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

与加速度相对应得减速度。不要设的太小。

```ini
减速度: 0.17
```

### 检索偏移x

**英文** `searchOffsetX` · **类型** `float` · **权威类型** `float` · **可用节** `放置规则`

检索偏移，默认为0

```ini
检索偏移x: 100
```

### 检索偏移y

**英文** `searchOffsetY` · **类型** `float` · **权威类型** `float` · **可用节** `放置规则`

检索偏移，默认为0

```ini
检索偏移y: 200
```

### 建造时透明度

**英文** `whenBeingBuiltMakeTransparentTill` · **类型** `float` · **权威类型** `float` · **可用节** `图像`

设为0则一开始便完全不透明，设为1则直到建造完成时才不透明。

```ini
建造时透明度: 0.4
```

### 建造时旋转

**英文** `whenBuilding_rotateTo` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

准备此动作时，将单位主体旋转到该方向

```ini
建造时旋转: 45
```

### 建造优先级

**英文** `buildPriority` · **类型** `float` · **权威类型** `float` · **可用节** `AI`

建造优先级，填0-1。 越大越AI越可能造。Luke的的第一座土地工厂使用0.8，空中工厂使用0.48，第一炮塔使用0.47。

```ini
建造优先级: 1
```

### 建筑覆盖范围

**英文** `constructionFootprint` · **类型** `ints` · **权威类型** `ints` · **可用节** `核心`

基础规则同上，这个区域内不能建造建筑，但是单位可以通过。一般需要设定比上一个面积大。好处是即使建造的密密麻麻，单位也有路可走

```ini
建筑覆盖范围: -1,-1,1,3
```

### 建筑碰撞范围

**英文** `footprint` · **类型** `ints` · **权威类型** `ints` · **可用节** `核心`

填四个数值(左，上，右，下)，单位是格子，建筑默认占一格，向左和向上需要填负数，填整数在选择时有偏移。
四个数值可以完全颠倒，这样不会阻碍单位移动。
决定建筑单位碰撞体积，此区域内单位不能通过，如果在单位运动途中单位则会绕过。单位实际可选择面积使用的是radius。

```ini
建筑碰撞范围: 0,0,1,1
```

### 建筑伤害乘数

**英文** `buildingDamageMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

默认为1。对建筑物伤害乘上此数值，用于伤害修正。比如闪电对建筑物伤害低，火焰对建筑物伤害高。

```ini
建筑伤害乘数: 3
```

### 建筑选择UI

**英文** `displayFootprint` · **类型** `ints` · **权威类型** `ints` · **可用节** `核心`

基础规则同上，用于选择单位时UI显示。不设定则默认为建筑覆盖区域。

```ini
建筑选择UI: 0,0,1,1
```

### 建筑UI调整

**英文** `buildingSelectionOffset` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

默认值为0。以像素为单位对选框额外设定。

```ini
建筑UI调整: 4
```

### 建筑X轴偏移

**英文** `buildingToFootprintOffsetX` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

设置X轴位置偏移，默认10.若设置非对称的建筑，单位会根据覆盖范围会挤在角上，这时如果要准确显示就需要额外偏移。与直接使用图像偏移的区别:
由于铁锈判断选中单位是根据单位体积(radius),因此图像偏移实际上单位的位置不变，会容易造成点击时的误差。而当前代码实际上是移动单位，则不会造成这个问题。

```ini
建筑X轴偏移: 4
```

### 建筑Y轴偏移

**英文** `buildingToFootprintOffsetY` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

设置Y轴位置偏移，默认10.其它参上条

```ini
建筑Y轴偏移: 4
```

### 降落加速度

**英文** `fallingAcceleration` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

单位降落时的加速度

```ini
降落加速度:
```

### 角度偏移

**英文** `dirOffset` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `效果`

- **贴花**：`int`
- **效果**：`float`

角度偏移

```ini
角度偏移: 45
```

### 结束缩放倍数

**英文** `scaleTo` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

效果结束时缩放倍数。

```ini
结束缩放倍数: 2
```

### 近战索敌距离

**英文** `meleeEngangementDistance` · **类型** `int` · **权威类型** `int` · **可用节** `攻击`

使部队游猎，自动移动以攻击附近的敌方部队。近战默认为250，非近战默认为0（即使非近战也可以使用。）

```ini
近战索敌距离: 400
```

### 开火冻结移动

**英文** `onShoot_freezeBodyMovementFor` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

射击时禁止运动。

```ini
开火冻结移动: 2s
```

### 开火间隔

**英文** `delay` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

延迟一定时间后显示

```ini
开火间隔: 2s
```

### 开火间隔乘数

**英文** `shootDelayMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `攻击`

默认为1。可以在setUnitStats动态改变

```ini
开火间隔乘数: 0.75
```

### 开火间隔过渡

**英文** `warmupShootDelayTransfer` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

默认值为0，这是一个乘数，用于通过预热值减少下一次开火延迟。与warmupNoReset一起使用时，可以使攻击速度越来越快。

```ini
开火间隔过渡: 20
```

### 开火伤害乘数

**英文** `shootDamageMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `攻击`

默认为1。可以在setUnitStats动态改变

```ini
开火伤害乘数: 2.43
```

### 开火音效大小

**英文** `shoot_sound_vol` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

播放声音大小。

```ini
开火音效大小: 0.4
```

### 科技

**英文** `tech` · **类型** `int` · **权威类型** `int` · **可用节** `可建造`

没啥用。科技等级。通常只会影响此单位界面中的颜色。默认为1，只能填1、2、3。

```ini
科技: 2
```

### 科技等级

**英文** `techLevel` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

“techLevel”单位生成参数,主要用于内置的单元和设置damagingBorder大小。

```ini
科技等级: 1
```

### 可攻击最大角度

**英文** `canAttackMaxAngle` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

设置炮塔的最大射击角度。如果单位在可攻击角度外则不会开火。默认值为5，不要设置得更低。可以设置为181，即不需要转向就可发射抛射体。

```ini
可攻击最大角度: 181
```

### 可同时被此数目单位回收

**英文** `resourceMaxConcurrentReclaimingThis` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

默认为无限制。限制有多少单位可以同时回收此资源。

```ini
可同时被此数目单位回收: 3
```

### 空闲扫描角度

**英文** `idleSweepAngle` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

炮塔闲时扫描角度。这一系列用于设置炮塔闲置时候转圈警戒动作。

```ini
空闲扫描角度: 35
```

### 空闲扫描速度

**英文** `idleSweepSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔闲时扫描转向的速度。

```ini
空闲扫描速度: 0.3
```

### 空闲扫描随机角度

**英文** `idleSweepAddRandomAngle` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

炮塔闲时扫描的随机角度

```ini
空闲扫描随机角度: 15
```

### 空闲扫描随机延迟

**英文** `idleSweepAddRandomDelay` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔闲时扫描的随机延迟

```ini
空闲扫描随机延迟: 30
```

### 空闲扫描延迟

**英文** `idleSweepDelay` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔闲时扫描间隔，多久动一次。

```ini
空闲扫描延迟: 20
```

### 拦截抛射体高度

**英文** `interceptProjectiles_andOverHeight` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

需要超过此高度才会拦截。默认值为0.

```ini
拦截抛射体高度: 10
```

### 拦截抛射体攻击范围

**英文** `interceptProjectiles_andUnderDistance` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

默认值为2000，当抛射体到达这个距离时才开始发射拦截。

```ini
拦截抛射体攻击范围: 300
```

### 拦截抛射体检索范围

**英文** `interceptProjectiles_andTargetingGroundUnderDistance` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

守护的范围，只有当敌方攻击目标地点落在此圈子内时才考虑拦截。

```ini
拦截抛射体检索范围: 100
```

### 离开高度

**英文** `exit_heightOffset` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为0。定义创建或卸载的单位出现的高度。

```ini
离开高度: 16
```

### 离开距离

**英文** `exit_moveAwayAmount` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为70。定义创建或卸载的单位与该单位的距离。

```ini
离开距离: 10
```

### 离开旋转角度

**英文** `exit_dirOffset` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

单位默认为180(朝下)，建筑物默认为0(朝右)。定义已创建或卸载单位的退出方向。

```ini
离开旋转角度: 140
```

### 离开x轴

**英文** `exit_x` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

从载具卸载或者工厂生产完成时，单位出现的位置，默认为0

```ini
离开x轴: 0
```

### 离开y轴

**英文** `exit_y` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

从载具卸载或者工厂生产完成时，单位出现的位置，默认为5，正数向下。

```ini
离开y轴: 5
```

### 列表图像高度偏移

**英文** `stack_hOffset` · **类型** `float` · **权威类型** `float` · **可用节** `贴花`

for 3d voxel style units.（用于三维立体体积元素样式单位。）

```ini
列表图像高度偏移: 4
```

### 列表图像帧偏移

**英文** `stack_frameOffset` · **类型** `int` · **权威类型** `int` · **可用节** `贴花`

for 3d voxel style units.（用于三维立体体积元素样式单位。）

```ini
列表图像帧偏移: 1
```

### 每个基地此采集者数量

**英文** `whenUsingAsHarvester_recommendedInEachBase` · **类型** `int` · **权威类型** `int` · **可用节** `AI`

当此单位用作采集者时，每个基地推荐的采集者数量。

```ini
每个基地此采集者数量: 3
```

### 每个基地最大数量

**英文** `maxEachBase` · **类型** `int` · **权威类型** `int` · **可用节** `AI`

每个基地最多拥有的数量。

```ini
每个基地最大数量: 10
```

### 瞄准偏移

**英文** `aimOffsetSpread` · **类型** `float` · **可用节** `攻击` `炮塔`

将每次攻击时的偏移量乘以目标半径。默认为0.6。设为0则不偏移，对范围武器影响较大。

```ini
瞄准偏移:0
```

### 末端角度偏移值

**英文** `endDirOffset` · **类型** `int` · **权威类型** `int` · **可用节** `腿`

目标脚/末端相对于身体的旋转角度。

```ini
末端角度偏移值: 45
```

### 目标为地面高度偏移

**英文** `targetGroundHeightOffset` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

在目标上方或下方射击。可能对分裂和落下的抛射体有用。

```ini
目标为地面高度偏移: 10
```

### 目标为地面散布

**英文** `targetGroundSpread` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

填数值，用于目标为地面时随机造成偏差效果

```ini
目标为地面散布: 20
```

### 能量初始百分比

**英文** `energyStartingPercentage` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

单位生成时所携带的能量百分比。

```ini
能量初始百分比: 0.5
```

### 能量恢复速度

**英文** `energyRegen` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

能量每帧恢复速度，游戏内一秒为60帧，所以不要写太大。可以写负值。

```ini
能量恢复速度: 0.001
```

### 能量需求

**英文** `energyUsage` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

发射武器所需的能量。与resourceUsage相同：energy = X

```ini
能量需求: 1
```

### 能量值

**英文** `energyMax` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为0。可以用作炮塔，激光防御和行动的弹药的能量。

```ini
能量值: 1
```

### 排序

**英文** `order` · **类型** `float` · **权威类型** `float` · **可用节** `贴花`

Order with other decals, defaults 0 and uses order it appears in ini（使用其他贴花排序，默认为0，并使用其在ini中显示的顺序）

```ini
排序: 2
```

### 炮弹高度

**英文** `barrelHeight` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

高度（用于3d）。影响抛射体产生的高度。

```ini
炮弹高度: 10
```

### 炮弹x

**英文** `barrelX` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

默认值为0。控制抛射体生成的X轴位置。

```ini
炮弹x: 0
```

### 炮弹y

**英文** `barrelY` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

默认为大0。注意：size和barrelY含义相同

```ini
炮弹y: 5
```

### 炮塔大小

**英文** `turretSize` · **类型** `float` · **可用节** `攻击`

设置所有炮塔大小。填数值，按像素计算，不决定素材大小，而是以此为半径的开火位置。

### 炮塔图像缩放

**英文** `scaleTurretImagesTo` · **类型** `float` · **权威类型** `float` · **可用节** `图像`

单位像素。缩放炮塔图像至指定像素，只用填宽度

```ini
炮塔图像缩放: 1.2
```

### 炮塔旋转加速度

**英文** `turnSpeedAcceleration` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔加速度，默认设置为禁用，并使用全转速度。

```ini
炮塔旋转加速度: 0.4
```

### 炮塔旋转减速度

**英文** `turnSpeedDeceleration` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

默认为turnSpeedAcceleration。将此值设置为高于转弯加速度可能会允许更快地命中目标

```ini
炮塔旋转减速度: 0.2
```

### 炮塔转速

**英文** `turretTurnSpeed` · **类型** `float` · **可用节** `攻击`

炮塔转速，单位是每帧旋转角度。

### 碰撞体积软化

**英文** `softCollisionOnAll` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

与其他单位碰撞时产生柔和的碰撞效果，填负数会导致单位相互吸引。

```ini
碰撞体积软化: 3
```

### 奇数大小的图像像素调整

**英文** `image_floatingPointSize` · **类型** `int` · **权威类型** `int` · **可用节** `图像`

(填写0/1,无直观区别,有说法是把x-1来改变单位坐标系使其更方便使用)--Fixes off by 1 pixel sizing for odd sized images -

```ini
奇数大小的图像像素调整: 10
```

### 奇数射击时炮弹偏移

**英文** `barrelOffsetX_onOddShots` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

默认0。在奇数射击时横向偏移，用于简化双管单位制作。

```ini
奇数射击时炮弹偏移: -10
```

### 强制卸载指定槽位单位

**英文** `forceUnloadTransportNow_onlyOnSlot` · **类型** `int` · **权威类型** `string` · **可用节** `行动`

强制卸载指定槽位单位

```ini
强制卸载指定槽位单位: drop1
```

### 全地图推荐采集者数量

**英文** `whenUsingAsHarvester_recommendedGlobal` · **类型** `int` · **权威类型** `int` · **可用节** `AI`

当此单位用作采集者时，全图推荐的采集者数量。

```ini
全地图推荐采集者数量: 20
```

### 全局缩放

**英文** `globalScale` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

废弃代码，不要用。将一个单位按比例放大，但不放大攻击力。默认值为1。不建议更改。

```ini
全局缩放: 2
```

### 全图没有时优先级

**英文** `noneGlobalExtraPriority` · **类型** `float` · **权威类型** `float` · **可用节** `AI`

如果此单位在地图上的任何位置都不存在，则增加其优先级。

```ini
全图没有时优先级: 4
```

### 全图最大数量

**英文** `maxGlobal` · **类型** `int` · **权威类型** `int` · **可用节** `AI`

全图最多拥有的数量。

```ini
全图最大数量: 50
```

### 扫描偏移

**英文** `sweepOffset` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

扫描偏移，用于制作类似于两栖护盾坦克的激光武器。对光束效果很有用。

```ini
扫描偏移: 0.5
```

### 扫描偏移目标半径

**英文** `sweepOffsetFromTargetRadius` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

以目标半径作为扫描偏移量乘数，用于制作类似于两栖护盾坦克的激光武器。对光束效果很有用。

```ini
扫描偏移目标半径: 0.4
```

### 扫描速度

**英文** `sweepSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

扫描速度，用于制作类似于两栖护盾坦克的激光武器。对光束效果很有用。

```ini
扫描速度: 1.5
```

### 闪光大小

**英文** `lightSize` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

抛射体光照大小,单位半径是地块格子

```ini
闪光大小: 1.5
```

### 设置身体旋转

**英文** `setBodyRotation` · **类型** `dynamic` · **权威类型** `dynamic` · **可用节** `行动`

设置单位旋转度数。单位朝右为0度。因为是结果而不是准备过程，所以很突然。（支持动态数字比如memory）

```ini
设置身体旋转: 270, 设置身体旋转: 之间方向(self, 自定义目标1)
```

### 设置完成度

**英文** `setBuilt` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

默认为1。填0-1之间的数字，将指定单位建造完成度。比如填0.5，执行完操作后单位自身建造完成度就成了50%

```ini
设置完成度: 0.5
```

### 生命恢复速度

**英文** `selfRegenRate` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

此数值决定每帧增加血量。游戏内默认速度下，一秒为60逻辑帧，而您看到的FPS帧数为渲染帧，所以电脑上几百帧和手机上60帧和省电模式下30帧并不影响计算。所以不要写太大。可以写负值用于自毁。

```ini
生命恢复速度: 0.01
```

### 生命值

**英文** `maxHp` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

单位最大生命值，默认生成时即为此值。

```ini
生命值: 200
```

### 视野

**英文** `fogOfWarSightRange` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

设置单位视野，在战争迷雾中可以看到的瓷砖数量。默认为15。

```ini
视野: 35
```

### 枢轴偏移随机

**英文** `pivotOffsetRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

按此值随机更改朝向。

```ini
枢轴偏移随机: 34
```

### 死亡产生碎片

**英文** `numBitsOnDeath` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

定义该单位死亡时散落的碎片的数量。

```ini
死亡产生碎片: 20
```

### 死亡核爆范围

**英文** `nukeOnDeathRange` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

不推荐使用。定义使用nukeOnDeath时的核抛射体效果范围。

```ini
死亡核爆范围: 140
```

### 死亡核爆伤害

**英文** `nukeOnDeathDamage` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

不推荐使用。定义使用nukeOnDeath时的核武器效果区域伤害。

```ini
死亡核爆伤害: 2000
```

### 搜索距离

**英文** `searchDistance` · **类型** `float` · **权威类型** `float` · **可用节** `放置规则`

搜索距离

```ini
搜索距离: 500
```

### 搜寻资源范围

**英文** `canReclaimResourcesNextSearchRange` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

当一处资源采集完后，搜寻更多资源的范围。

```ini
搜寻资源范围: 100
```

### 速度

**英文** `speed` · **类型** `float` · **权威类型** `float` · **可用节** `动画` `抛射体`

抛射体飞行速度，单位是每帧飞行像素。铁锈地块1格20像素，填1则每秒飞行3格。

```ini
速度 : float
```

### 速度散布

**英文** `speedSpread` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

随机改变抛射体初始速度

```ini
速度散布: 3
```

### 随机高度偏移

**英文** `hOffsetRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

按此值随机更改初始值。

```ini
随机高度偏移: 5
```

### 随机高度速度偏移

**英文** `hSpeedRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

按此值随机更改初始值。

```ini
随机高度速度偏移: 1
```

### 随机角度偏移量

**英文** `dirOffsetRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

按此值随机更改偏移角度。

```ini
随机角度偏移量: 50
```

### 随机转速

**英文** `dirSpeedRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

按此值随机更改旋转速度。

```ini
随机转速: 3
```

### 缩放到当前大小或更大时绘制

**英文** `onlyWithZoomLevelOrMore` · **类型** `float` · **权威类型** `float` · **可用节** `贴花`

onscreen. Recommended to be set on decals for 3d voxel style units, small details or small shadows.Would hide decal when zoomed out a bit. Useful to reduce draw calls for performance when a lot of units might be
（屏幕上。建议设置在三维立体体积元素样式单位、小细节或小阴影的贴花上。缩小一点时会隐藏贴花。当许多单元可能需要时，有助于减少性能的调用）

```ini
缩放到当前大小或更大时绘制: 0.4
```

### 缩放结束

**英文** `scale_end` · **类型** `float` · **可用节** `动画`

data.sections.animation.scale_end

```ini
缩放结束 : float
```

### 缩放开始

**英文** `scale_start` · **类型** `float` · **可用节** `动画`

data.sections.animation.scale_start

```ini
缩放开始 : float
```

### 缩放图像到

**英文** `scaleImagesTo` · **类型** `float` · **权威类型** `float` · **可用节** `图像`

单位像素。调整图像大小以使其适合像素值。同时影响腿部和阴影图像。

```ini
缩放图像到: 1
```

### 提取资源范围

**英文** `takeResources_includeUnitsWithinRange` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

提取资源，在此范围内。

```ini
提取资源范围: 300
```

### 提取资源目标量

**英文** `takeResources_maxUnits` · **类型** `int` · **权威类型** `int` · **可用节** `行动`

最多从多少单位提取资源，默认为1。

```ini
提取资源目标量: 10
```

### 添加路径点检索范围

**英文** `addWaypoint_target_nearestUnit_maxRange` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

添加的路径点靠近某目标时考虑的最大范围。

```ini
添加路径点检索范围: 2000
```

### 添加指定范围的随机单位

**英文** `addWaypoint_target_randomUnit_maxRange` · **类型** `int` · **权威类型** `int` · **可用节** `行动`

添加路径点单位为指定范围的随机单位

```ini
添加指定范围的随机单位: 1000
```

### 透明度

**英文** `alpha` · **类型** `logicNumber` · **可用节** `贴花` `效果`

LogicFloat (0-1). Stacks with alpha in color. affects image and line.（动态浮点值（0-1）。以颜色堆叠透明度。影响图像和线条。）

```ini
透明度: 0.5
```

### 图像绘制偏移x

**英文** `image_drawOffsetX` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔图像在X轴偏移。

```ini
图像绘制偏移x: 0
```

### 图像绘制偏移y

**英文** `image_drawOffsetY` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔图像在Y轴偏移。

```ini
图像绘制偏移y: 30
```

### 图像缩放

**英文** `imageScale` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `图像`

- **贴花**：`logicNumber`
- **图像**：`float`

图像缩放(倍数)

```ini
图像缩放: 1 + (自身高度 * 0.1)
```

### 图像X轴偏移

**英文** `image_offsetX` · **类型** `int` · **权威类型** `int` · **可用节** `图像`

图像在X轴偏移，用于不对称图像，比如您的单位有个吊臂，这样中心不在正中间。

```ini
图像X轴偏移: 21
```

### 图像Y轴偏移

**英文** `image_offsetY` · **类型** `int` · **权威类型** `int` · **可用节** `图像`

图像在Y轴偏移,用于比较高的单位。

```ini
图像Y轴偏移: 41
```

### 推动力量

**英文** `pushForce` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

抛射体丸所携带的“重量”,质量越大，推力越大。推动（或以负值拉动）被击中的单位。除以目标质量

```ini
推动力量: 2
```

### 推荐条件不满足时优先级

**英文** `recommendedInEachBasePriorityIfUnmet` · **类型** `float` · **权威类型** `float` · **可用节** `AI`

如果未满足，推荐在基地中的优先级。

```ini
推荐条件不满足时优先级: 4
```

### 推荐在每个基地的数量

**英文** `recommendedInEachBaseNum` · **类型** `float` · **权威类型** `float` · **可用节** `AI`

推荐在每个基地中的数量。

```ini
推荐在每个基地的数量: 2
```

### 推进速度

**英文** `pushVelocity` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

抛射体爆炸后可用推动单位,推力相对于方向的速度，推动（或以负值拉动）被击中的单位。忽略目标质量

```ini
推进速度: 5
```

### 维修或建造范围

**英文** `nanoRange` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

默认值为85。定义单位的构建/修复/回收范围。

```ini
维修或建造范围: 110
```

### 维修距离

**英文** `nanoRangeForRepair` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

此单位维修距离。

```ini
维修距离: 60
```

### 维修速度

**英文** `nanoRepairSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为0.2。定义单位修复速度。

```ini
维修速度: 0.01
```

### 维修效果频率

**英文** `repairEffectRate` · **类型** `int` · **权威类型** `int` · **可用节** `图像`

维修效果每隔多少帧产生一次。

```ini
维修效果频率: 20
```

### 尾焰频率

**英文** `trailEffectRate` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

产生频率

```ini
尾焰频率: 4
```

### 为目标时半径

**英文** `uiTargetRadius` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

默认值为displayRadius。当攻击/回收/等等这个单位使用半径

```ini
为目标时半径: 10
```

### 未完成时视野

**英文** `fogOfWarSightRangeWhileNotBuilt` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

填数值，当建筑或单位没有建造完成时，它的视野范围

```ini
未完成时视野: 10
```

### 无视装甲数量

**英文** `armourIgnoreAmount` · **类型** `int` · **权威类型** `int` · **可用节** `抛射体`

无视目标装甲数量并造成伤害。

```ini
无视装甲数量
```

### 物理重力

**英文** `physicsGravity` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

默认为1。physics:true时的垂直方向加速度。

```ini
物理重力: 0.5
```

### 闲时转速

**英文** `idleSpin` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

炮塔闲置时的旋转速度,单位度每帧。

```ini
闲时转速: 2
```

### 闲置方向

**英文** `idleDir` · **可按节区分类型** · **权威类型** `float` · **可用节** `附属` `炮塔`

- **附属**：`int`
- **炮塔**：`float`

闲置时角度。

```ini
闲置方向
```

### 闲置方向反转

**英文** `idleDirReversing` · **可按节区分类型** · **权威类型** `float` · **可用节** `附属` `炮塔`

- **附属**：`int`
- **炮塔**：`float`

允许朝向反转，比如重坦倒退移动时炮塔反转，而不需要整个单位转向。

```ini
闲置方向反转
```

### 线宽度

**英文** `lineWidth` · **类型** `float` · **权威类型** `float` · **可用节** `贴花`

表明这条线的宽度

```ini
线宽度: 2
```

### 限制范围

**英文** `limitingRange` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

限制此炮塔的最远攻击范围。不要将此代码应用到所有炮塔，如果需要则设置maxAttackRange。

```ini
限制范围: 100
```

### 限制角度

**英文** `limitingAngle` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

与idleDir配合。炮塔只能以+/-此角度进行攻击。

```ini
限制角度: 60
```

### 效果存在时间

**英文** `life` · **可按节区分类型** · **权威类型** `int` · **可用节** `效果` `抛射体`

- **效果**：`float`
- **抛射体**：`int`

默认值为200。到时间效果消失。 设置得尽可能低以减少效果开销。

```ini
效果存在时间: 70
```

### 效果存在时间随机值

**英文** `lifeRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

效果存在时间的随机范围。

```ini
效果存在时间随机值: 12
```

### 卸载单位并赋予攻击目标

**英文** `unloadUpToXUnitsAndGiveAttackOrder` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

在炮塔炮筒位置卸下X个单位，并让它们攻击炮塔指定的目标。

```ini
卸载单位并赋予攻击目标: 1
```

### 卸载单位至命中地

**英文** `unloadUpToXUnitsFromSource` · **类型** `int` · **权威类型** `int` · **可用节** `抛射体`

将指定个数单位卸载到抛射体爆炸位置。

```ini
卸载单位至命中地: 2
```

### 修正范围伤害

**英文** `mutatorX_areaDamageMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

伤害修正,范围伤害(areaDamage)乘数。比如直接伤害100，这里填2，对目标就造成200伤害。

```ini
修正范围伤害: 1.2
```

### 修正直接伤害

**英文** `mutatorX_directDamageMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

伤害修正,直接伤害(directDamage)乘数。比如直接伤害100，这里填2，对目标就造成200伤害。

```ini
修正直接伤害: 5
```

### 旋转速度

**英文** `rotateSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

设置脚旋转速度，非对称情况下很明显。

```ini
旋转速度: 2
```

### 延迟创建

**英文** `delayedStartTimer` · **类型** `float` · **权威类型** `float` · **可用节** `效果` `抛射体`

等待这么久再播放此动画，单位为帧。

```ini
延迟创建: 2s
```

### 严格级别

**英文** `strictLevel` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为0，忽略代码重复。设为1时如果当前单位内有重复代码，则报错。建议添加到"all-units.template"以应用于所有单位,进行统一查错。

```ini
严格级别: 1
```

### 移动滑动角度

**英文** `moveSlidingDir` · **类型** `int` · **权威类型** `int` · **可用节** `运动`

移动滑动角度。无明显效果。

```ini
移动滑动角度: 180
```

### 移动速度

**英文** `moveSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `腿` `运动`

腿脚移动速度。

```ini
移动速度: 3
```

### 硬性限制

**英文** `hardLimit` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

默认值为50。强制腿部不要走的超过此数值。最好不要用到。

```ini
硬性限制: 60
```

### 预测位置乘数

**英文** `estimatingPositionMultiplier` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

默认值为1。根据单位速度预测单位可用的腿脚放置位置。

```ini
预测位置乘数: 2
```

### 预判计算

**英文** `leadTargetingSpeedCalculation` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

用于使用targetGround时预判计算。目标计算的预期速度。默认为“targetSpeed”。如果设置则基于speed.

```ini
预判计算: 2
```

### 预热

**英文** `warmup` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

满足激活条件后，先延迟此时间

```ini
预热: 5s
```

### 预热下降速度

**英文** `warmupCallDownRate` · **类型** `float` · **权威类型** `float` · **可用节** `炮塔`

停止攻击后预热条下降速度

```ini
预热下降速度: 10
```

### 运动效果频率

**英文** `movementEffectRate` · **类型** `float` · **权威类型** `float` · **可用节** `图像`

自定义效果产生频率，每隔多少帧一次。

```ini
运动效果频率: 10
```

### 运输槽位数量

**英文** `maxTransportingUnits` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

该单位载员格子数量。

```ini
运输槽位数量: 5
```

### 运输占用位置

**英文** `transportSlotsNeeded` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

默认值为1。此单位在运输载具中占据的格子数。

```ini
运输占用位置: 2
```

### 在队列中有单位播放

**英文** `onActionsQueuedUnitPlayAt` · **类型** `float` · **权威类型** `float` · **可用节** `动画`

当使用onAction:queueedUnits时，动画开始之前需要达到值，设置为0-1之间

```ini
在队列中有单位播放: 0
```

### 载具卸载时间间隔

**英文** `transportUnitsUnloadDelayBetweenEachUnit` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

卸载单位之间的延迟时间。

```ini
载具卸载时间间隔: 12
```

### 载具治疗内部单位速度

**英文** `transportUnitsHealBy` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

填数值，以帧为单位。自动治疗载具内部单位。

```ini
载具治疗内部单位速度: 0.1
```

### 帧

**英文** `frame` · **可按节区分类型** · **权威类型** `int` · **可用节** `贴花` `抛射体`

- **贴花**：`logicNumber`
- **抛射体**：`int`

(与total_frames相比,frame是动值,可以改变内存来修改第几帧)

```ini
帧: 内存.帧
```

### 帧高度

**英文** `frame_height` · **类型** `int` · **权威类型** `int` · **可用节** `贴花` `图像`

帧高度

```ini
帧高度: 40
```

### 帧宽度

**英文** `frame_width` · **类型** `int` · **权威类型** `int` · **可用节** `贴花` `图像`

帧宽度（不可与总帧数一起设置）

```ini
帧宽度: 20
```

### 帧时长缩放

**英文** `KeyframeTimeScale` · **类型** `float` · **权威类型** `float` · **可用节** `动画`

缩放所有关键帧时间，这有助于在不更改所有内容的情况下更快/更慢地制作动画

```ini
帧时长缩放: 1
```

### 帧索引

**英文** `frameIndex` · **类型** `int` · **权威类型** `int` · **可用节** `效果`

使用内置效果时所取用的帧编号，0取第一帧。

```ini
帧索引: 0
```

### 阵营色色相比例

**英文** `teamColorRatio_sourceRatio` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

将团队颜色混合到抛射体颜色中与原色比例，默认为(1-teamColorRatio)

```ini
阵营色色相比例: 1
```

### 直接伤害

**英文** `directDamage` · **类型** `int` · **权威类型** `int` · **可用节** `抛射体`

击中对目标单位造成伤害。不适用于targetGround:true。

```ini
直接伤害: 30
```

### 指定攻击地面数量

**英文** `fireTurretXAtGround_count` · **可用节** `行动`

设置发射的抛射体数量，默认为1

```ini
指定攻击地面数量: 3
```

### 质量

**英文** `mass` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

单位的“重量”定义了它与其他单位的碰撞方式，值越大，推动就越困难。

```ini
质量: 3000
```

### 重力

**英文** `gravity` · **可用节** `抛射体`

控制地面的抛射体丸的拉力。与initialUnguidedSpeedHeight一起使用。

```ini
重力: 29
```

### 重新瞄准在飞行时搜索范围

**英文** `retargetingInFlightSearchRange` · **类型** `int` · **权威类型** `int` · **可用节** `抛射体`

重新选择目标的范围。默认值120

```ini
重新瞄准在飞行时搜索范围: 300
```

### 重新瞄准在飞行时预判

**英文** `retargetingInFlightSearchLead` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

引导射抛射体试图击中目标。默认15

```ini
重新瞄准在飞行时预判: 30
```

### 转换资源倍率

**英文** `convertResource_multiplyAmountBy` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

资源转换倍率，不影响提取的数量。默认值为1。

```ini
转换资源倍率: 1.5
```

### 转换资源至多

**英文** `convertResource_maxAmount` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

“来源资源”和“转换后资源”之间的最大转化量

```ini
转换资源至多: 100
```

### 转换资源至少

**英文** `convertResource_minAmount` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

如果来源中资源小于该数量，则跳过。默认为0。在大多数情况下可能不需要。

```ini
转换资源至少: 10
```

### 转速

**英文** `dirSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

修改效果的旋转速度。

```ini
转速: 2
```

### 转弯加速度

**英文** `turnAcceleration` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

转弯加速度。

```ini
转弯加速度: 1
```

### 转弯速度

**英文** `turnSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体` `炮塔`

限制抛射体的转弯速度，更好的做出弹道效果。

```ini
转弯速度: 0
```

### 装甲

**英文** `armour` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

抵消敌方攻击所造成的伤害。

```ini
装甲: 6
```

### 装甲最低伤害

**英文** `armourMinDamageToKeep` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

至少造成多少点伤害，默认为1.防止护甲太高完全打不动。

```ini
装甲最低伤害: 2
```

### 坠毁加速度

**英文** `fallingAccelerationDead` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

单位被摧毁时降落的加速度

```ini
坠毁加速度:
```

### 资金获取

**英文** `generation_credits` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

生成资源，仅用于默认的资金，也就是铁锈默认的金钱。

```ini
资金获取: 2
```

### 资金获取时间

**英文** `generation_delay` · **类型** `int` · **权威类型** `int` · **可用节** `核心`

多久帧添加添加一次资源(generation_creditsvi指定数值)。默认值为40，一秒为60帧。Luke不建议使用。

```ini
资金获取时间: 40
```

### 资源类型乘数

**英文** `resourceAmount_multiplyBy` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

默认值为1。将当前资源值乘上此值。

```ini
资源类型乘数: 2
```

### 资源类型设置数值

**英文** `resourceAmount_setValue` · **类型** `float` · **权威类型** `float` · **可用节** `行动`

设置此资源的绝对值，忽略资源的当前值。默认忽略。

```ini
资源类型设置数值: 20
```

### 自动建造速度

**英文** `selfBuildRate` · **类型** `float` · **权威类型** `float` · **可用节** `核心`

此单位自动构造所需要的时间，目前用于虫族。计算方式为：此处所填时间=1÷(60x您需要的秒)。

```ini
自动建造速度: 0.0008
```

### 自动切换目标检索范围

**英文** `autoTargetingOnDeadTargetRange` · **类型** `int` · **权威类型** `int` · **可用节** `抛射体`

当旧目标死亡时选择新目标的范围

```ini
自动切换目标检索范围: 100
```

### 自动切换目标预判

**英文** `autoTargetingOnDeadTargetLead` · **类型** `float` · **权威类型** `float` · **可用节** `抛射体`

尝试新的目标时的预判

```ini
自动切换目标预判: 300
```

### 总帧数

**英文** `total_frames` · **类型** `int` · **权威类型** `int` · **可用节** `贴花` `效果` `图像`

Use total_frames or frame_width/frame_height（使用 总帧数 或者 帧宽度/帧高度）

```ini
总帧数: 10
```

### 最大数量

**英文** `maxCount` · **类型** `int` · **权威类型** `int` · **可用节** `放置规则`

在匹配失败前设置最大单位数量(例如不能接近某个值)。默认为无限的

```ini
最大数量: 1
```

### 最大移动距离

**英文** `holdDisMax` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

默认值为16。如果已经移动超过此距离则强制重新放置腿。

```ini
最大移动距离: 20
```

### 最大移动腿数目

**英文** `holdDisMin_maxMovingLegs` · **类型** `int` · **权威类型** `int` · **可用节** `腿`

最多移动腿数目

```ini
最大移动腿数目: 4
```

### 最大转弯速度

**英文** `maxTurnSpeed` · **类型** `float` · **权威类型** `float` · **可用节** `运动`

最大转弯速度。

```ini
最大转弯速度: 4
```

### 最小距离

**英文** `limitingMinRange` · **类型** `int` · **权威类型** `int` · **可用节** `炮塔`

设置炮塔的最小攻击范围，不能攻击此范围内单位，也就是死角。

```ini
最小距离: 200
```

### 最小数量

**英文** `minCount` · **类型** `int` · **权威类型** `int` · **可用节** `放置规则`

设定需要在搜索中找到的单位的最小数量。(如需要靠近某物)。默认值为0

```ini
最小数量: 0
```

### 最小移动距离

**英文** `holdDisMin` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

默认值为7。如果相邻的腿还没有重新移动，则至少移动此距离。

```ini
最小移动距离: 10
```

### 坐标系旋转

**英文** `pivotOffset` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `效果`

- **贴花**：`int`
- **效果**：`float`

(相当于把单位定位的直角坐标系旋转该角度,然后重新确定xy) 只影响X/Y相对偏移,不旋转图像-----only effects xOffsetRelative/yOffsetRelative without rotating image

```ini
坐标系旋转: 45
```

### action_#_addEnergy

**类型** `float` · **可用节** `核心`

为单位增加能量。除非设置了energyMax，否则不起作用

```ini
action_1_addEnergy: 10
```

### action_#_pos

**类型** `float` · **可用节** `核心`

订单操作出现在用户界面中

```ini
action_1_pos: 0.1
```

### action_#_price

**类型** `int` · **可用节** `核心`

您对该单位采取的行动的价格。 （您所有的子操作都将链接到您使用的＃号）

```ini
action_1_price: 1000
```

### addBodyFrameMultipliedBy

**类型** `int` · **权威类型** `int` · **可用节** `贴花`

添加主体帧时的乘数

```ini
addBodyFrameMultipliedBy: 2
```

### AI升级优先级

**英文** `ai_upgradePriority` · **类型** `float` · **权威类型** `float` · **可用节** `AI`

升级优先级。默认值为0.06。 设置在0-1之间，越高，表示AI更有可能先于其他升级该单位。

```ini
AI升级优先级
```

### animation_direction_starting

**类型** `float` · **权威类型** `float` · **可用节** `图像`

第一帧的方向，取决于您的素材。您要考虑转向后避免正负数交替现象，否则会导致素材朝向异常。

```ini
animation_direction_starting: 90
```

### animation_direction_strideX

**类型** `int` · **权威类型** `int` · **可用节** `图像`

动画帧取值在X方向改变时偏移。一般填0

```ini
animation_direction_strideX: 40
```

### animation_direction_strideY

**类型** `int` · **权威类型** `int` · **可用节** `图像`

动画帧在方向改变Y轴偏移偏移。与frame_height一起使用。一般填1

```ini
animation_direction_strideY: 60
```

### animation_direction_units

**类型** `float` · **权威类型** `float` · **可用节** `图像`

每隔多少度切换一次素材,比如填45代表8个方向，90个代表4个方向的动画。与rotate_with_direction一起使用。

```ini
animation_direction_units: 45
```

### autoTriggerOnEventRecursionLimit

**类型** `int` · **权威类型** `int` · **可用节** `行动`

自己触发自己的限制次数，默认为1

```ini
autoTriggerOnEventRecursionLimit: 4
```

### builtFrom__pos

**类型** `float` · **权威类型** `float` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_pos

```ini
builtFrom_1_pos: 0.1
```

### builtFrom__sortOrder

**类型** `int` · **权威类型** `int` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_sortOrder

```ini
builtFrom_1_sortOrder: 10
```

### canBuild_#_pos

**类型** `float` · **可用节** `核心`

data.sections.core.canBuild_#_pos

```ini
canBuild_1_pos: 0.1
```

### default_frame

**类型** `int` · **权威类型** `int` · **可用节** `图像`

指定默认显示为第几帧,默认为0即第一帧

```ini
default_frame: 0
```

### displayPos

**可按节区分类型** · **权威类型** `Number` · **可用节** `全局资源` `资源`

- **全局资源**：`int`
- **资源**：`Number`

显示优先级，越小越居上

```ini
displayPos: 1
```

### end

**类型** `int` · **可用节** `动画`

data.sections.animation.end

```ini
end : int
```

### heightSpeed

**类型** `float` · **权威类型** `float` · **可用节** `腿`

移动时脚抬高速度和高度。

```ini
heightSpeed: 2
```

### liftingHeightOffset

**类型** `int` · **权威类型** `int` · **可用节** `腿`

离开时的脚的高度

```ini
liftingHeightOffset: 20
```

### nanoBuildSpeed

**类型** `float` · **权威类型** `float` · **可用节** `核心`

默认值为1。定义单位建造速度。 （可能与目标的buildSpeed相乘）

```ini
nanoBuildSpeed: 0.9
```

### nanoReclaimSpeed

**类型** `float` · **权威类型** `float` · **可用节** `核心`

默认为nanoRepairSpeed*5.1)以符合旧版本的行为

```ini
nanoReclaimSpeed: 0.23
```

### nanoUnbuildSpeed

**类型** `float` · **权威类型** `float` · **可用节** `核心`

(俗称"蓝影"，默认为1)

```ini
nanoUnbuildSpeed: 1.4
```

### onlyOnBodyFrameOf

**类型** `int` · **权威类型** `int` · **可用节** `贴花`

只有在主体到某个关键帧时绘制

```ini
onlyOnBodyFrameOf: 0
```

### pos

**类型** `float` · **权威类型** `float` · **可用节** `行动` `可建造`

此动作在用户界面排序。

```ini
pos: 1
```

### resourceReclaimMultiplier

**类型** `float` · **权威类型** `float` · **可用节** `核心`

默认为1

```ini
resourceReclaimMultiplier: 1.5
```

### size

**类型** `float` · **权威类型** `float` · **可用节** `炮塔`

控制炮塔中心与抛射体丸产生点之间的距离。

```ini
size: 5
```

### spinRate

**类型** `float` · **权威类型** `float` · **可用节** `腿`

使手臂/腿脚一直旋转，像转塔的idleSpin。用处如直升机的螺旋桨。

```ini
spinRate: 2
```

### start

**类型** `int` · **可用节** `动画`

data.sections.animation.start

```ini
start : int
```

### targetSpeedAcceleration

**类型** `float` · **权威类型** `float` · **可用节** `抛射体`

加速度，控制targetSpeed的速度提升

```ini
targetSpeedAcceleration: 0.3
```

### updateUnitMemoryRate

**类型** `int` · **权威类型** `int` · **可用节** `核心`

(默认1s,填写0则为每帧)How often to call updateUnitMemory, defaults to 1s. At zero would trigger every frame.

```ini
updateUnitMemoryRate: 0
```

### weaponSlot__pos

**类型** `float` · **权威类型** `float` · **可用节** `核心`

data.sections.core.weaponSlot_{NUM}_pos

```ini
weaponSlot_1_pos: 0.5
```

### weaponSlots

**类型** `int` · **权威类型** `int` · **可用节** `核心`

data.sections.core.weaponSlots

```ini
weaponSlots: 4
```

### weaponSlotsForAI

**类型** `int` · **权威类型** `int` · **可用节** `核心`

data.sections.core.weaponSlotsForAI

```ini
weaponSlotsForAI: 2
```

### weaponSlotsForBuild

**类型** `int` · **权威类型** `int` · **可用节** `核心`

data.sections.core.weaponSlotsForBuild

```ini
weaponSlotsForBuild: 3
```

### x

**类型** `float` · **权威类型** `float` · **可用节** `附属` `腿` `炮塔`

子单位x轴位置

```ini
x
```

### X绝对速度

**英文** `xSpeedAbsolute` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

X轴相对于起点的绝对移动速度。

```ini
X绝对速度: 5
```

### X绝对随机偏移量

**英文** `xOffsetAbsoluteRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

随机增加或减少此值，用于偏移像素。

```ini
X绝对随机偏移量: 5
```

### X随机绝对速度

**英文** `xSpeedAbsoluteRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

按此值随机更改初始值。

```ini
X随机绝对速度: 2
```

### X随机相对速度

**英文** `xSpeedRelativeRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

随机增加或减少此值，用于偏移像素。

```ini
X随机相对速度: 2
```

### X相对速度

**英文** `xSpeedRelative` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

X轴相对于目标的移动速度。，正数向右，负数向左。

```ini
X相对速度: 2
```

### X相对随机偏移量

**英文** `xOffsetRelativeRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

X相对目标随机偏移量。随机增加或减少此值，用于偏移像素。

```ini
X相对随机偏移量: 4
```

### X轴绝对偏移

**英文** `xOffsetAbsolute` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `效果`

- **贴花**：`logicNumber`
- **效果**：`float`

(note use basePosition with createMarker/etc for logic use)（请注意，动态使用base Position（基本位置）和create Marker（创建标记）/ect（等））

```ini
X轴绝对偏移: 40
```

### x轴链接位置

**英文** `attach_x` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

在X轴上设置腿部的附着点。

```ini
x轴链接位置: 10
```

### X轴相对偏移

**英文** `xOffsetRelative` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `效果`

- **贴花**：`int`
- **效果**：`float`

(note use basePosition with createMarker/etc for logic use)（请注意，动态使用base Position（基本位置）和create Marker（创建标记）/ect（等））

```ini
X轴相对偏移: 50
```

### X轴阴影偏移

**英文** `shadowOffsetX` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `图像`

- **贴花**：`int`
- **图像**：`float`

阴影偏移X

```ini
X轴阴影偏移: 0
```

### y

**类型** `float` · **权威类型** `float` · **可用节** `附属` `腿` `炮塔`

子单位Y轴位置

```ini
y
```

### Y绝对速度

**英文** `ySpeedAbsolute` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

Y轴相对于起点的绝对移动速度。

```ini
Y绝对速度: 5
```

### Y绝对随机偏移量

**英文** `yOffsetAbsoluteRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

随机增加或减少此值，用于偏移像素。

```ini
Y绝对随机偏移量: 5
```

### Y随机绝对速度

**英文** `ySpeedAbsoluteRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

按此值随机更改初始值。

```ini
Y随机绝对速度: 2
```

### Y随机相对速度

**英文** `ySpeedRelativeRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

随机增加或减少此值，用于偏移像素。

```ini
Y随机相对速度: 2
```

### Y相对速度

**英文** `ySpeedRelative` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

Y轴相对于目标的移动速度。正数向前，负数向后。

```ini
Y相对速度: 3
```

### Y相对随机偏移量

**英文** `yOffsetRelativeRandom` · **类型** `float` · **权威类型** `float` · **可用节** `效果`

Y相对目标随机偏移量。随机增加或减少此值，用于偏移像素。

```ini
Y相对随机偏移量: 4
```

### Y轴绝对偏移

**英文** `yOffsetAbsolute` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `效果`

- **贴花**：`logicNumber`
- **效果**：`float`

(note use basePosition with createMarker/etc for logic use)（请注意，动态使用base Position（基本位置）和create Marker（创建标记）/ect（等））

```ini
Y轴绝对偏移: 40
```

### Y轴链接位置

**英文** `attach_y` · **类型** `float` · **权威类型** `float` · **可用节** `腿`

在Y轴上设置腿部的附着点。

```ini
Y轴链接位置: 0
```

### Y轴相对偏移

**英文** `yOffsetRelative` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `效果`

- **贴花**：`int`
- **效果**：`float`

(note use basePosition with createMarker/etc for logic use)（请注意，动态使用base Position（基本位置）和create Marker（创建标记）/ect（等））

```ini
Y轴相对偏移: 50
```

### Y轴阴影偏移

**英文** `shadowOffsetY` · **可按节区分类型** · **权威类型** `float` · **可用节** `贴花` `图像`

- **贴花**：`int`
- **图像**：`float`

阴影偏移Y

```ini
Y轴阴影偏移: 0
```

---

## 价格（7 个）

### 流式造价

**英文** `streamingCost` · **类型** `price` · **权威类型** `price` · **可用节** `核心`

就像价格，但在建造时逐渐消耗资金，如果在构建过程中资源耗尽，建造或生产队列将暂停。就像是红警中那样。铁锈默认是预先扣除资金。

```ini
流式造价: gem=420
```

### 提取资源

**英文** `takeResources` · **类型** `customPrice` · **权威类型** `customPrice` · **可用节** `行动`

提取资源，至少写一种资源。

```ini
提取资源: hp=5, gold=10
```

### 提取资源排除资源

**英文** `takeResources_excludeUnitsWithTheseResources` · **类型** `customPrice` · **权威类型** `customPrice` · **可用节** `行动`

提取资源，不包括拥有这些资源的单位。

```ini
提取资源排除资源: oil, juice, sauce
```

### 资源活着时借用

**英文** `borrowResourcesWhileAlive` · **类型** `price` · **权威类型** `price` · **可用节** `核心`

创建时获取这些资源，删除或销毁时将其返回。例如用于电力逻辑，负数供电和正数耗电。

```ini
资源活着时借用: gold=10
```

### 资源获取

**英文** `generation_resources` · **类型** `price` · **权威类型** `price` · **可用节** `核心`

单位定时获得的资源，可自定义资源。

```ini
资源获取: 资金=5, gold=20
```

### 资源需求

**英文** `resourceUsage` · **类型** `price` · **权威类型** `price` · **可用节** `炮塔`

可以使用资金/能源/生命/护盾/弹药等。如果不符合条件则停止攻击。credits/energy/hp/shield/ammo

```ini
资源需求: 资金=5, 能量=5, hp=100, 护盾=5, ammo=1
```

### borrowResourcesWhileBuilt

**类型** `price` · **权威类型** `price` · **可用节** `核心`

类似于[core]borrowResourcesWhileAlive但在单位建成后才会生效。主要对房屋之类的建筑有用，这些建筑有负的资源，可以加到单位上限等

```ini
borrowResourcesWhileBuilt: supplyCap = -10
```

---

## 布尔（268 个）

### 包含父单位选中绘制

**英文** `includeParentsSelection` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

onlyWhenSelected*also check parents selection（仅当选定时*还检查父项选择）

```ini
包含父单位选中绘制: 真
```

### 保持需要移动路径点

**英文** `keepWaypointsNeedingMovement` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

默认值为false。当为false时，队列中任何需要移动才能完成的队列路径点都会被移除。

```ini
保持需要移动路径点
```

### 保持中立

**英文** `stayNeutral` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

设置为false时，单位处于中立队伍情况下，禁止靠近时更改为玩家所属。仍然有其它方式获得。

```ini
保持中立: 假
```

### 保持子存活

**英文** `keepAliveWhenParentDies` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

当此单位死亡时，子单位保持存活。

```ini
保持子存活: 真
```

### 标签重置

**英文** `resetToDefaultTags` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

重置为默认标签。

```ini
标签重置: 真
```

### 波束末端旋转

**英文** `beamImageEndRotated` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

光束动画命中端效果图像是否旋转,默认false.

```ini
波束末端旋转: 真
```

### 波束始端旋转

**英文** `beamImageStartRotated` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

光束动画开火端效果图像是否以炮塔角度旋转旋转,默认false,不旋转.

```ini
波束始端旋转: 真
```

### 播放动画限制

**英文** `playAnimationIfNotPlaying` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

如果没有播放该动画，就播放该动画

```ini
播放动画限制: 真
```

### 补充时平滑移动

**英文** `smoothlyBlendPositionWhenExistingUnitAdded` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

补充所装载单位至槽位时平滑移动。

```ini
补充时平滑移动
```

### 不被动地被其他单位瞄准

**英文** `notPassivelyTargetedByOtherUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `AI`

允许更好的墙壁建筑物，默认情况下，这些建筑物不会瞄准目标。用于围墙（当canAttack=true时使用这条代码会报错）

```ini
不被动地被其他单位瞄准: 真
```

### 不攻击时重置角度

**英文** `resetRotationWhenNotAttacking` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

不攻击时恢复到自身默认角度

```ini
不攻击时重置角度
```

### 不计入单位数量统计

**英文** `ignoreInUnitCapCalculation` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

铁锈中有单位数量上限设定，填true时不计入统计，多用于辅助单位，建筑物的默认值为true。

```ini
不计入单位数量统计: 真
```

### 不可选择

**英文** `isUnselectable` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属` `核心`

是不可选择的。

```ini
不可选择
```

### 不能主动攻击

**英文** `disablePassiveTargeting` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击`

单位只攻击手动选择的目标。很少使用或需要。如用于自爆卡车守家，如果自动攻击敌人就太蠢了。

```ini
不能主动攻击: 真
```

### 侧边栏和建筑放置预览中显示

**英文** `onlyInPreview` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

Only show in sidebar, and building placement preview（仅在侧边栏中显示，以及建筑布局预览）

```ini
侧边栏和建筑放置预览中显示: 假
```

### 朝向总是零度

**英文** `alwayStartDirAtZero` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

生成出的单位朝向总是设置为零度。即朝右。

```ini
朝向总是零度: 真
```

### 传送到抛射体位置

**英文** `teleportSource` · **类型** `bool` · **可用节** `抛射体`

将单位移动到抛射体爆炸的位置，用于传送自身。

```ini
传送到抛射体位置: 真
```

### 创建时中立

**英文** `createNeutral` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

设置为true时，产生该单位总是自动转变为中立队伍。创建时中立，但不妨碍玩家捕获。

```ini
创建时中立: 真
```

### 创建随父单位

**英文** `createIncompleteIfParentIs` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

如果父节单位不完整，则创建子单位不完整

```ini
创建随父单位:
```

### 创建为敌对中立

**英文** `createOnAggressiveTeam` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

设置为true时，此中立单位对所有玩家有敌意。

```ini
创建为敌对中立: 真
```

### 创建于陆地上

**英文** `createWhenOverLand` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

允许在陆地上方产生此效果，默认值为true

```ini
创建于陆地上: 假
```

### 创建于液体上

**英文** `createWhenOverLiquid` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

允许在液体上方产生此效果，默认值为true

```ini
创建于液体上: 假
```

### 存活时绘制

**英文** `onlyWhileAlive` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

(If beforeUI layer default true, else default false)(如果图层(layer)为beforeUI默认为true,否则false，为ture死后消失)

```ini
存活时绘制: 假
```

### 打击音效

**英文** `hitSound` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

启用命中音效，默认true

```ini
打击音效: 真
```

### 大的打击效果

**英文** `largeHitEffect` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

为true时会产生大的爆炸效果和声音（仅用于装饰），设为false时会禁用范围武器的冲击波效果。

```ini
大的打击效果: 真
```

### 单位建造完成时保留在载具内

**英文** `transportUnitsKeepBuiltUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

使建造的单位留在运输者中，而不是造完直接从载具中出来。

```ini
单位建造完成时保留在载具内: 真
```

### 单位建造完成时绘制

**英文** `onlyWhileActive` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

Only draw when unit has been completed（仅在单位完成时绘制，比如建造者建造一般的建筑就不算)

```ini
单位建造完成时绘制: 真
```

### 单元在UI显示进度条

**英文** `unitShownInUIWithProgressBar` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

在ui中显示单位进度条。默认true,仅当unitShownInUI是unitRef时使用。如果激活，则替换HP bar

```ini
单元在UI显示进度条: 假
```

### 单元在UI显示Hp条

**英文** `unitShownInUIWithHpBar` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

在ui中显示单位血条。默认true,仅当unitShownInUI是unitRef时使用

```ini
单元在UI显示Hp条: 真
```

### 弹道

**英文** `ballistic` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

弹道导抛射体效果，使抛射体先飞向空中并向下飞，而不是走直线。

```ini
弹道: 真
```

### 低优先级目标

**英文** `lowPriorityTargetForOtherUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `AI`

其它单位不优先考虑此单位。用于围墙

```ini
低优先级目标: 真
```

### 敌方玩家选中时绘制

**英文** `onlyWhenSelectedByEnemyPlayer` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

被敌方玩家选中时绘制

```ini
敌方玩家选中时绘制: 真
```

### 动画_类型_膨胀

**英文** `animation_TYPE_pingPong` · **类型** `bool` · **可用节** `图像`

重复播放之前，请反向播放动画。与scale_start / scale_end一起使用

### 动画帧循环

**英文** `animateFrameLooping` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

默认false。当为false时,效果被移除时，动画结束

```ini
动画帧循环: 真
```

### 对敌人可见

**英文** `isVisibleToEnemies` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

与showOnMinimapToEnemies,canOnlyBeAttackedByUnitsWithTags一起使用时，可以创建隐形系统。

```ini
对敌人可见: 假
```

### 发送战争快报至所有

**英文** `showQuickWarLogToAllPlayers` · **可用节** `行动`

向所有玩家发送战争快报。（在左下角）

```ini
发送战争快报至所有: 500 oil sold to allied market
```

### 范围伤害不衰减

**英文** `areaDamageNoFalloff` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

默认false.为true时范围伤害不再向边缘递减。

```ini
范围伤害不衰减: 真
```

### 范围伤害从边缘计算

**英文** `areaRadiusFromEdge` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

范围伤害将从单位的边缘计算，而不是默认的中心计算。主要用于攻击大型单位。

```ini
范围伤害从边缘计算: 真
```

### 范围武器可攻击深海

**英文** `areaHitUnderwaterAlways` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

范围武器将可以打击深海单位如潜艇。

```ini
范围武器可攻击深海: 真
```

### 范围武器同时攻击空中和地面

**英文** `areaHitAirAndLandAtSameTime` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

范围武器将同时打击空中和地面。

```ini
范围武器同时攻击空中和地面: 真
```

### 附属断开连接

**英文** `attachments_disconnect` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

断开所有现在所在位置的附属。可以与attachments_onlyonslot一起使用。

```ini
附属断开连接: 真
```

### 附着在单位上

**英文** `attachedToUnit` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

此效果产生时吸附到目标单位或抛射体。将与之一起移动。

```ini
附着在单位上: 真
```

### 改变队伍时子单位保持原队伍

**英文** `onParentTeamChangeKeepCurrentTeam` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

默认值为false。如果为true则父单位改变队伍时子单位不改变队伍。

```ini
改变队伍时子单位保持原队伍
```

### 高优先级队列

**英文** `highPriorityQueue` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

默认为false。如果为true，则此操作将跳过队列中的所有其他低优先级操作。适用于fireTurret类动作。

```ini
高优先级队列: 真
```

### 构建时死亡

**英文** `dieOnConstruct` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则创建建筑时删除自身。（目标建筑物/单位需要selfBuildRate以自动建造完成）

```ini
构建时死亡: 真
```

### 固定角度为零

**英文** `alwaysStartDirAtZero` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

Useful for UI(对UI有用)

```ini
固定角度为零: 真
```

### 固定射击

**英文** `isFixedFiring` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击`

必须将身体对准目标射击。通常会使得部队需要停下来才能瞄准和射击。比如火炮。

```ini
固定射击: 真
```

### 核武器

**英文** `nukeWeapon` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

发射时在迷您地图上显示。还有其他一些效果。

```ini
核武器: 真
```

### 忽略父单位射击伤害乘数

**英文** `ignoreParentShootDamageMultiplier` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

忽略父单位射击伤害乘数

```ini
忽略父单位射击伤害乘数: 真
```

### 护盾初始为0

**英文** `startShieldAtZero` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则单位护盾值从0开始增加。

```ini
护盾初始为0: 真
```

### 护盾只在受攻击时显示

**英文** `shieldDisplayOnlyDeflection` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

隐藏护盾，只在受到攻击时显示。

```ini
护盾只在受攻击时显示: 真
```

### 恢复单位状态

**英文** `resetUnitStats` · **类型** `boolean` · **权威类型** `boolean` · **可用节** `行动`

将setUnitStats所做的更改重置为基本值

```ini
恢复单位状态: 真
```

### 回收范围按近战逻辑

**英文** `nanoRangeForReclaimIsMelee` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

与上条类似，本条定义其回收范围是否为近战算法。

```ini
回收范围按近战逻辑: 真
```

### 绘制脚在顶层

**英文** `draw_foot_on_top` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

绘制脚在顶层。

```ini
绘制脚在顶层: 真
```

### 绘制所有单位之下

**英文** `drawUnderAllUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

绘制所有单位之下。

```ini
绘制所有单位之下: 真
```

### 绘制于底层

**英文** `setDrawLayerOnBottom` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

此子单位绘制于底层。

```ini
绘制于底层
```

### 绘制于顶层

**英文** `setDrawLayerOnTop` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

此子单位绘制于顶层。

```ini
绘制于顶层
```

### 绘制在单位下方

**英文** `drawUnderUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果` `抛射体`

将此效果绘制在单位下方。

```ini
绘制在单位下方: 假
```

### 绘制主体之上

**英文** `drawOverBody` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

绘制在主体之上。

```ini
绘制主体之上: 假
```

### 火焰武器

**英文** `flameWeapon` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

命中时产生小火焰（仅用于装饰）

```ini
火焰武器: 真
```

### 激光效果

**英文** `laserEffect` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

将抛射体图像修改为激光。

```ini
激光效果: 真
```

### 加入队形

**英文** `joinsGroupFormations` · **类型** `bool` · **权威类型** `bool` · **可用节** `运动`

将单位组成小队。铁锈调集大量部队时将其组成方阵以降低运算压力，但队形可能影响速度或浪费时间。禁用后则不参与组队。

```ini
加入队形: 假
```

### 检查每个地块

**英文** `checkEachTile` · **类型** `bool` · **权威类型** `bool` · **可用节** `放置规则`

[true / false]默认为true（仅对测试单元中心设置为false，true会检查显示在放置网格上的单元下方的每个图块）

```ini
检查每个地块: 假
```

### 建造方式

**英文** `forceNano` · **类型** `bool` · **权威类型** `bool` · **可用节** `可建造`

如果为true，则将目标当作是建筑物建造。 （即使是一个单位）

```ini
建造方式: 真
```

### 建造时无法移动

**英文** `whenBuilding_cannotMove` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

执行操作准备时停止单位移动。适用于类似部署类动作。

```ini
建造时无法移动: 真
```

### 建造时需等待旋转完成

**英文** `whenBuilding_rotateTo_waitTillRotated` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

暂停准备的行为，直到旋转完成再继续。

```ini
建造时需等待旋转完成: 真
```

### 建造时旋转或反转

**英文** `whenBuilding_rotateTo_orBackwards` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

如果为true，则当角度较小时，允许从whenBuilding_rotateTo旋转180度。

```ini
建造时旋转或反转: 真
```

### 建造时转向瞄准目标

**英文** `whenBuilding_rotateTo_aimAtActionTarget` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

准备时转向瞄准目标,luke:测试版注意:在某些情况下是坏的

```ini
建造时转向瞄准目标: 真
```

### 建筑只能建造在资源池上

**英文** `placeOnlyOnResPool` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

通常用于提取器，会强制在资源池中进行建筑物构造。可以用于在自定义战役中限制某单位只能在某地建造。

```ini
建筑只能建造在资源池上: 真
```

### 揭开迷雾的

**英文** `shouldRevealFog` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

抛射体在爆炸时向玩家揭开迷雾。

```ini
揭开迷雾的: 真
```

### 仅在非预览中显示

**英文** `onlyOnNonPreview` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

Only show on real unit, not action sidebar, etc（仅在真实单元上显示，而不是动作侧边栏等）

```ini
仅在非预览中显示: 真
```

### 近战

**英文** `isMelee` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击`

近战单位。与低攻击范围配合使用（例如maxAttackRange：9）可使自身和目标半径添加到范围内，并影响AI。

```ini
近战: 真
```

### 禁核模式锁定

**英文** `isLockedIfGameModeNoNuke` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果在游戏开始前选择了禁用核武器，禁止建造此单位。

```ini
禁核模式锁定: 真
```

### 禁用

**英文** `disableUse` · **类型** `bool` · **权威类型** `bool` · **可用节** `AI`

禁止AI建立这个单位或建筑物

```ini
禁用:
```

### 禁用低生命冒火

**英文** `disableLowHpFire` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

禁用低生命值时冒火的视觉效果

```ini
禁用低生命冒火: 假
```

### 禁用低生命冒烟

**英文** `disableLowHpSmoke` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

禁用低生命值时冒烟的视觉效果

```ini
禁用低生命冒烟: 假
```

### 禁用零血死亡

**英文** `disableDeathOnZeroHp` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，允许单位在0血量的情况下仍然存活，单位不会自然死亡。对自定义“死亡”动作很有用

```ini
禁用零血死亡: 真
```

### 禁用碰撞

**英文** `disableAllUnitCollisions` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则该单位无视与其它单位碰撞。无碰撞体积单位可以用于固定位置制造效果，或是用于一些不影响单位通行的场景。

```ini
禁用碰撞: 真
```

### 禁用预判

**英文** `disableLeadTargeting` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

瞄准移动目标时，禁用预判计算。默认为false。

```ini
禁用预判: 真
```

### 禁止被修复

**英文** `isUnrepairableUnit` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则任何单位都无法修复此单位。但负伤害仍然可以维修。

```ini
禁止被修复: 真
```

### 禁止放置

**英文** `blocksPlacement` · **类型** `bool` · **权威类型** `bool` · **可用节** `放置规则`

禁止放置。默认为true。

```ini
禁止放置: 假
```

### 禁止加载

**英文** `dont_load` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

不加载此单位，缺失数据时不会出错。与copyFrom一起使用时很有用。

```ini
禁止加载: 真
```

### 禁止接受指令

**英文** `canNotBeGivenOrdersByPlayer` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则单位不会接受玩家或AI的命令。可以选中查看信息。

```ini
禁止接受指令: 真
```

### 禁止受到伤害

**英文** `canNotBeDamaged` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

无敌，可被敌方攻击，但不造成实质伤害。如果canNotBeDirectlyAttacked为true,此语句为false,则不可被直接攻击但受到范围伤害。

```ini
禁止受到伤害: 真
```

### 禁止选择和作为目标

**英文** `isUnselectableAsTarget` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

默认效果为isUnselectable。可以用来创造不能被选择但是可以被攻击，回收的单位

```ini
禁止选择和作为目标: 假
```

### 禁止运输空军和海军

**英文** `transportUnitsBlockAirAndWaterUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

禁止运输空军和海军，默认为true。如果为true，则此单位只能运输LAND单位。

```ini
禁止运输空军和海军: 假
```

### 禁止直接攻击

**英文** `canNotBeDirectlyAttacked` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

无敌，如果为true，任何单位都不能直接瞄准此单位，也不会受到范围武器伤害。则还将在胜利/失败检查中跳过检查。
注：如果初始单位有这个属性且可能会导致游戏开局就秒输时，Mod作者会看到警告消息

```ini
禁止直接攻击: 真
```

### 开始卸载单位

**英文** `startUnloadingTransport` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

开始卸载单位

```ini
开始卸载单位: 真
```

### 可被攻击或损坏

**英文** `canBeAttackedAndDamaged` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

可以被攻击或受到伤害。

```ini
可被攻击或损坏
```

### 可创建于画面之外

**英文** `createWhenOffscreen` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

允许在画面之外产生此效果，默认为false。

```ini
可创建于画面之外: 真
```

### 可攻击

**英文** `canAttack` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属` `攻击` `炮塔`

默认值为true。设置为false以阻止子单位自动攻击。

```ini
可攻击
```

### 可以回收资源

**英文** `canReclaimResources` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则此单位可以收集资源，这对于resourceRate很有用。

```ini
可以回收资源: 真
```

### 可以开火

**英文** `canShoot` · **类型** `bool` · **权威类型** `bool` · **可用节** `炮塔`

可以开火，默认为true

```ini
可以开火: 真
```

### 可以修单位

**英文** `canRepairUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，此单位可以修复单位。 （isBuilder：true是必需的）

```ini
可以修单位: 真
```

### 可以修建筑

**英文** `canRepairBuildings` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，该单位可以修复建筑。 （isBuilder：true是必需的）

```ini
可以修建筑: 真
```

### 可以自动修

**英文** `autoRepair` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

自动维修附近单位，范围为nanoRange。 （isBuilder：true是必需的）

```ini
可以自动修: 真
```

### 空气效果

**英文** `atmospheric` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

添加阻力使此效果减慢速度，并添加随机移动。用于模拟尾焰。

```ini
空气效果: 真
```

### 空闲自动归位

**英文** `shouldResetTurret` · **类型** `bool` · **权威类型** `bool` · **可用节** `炮塔`

默认为true。 填false时禁止炮塔自动归位。

```ini
空闲自动归位: 假
```

### 拦截抛射体移除目标存活时间

**英文** `interceptProjectile_removeTargetLifeOnly` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

默认值为false，当为false时射抛射体移除。true时使被击中的抛射体爆炸或分裂。用于设定命中抛射体时是否执行分裂逻辑。

```ini
拦截抛射体移除目标存活时间: 真
```

### 离开高度忽略父单位

**英文** `exitHeightIgnoreParent` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

离开时的高度不考虑父高度;用于与父单位一起建造时分离附件

```ini
离开高度忽略父单位: 真
```

### 隶属

**英文** `slave` · **类型** `bool` · **权威类型** `bool` · **可用节** `炮塔`

锁定此转塔的方向，并为附加的转塔发射冷却时间。常用于多炮管单位。

```ini
隶属: 真
```

### 列表图像绘图顺序

**英文** `stack_drawInReverseOrder` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

列表图像绘图顺序

```ini
列表图像绘图顺序: 真
```

### 落到地面上

**英文** `landOnGround` · **类型** `bool` · **权威类型** `bool` · **可用节** `运动`

闲置时使飞行器降落。

```ini
落到地面上: 假
```

### 落地灰尘

**英文** `dust_effect` · **类型** `bool` · **可用节** `腿`

脚落地时产生灰尘。

```ini
落地灰尘: 真
```

### 每个炮塔向不同目标射击

**英文** `turretMultiTargeting` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击`

允许每个炮塔同时向不同的目标射击。如果使用[turretlimitingAngle(限制角度)非常有用

```ini
每个炮塔向不同目标射击: 真
```

### 迷雾中显示

**英文** `alwaysVisibleInFog` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

抛射体总是在迷雾中显示。

```ini
迷雾中显示: 假
```

### 命中回收抛射体且改变瞄准

**英文** `instantReuseLast_alsoChangeTurretAim` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

使炮塔瞄准受到命中目标扫描效果的影响，对光束武器有用

```ini
命中回收抛射体且改变瞄准: 真
```

### 命中回收抛射体区域伤害列表

**英文** `instantReuseLast_keepAreaDamageList` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

瞬间命中并回收最后的抛射体并且保持区域伤害列表，默认false,保持列表是1.13的正常行为，造成区域伤害不会第二次生效，但不是很有用。只有当您想要旧的行为时才使用这个。

```ini
命中回收抛射体区域伤害列表: 真
```

### 命中时更改所属

**英文** `convertHitToSourceTeam` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

将更改被命中的单位转换队伍为自身所属。

```ini
命中时更改所属: 真
```

### 末端图像着色

**英文** `image_end_teamColors` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

末端图像使用阵营色。

```ini
末端图像着色: 真
```

### 目标死后存活

**英文** `liveAfterAttachedDies` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

目标死后动画继续存活,使用AttachedToUnit时默认为false.

```ini
目标死后存活: 假
```

### 目标为地面

**英文** `targetGround` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

抛射体瞄准并攻击目标所在的地面，而不是追踪目标。直接伤害(directDamage)将会失效，需要使用areaDamage和areaRadius。

```ini
目标为地面: 真
```

### 目标为地面包括目标高度

**英文** `targetGround_includeTargetHeight` · **类型** `bool` · **权威类型** `float` · **可用节** `抛射体`

区域影响AA武器

```ini
目标为地面包括目标高度: 40
```

### 能量需要充满

**英文** `energyNeedsToRechargeToFull` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

若果能量耗尽，则需要完全充能才能进行攻击。

```ini
能量需要充满: 真
```

### 逆向灰尘效果

**英文** `dustEffectReverse` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

填true时，在倒车时也能产生效果

```ini
逆向灰尘效果: 真
```

### 逆向时反转运动效果

**英文** `movementEffectReverseFlipEffects` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

倒车时反转效果

```ini
逆向时反转运动效果: 真
```

### 逆向水波效果

**英文** `splastEffectReverse` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

填true时，在倒车时也能产生效果

```ini
逆向水波效果: 真
```

### 排除不完整建筑

**英文** `excludeIncompleteBuildings` · **类型** `bool` · **权威类型** `bool` · **可用节** `放置规则`

排除不完整的建筑

```ini
排除不完整建筑: 真
```

### 排除非建筑

**英文** `excludeNonBuildings` · **类型** `bool` · **权威类型** `bool` · **可用节** `放置规则`

排除非建筑

```ini
排除非建筑:真
```

### 炮塔使用队伍色

**英文** `teamColorsOnTurret` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

默认为false。在转塔上也应用团队颜色。

```ini
炮塔使用队伍色: 真
```

### 炮塔随主体旋转

**英文** `turretRotateWithBody` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击`

炮塔随主体旋转。默认true

```ini
炮塔随主体旋转: 真
```

### 偏向于临近一侧

**英文** `favourOppositeSideNeighbours` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

填true时，使得左右的腿脚拉开时间差，这样更好看。否则看起来顺拐。计算相邻的X比接近的Y查10倍时间。

```ini
偏向于临近一侧: 真
```

### 强制卸载单位

**英文** `forceUnloadTransportNow` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

立即强制卸载单位

```ini
强制卸载单位: 真
```

### 切换价格为流造价

**英文** `switchPriceWithStreamingCost` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

快捷设置为默认资金消耗方式或为流式建造方式。建议使用模板快速将一个模组为所有单位切换流资源。

```ini
切换价格为流造价: 真
```

### 切换至侵略性队伍

**英文** `switchToAggressiveTeam` · **类型** `boolean` · **权威类型** `boolean` · **可用节** `行动`

将此单位队伍更改为侵略性的内置队伍。不能被其它单位捕获。

```ini
切换至侵略性队伍: 真
```

### 切换至中立队伍

**英文** `switchToNeutralTeam` · **类型** `boolean` · **权威类型** `boolean` · **可用节** `行动`

将队伍更改为中立。该队伍与其他所有队伍结盟。除非使用[core]stayNeutral:true，否则它将被附近的单位捕获。

```ini
切换至中立队伍: 假
```

### 清除当前路径点

**英文** `clearActiveWaypoint` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

清除当前路径，也就是停止当前动作，执行下一个路径点。

```ini
清除当前路径点: 真
```

### 清除所有动作冷却时间

**英文** `clearAllActionCooldowns` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

清除所有动作冷却时间，使其立即可用。

```ini
清除所有动作冷却时间: 真
```

### 清除所有路径点

**英文** `clearAllWaypoints` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

清除当前所有路径。

```ini
清除所有路径点: 真
```

### 任意玩家选中时绘制

**英文** `onlyWhenSelectedByAnyPlayer` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

Show when any player selects this unit.（在任何玩家选中本单位值绘制贴花）

```ini
任意玩家选中时绘制: 真
```

### 删除自身

**英文** `deleteSelf` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

删除执行此操作的单位，没有死亡效果，不执行死亡触发。

```ini
删除自身: 真
```

### 闪电效果

**英文** `lightingEffect` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

将抛射体图像修改为闪电，一般需要instant(瞬间命中)搭配.

```ini
闪电效果: 真
```

### 闪光映射到地面

**英文** `lightCastOnGround` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

将闪光效果映射到地面，如火炮的光和抛射体是分离的。

```ini
闪光映射到地面: 真
```

### 射击后重置炮塔目标

**英文** `clearTurretTargetAfterFiring` · **类型** `bool` · **权威类型** `bool` · **可用节** `炮塔`

射击后重置炮塔目标。

```ini
射击后重置炮塔目标:
```

### 实验单位标志

**英文** `experimental` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

标记单位为实验单位。影响缩小图标和游戏结束统计。

```ini
实验单位标志: 真
```

### 使用者能够控制此单位时绘制

**英文** `onlyPlayersWithUnitControl` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

Only draw if viewing player could control this unit（仅当观看者可以控制此单元时才绘制贴花）

```ini
使用者能够控制此单位时绘制: 真
```

### 是虫子

**英文** `isBug` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

是否为虫子，用于沙盒中的单独分类。

```ini
是虫子: 假
```

### 是否算在采集者计数中

**英文** `whenUsingAsHarvester_includeOtherHarvesterCounts` · **类型** `bool` · **权威类型** `bool` · **可用节** `AI`

当此单位用作采集者时，是否算在其它采集者统计中。

```ini
是否算在采集者计数中: 真
```

### 是建造者

**英文** `isBuilder` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

若需要此单位建造建筑物，则通常需要此代码。并且默认设为[ai] useAsBuilder。

```ini
是建造者: 真
```

### 是建筑

**英文** `isBuilding` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

定义单位是否为建筑物。

```ini
是建筑: 真
```

### 是生物

**英文** `isBio` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

此单位是否为生物，为生物则影响死亡声音和血迹（图像在drawable/blood_mark.png，hideScorchMark：true时可以隐藏）非生物则为黑色爆炸效果。

```ini
是生物: 真
```

### 是主构建炮塔

**英文** `isMainNanoTurret` · **类型** `bool` · **权威类型** `bool` · **可用节** `炮塔`

默认为false。用于创建建筑物的炮塔。只能在一个炮塔上为true，并且canShoot设置为false。

```ini
是主构建炮塔: 真
```

### 寿终爆炸

**英文** `explodeOnEndOfLife` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

默认为false。True则会在寿命终结时爆炸，并产生其命中时该有的效果。而不是直接消失。

```ini
寿终爆炸: 真
```

### 水波效果

**英文** `splastEffect` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

在水上时创建水波效果。默认为false

```ini
水波效果: 真
```

### 瞬间命中

**英文** `instant` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

开火时立即击中目标,用于激光、闪电、波束。

```ini
瞬间命中: 真
```

### 瞬间命中回收最后抛射体

**英文** `instantReuseLast` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

用于激光和闪电。激光或闪电命中后还会存在一段时间，当此抛射体命中后，清除上一个抛射体效果。使得只有一个抛射体存在。如使用高频射速变成光束武器，而不是瞎眼叠加。

```ini
瞬间命中回收最后抛射体: 真
```

### 死亡爆炸

**英文** `explodeOnDeath` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

单位死亡爆炸时腿脚跟着一起爆炸

```ini
死亡爆炸: 假
```

### 死亡产生单位所属

**英文** `unitsSpawnedOnDeath_setToTeamOfLastAttacker` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，死亡时产生的单位将更改归属为最后一个攻击者的队伍。

```ini
死亡产生单位所属: 真
```

### 死亡核爆

**英文** `nukeOnDeath` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

不推荐使用。建议使用死亡武器代替，其拥有更丰富的设定选项。如果为true，单位死亡时会生成一个内置的核抛射体爆炸效果。

```ini
死亡核爆: 真
```

### 死亡核爆条件

**英文** `nukeOnDeathDisableWhenNoNuke` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

不推荐使用。默认为false。如果为true，则禁用核武器时，该单位死亡时不会产生核爆炸。

```ini
死亡核爆条件: 真
```

### 死亡痕迹

**英文** `hideScorchMark` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则禁止单位死亡时留下焦痕。

```ini
死亡痕迹: 真
```

### 死亡缓慢降落

**英文** `slowDeathFall` · **类型** `bool` · **权威类型** `bool` · **可用节** `运动`

为"true"时，被击毁时降落速度减慢，并且向前滑行一段距离。注意，必须设置死亡图像，不然坠落后会显示生前图像，而不是直接炸掉。

```ini
死亡缓慢降落: 真
```

### 死亡撞击地面爆炸

**英文** `explodeOnDeathGroundCollision` · **类型** `boolean` · **权威类型** `boolean` · **可用节** `核心`

默认为true。如果为false，则禁用接触地面时内置爆炸效果。

```ini
死亡撞击地面爆炸: 假
```

### 随父物体移动

**英文** `moveWithParent` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

当父单位移动时也移动抛射体。对光束效果武器很有用。

```ini
随父物体移动: 真
```

### 随角度旋转

**英文** `rotate_with_direction` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

默认为true。设置为false时将单位图像锁定为0度。通常与animation_direction_ *一起用作多向视图。

```ini
随角度旋转: 假
```

### 缩放图标永不显示

**英文** `icon_zoomed_out_neverShow` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

缩放图标永不显示。

```ini
缩放图标永不显示: 假
```

### 缩放效果

**英文** `pingPong` · **类型** `bool` · **权威类型** `bool` · **可用节** `动画`

结束后反向播放动画，它一般用来做生物的呼吸效果。

```ini
缩放效果: 真
```

### 缩小时绘制脚

**英文** `drawFootWhenZoomedOut` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

在缩小显示倍数(看到东西更多)后绘制脚。False时不绘制。为了提高绘制腿性能，默认值根据单位大小而变化。如果单独设置阴影则可能比较怪异。

```ini
缩小时绘制脚: 假
```

### 缩小时绘制腿

**英文** `drawLegWhenZoomedOut` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

在缩小显示倍数(看到东西更多)后绘制腿。False时不绘制。为了提高绘制腿性能，默认值根据单位大小而变化。

```ini
缩小时绘制腿: 假
```

### 缩小时可产生效果

**英文** `createWhenZoomedOut` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

允许缩小时产生此效果，默认值为true

```ini
缩小时可产生效果: 假
```

### 锁定腿脚旋转随主炮塔

**英文** `lock_leg_rotation_with_main_turret` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

锁定腿脚图像旋转追随主炮塔

```ini
锁定腿脚旋转随主炮塔: 真
```

### 锁定腿脚运动

**英文** `lockLegMovement` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

锁定腿脚防止乱动。

```ini
锁定腿脚运动
```

### 锁定旋转

**英文** `lockRotation` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

锁定方向，禁止旋转。

```ini
锁定旋转
```

### 锁定运动

**英文** `lockMovement` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

禁止使用腿脚，将其锁定到主体。用于将行走单位转换为飞行单。

```ini
锁定运动: 真
```

### 锁定主体旋转随主炮塔

**英文** `lock_body_rotation_with_main_turret` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

锁定主体图像旋转追随主炮塔

```ini
锁定主体旋转随主炮塔: 真
```

### 提取资源包括父单位或载具

**英文** `takeResources_includeParent` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

提取资源，包括父单位和运输载具。

```ini
提取资源包括父单位或载具: 真
```

### 提取资源包括载具内

**英文** `takeResources_includeUnitsInTransport` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

提取资源，包括载具内单位。

```ini
提取资源包括载具内: 真
```

### 提取资源仅搜索

**英文** `takeResources_searchOnly` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

#新增快捷方式 eg. (
takeResources_maxUnits=200
takeResources_discardCollected=true
takeResources_keepResourcesOnTarget=true
)
注意:填写本代码时必须包含至少一下的其中一个代码
takeResources_includeUnitsWithinRange:#

```ini
提取资源仅搜索: 真
```

### 提取资源克隆

**英文** `takeResources_keepResourcesOnTarget` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

不从目标添加或删除资源。但克隆资源。与takeResources_discardCollected和takeResources_triggerActionIfAnyCollected一起使用以创建资源检测器。

```ini
提取资源克隆: 真
```

### 提取资源排除不足

**英文** `takeResources_excludeUnitsWithoutAllResources` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

提取资源，排除缺乏资源者。默认为true.

```ini
提取资源排除不足: 真
```

### 提取资源删除

**英文** `takeResources_discardCollected` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

从目标中获取资源，但不向自身添加也就是删除。

```ini
提取资源删除: 假
```

### 提取资源直接转换至零

**英文** `takeResources_directTransferStoppingAtZero` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

提取资源直接转换至0,用于完全转换。如果目标上的资源少于转帐金额，则只有剩余的资源将被转帐。不支持使用其他一些takeResources_*代码

```ini
提取资源直接转换至零: 真
```

### 添加路径点路径可达

**英文** `addWaypoint_target_mapMustBeReachable` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

添加的路径点靠近某目标时必须是有可到达路径的。

```ini
添加路径点路径可达: 真
```

### 添加路径点序列位置

**英文** `addWaypoint_prepend` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

添加到路径点的开头或结尾

```ini
添加路径点序列位置: 假
```

### 停止最后动画

**英文** `stopLastAnimation` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

停止最后一个动画，跳过融合

```ini
停止最后动画: 真
```

### 图像应用队伍颜色

**英文** `image_applyTeamColors` · **类型** `bool` · **权威类型** `bool` · **可用节** `炮塔`

炮塔图像使用队伍颜色。默认为false。

```ini
图像应用队伍颜色: 真
```

### 脱离父单位

**英文** `disconnectFromParent` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

用于子单位脱离父单位

```ini
脱离父单位: 真
```

### 完成最会动画

**英文** `finishPlayingLastAnimation` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

完成最后一个动画，包括融合

```ini
完成最会动画: 真
```

### 玩家可以取消

**英文** `canPlayerCancel` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

玩家可以取消此动作。

```ini
玩家可以取消: 假
```

### 维修范围按近战逻辑

**英文** `nanoRangeForRepairIsMelee` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

定义此单位维修时是否为近战算法，默认计算是从自身中心到目标中心，如果单位过大运动又受到阻碍，则虽然看起来很近，然而实际太远无法建造。
近战类算法则是算上其半径，由中心计算改为从边缘计算。

```ini
维修范围按近战逻辑: 真
```

### 尾焰

**英文** `trailEffect` · **类型** `bool/effect` · **权威类型** `bool/effect` · **可用节** `抛射体`

类似于抛射体的尾焰

```ini
尾焰: 真, 尾焰: CUSTOM:rocketThrust
```

### 为可选初始单位

**英文** `isPickableStartingUnit` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则将单位添加到游戏高级设置菜单中，用于可选初始单位菜单。

```ini
为可选初始单位: 真
```

### 为可选单位开局后从天而降

**英文** `startFallingWhenStartingUnit` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则游戏开始时，此单位会从从天而降。

```ini
为可选单位开局后从天而降: 真
```

### 为零时显示

**英文** `displayWhenZero` · **类型** `bool` · **权威类型** `bool` · **可用节** `全局资源`

资源为零时也显示。

```ini
为零时显示: 真
```

### 未完成时检查自动触发

**英文** `autoTriggerCheckWhileNotBuilt` · **类型** `bool` · **可用节** `核心`

- 默认为 false - 当 true 时，即使单位未完成建造，所有自动触发器仍在运行

```ini
未完成时检查自动触发: 真
```

### 我方队伍选中时绘制

**英文** `onlyWhenSelectedByOwnPlayer` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

Only draw if the viewing player is the same as this unit.（只有当玩家查看本单位时，才可绘制贴花。）

```ini
我方队伍选中时绘制 : 真
```

### 无能量时死亡

**英文** `dieOnZeroEnergy` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果能量值为零，该单位死亡。

```ini
无能量时死亡: 真
```

### 物理效果

**英文** `physics` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

撞击地面时反抛射体。需要高度才能生效。

```ini
物理效果: 真
```

### 显示队列条

**英文** `showQueueBar` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

填true时显示操作、生成的队列条,false禁用。默认为true.

```ini
显示队列条: 假
```

### 显示范围UI

**英文** `showRangeUIGuide` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击` `炮塔`

显示攻击范围的白圈。默认true.

```ini
显示范围UI: 真
```

### 显示给敌人

**英文** `isAlsoViewableByEnemies` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

允许敌方玩家看到这个单位的动作，对显示其他玩家有用(例如导抛射体数量，收集的物品)

```ini
显示给敌人: 真
```

### 显示给盟友

**英文** `isAlsoViewableByAllies` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

允许盟友玩家看到这个单位的动作，对显示其他玩家有用(例如导抛射体数量，收集的物品)

```ini
显示给盟友: 真
```

### 显示护盾条

**英文** `showShieldBar` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

填true时显示护盾条,false禁用。默认为true.

```ini
显示护盾条: 假
```

### 显示能量条

**英文** `showEnergyBar` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

填true时显示能量条,false禁用。默认为true.

```ini
显示能量条: 假
```

### 显示生命条

**英文** `showHealthBar` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

填true时显示生命条,false禁用。默认为true.

```ini
显示生命条: 假
```

### 显示剩余库存

**英文** `displayRemainingStockpile` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

显示剩余库存,显示为根据资源计算可以触发操作的次数。

```ini
显示剩余库存: 真
```

### 显示为整数

**英文** `displayRoundedDown` · **可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

对资源进行舍入显示为整数。

```ini
显示为整数: 真
```

### 显示选择指示

**英文** `showSelectionIndicator` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

(单位的选择框)

```ini
显示选择指示: 假
```

### 显示于敌人小地图

**英文** `showOnMinimapToEnemies` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

是否在敌人小地图上显示。目前不支持逻辑。

```ini
显示于敌人小地图: 假
```

### 显示运输条

**英文** `showTransportBar` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

填true时显示运输条,false禁用。默认为true.

```ini
显示运输条: 假
```

### 显示在沙盒中

**英文** `showInEditor` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

设置为false可在沙箱编辑器中隐藏此单位。 （默认为true）

```ini
显示在沙盒中: 假
```

### 显示在小地图上

**英文** `showOnMinimap` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

默认为true。如果为false，则在小地图上不显示此单位。

```ini
显示在小地图上: 假
```

### 卸载附属

**英文** `attachments_unload` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

卸载所有附属。可以与attachments_onlyonslot一起使用。与运输单位卸载相同

```ini
卸载附属: 真
```

### 卸载所有单位时恢复为中立

**英文** `convertToNeutralIfNotTransporting` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

卸载单位时将其恢复为中立队伍。与whileNeutralTransportAnyTeam一起使用。

```ini
卸载所有单位时恢复为中立: 真
```

### 一次只有一个单位

**英文** `onlyOneUnitAtATime` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

填true时选中多个同类单位时，只有一个执行此行为。

```ini
一次只有一个单位: 真
```

### 移动后滑动

**英文** `moveSlidingMode` · **类型** `bool` · **权威类型** `bool` · **可用节** `运动`

移动后滑动，相当于惯性。和单位的速度，加速度，转弯速度有关。True时受推力影响

```ini
移动后滑动: 真
```

### 移动忽略转向

**英文** `moveIgnoringBody` · **类型** `bool` · **权威类型** `bool` · **可用节** `运动`

true效果即不必转弯便能运动，false效果即原地转弯完成才能运动

```ini
移动忽略转向: 真
```

### 移动时脱离

**英文** `deattachIfWantingToMove` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

若收到移动命令，子单位将自动分离。这包括来自action的路径点。可以用来制作机场。

```ini
移动时脱离
```

### 阴影

**英文** `shadow` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

true时绘制阴影。如果使用imageShadow则强制为true

```ini
阴影: 假
```

### 阴影锁定为主炮塔方向

**英文** `lock_shadow_rotation_with_main_turret` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

将身体图像阴影锁定到主炮塔的方向

```ini
阴影锁定为主炮塔方向: 真
```

### 阴影图像指定

**英文** `image_shadow_frames` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

true时阴影不随单位移动而改变方向。

```ini
阴影图像指定: 真
```

### 隐藏

**英文** `hidden` · **可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `腿` `资源`

- **全局资源**：`bool`
- **腿**：`logic boolean`
- **资源**：`Boolean`

对玩家隐藏这个资源

```ini
隐藏: 真
```

### 用作采集

**英文** `useAsHarvester` · **类型** `bool` · **权威类型** `bool` · **可用节** `AI`

作为采集者。如果单位可以回收资源，则默认为true

```ini
用作采集
```

### 用作建造者

**英文** `useAsBuilder` · **类型** `bool` · **权威类型** `bool` · **可用节** `AI`

作为建造者。如果单位可以建造或维修建筑物，则设置为true。 默认为[core] isBuilder。

```ini
用作建造者:
```

### 用作运输

**英文** `useAsTransport` · **类型** `bool` · **权威类型** `bool` · **可用节** `AI`

作为载具。如果单位可以运输单位，则默认为true.如果您的单位可以运输又能攻击，则AI可能囤积起来而不攻击，则需要设置false.

```ini
用作运输
```

### 优先考虑父单位的主要目标

**英文** `prioritizeParentsMainTarget` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

子单位优先考虑父单位当前的目标。

```ini
优先考虑父单位的主要目标
```

### 友伤

**英文** `friendlyFire` · **类型** `bool/string` · **权威类型** `bool/string` · **可用节** `抛射体`

范围武器有友伤，可以伤害自己单位。铁锈限制不能对盟友有伤害。负数友伤可以用于范围维修，如果修盟友则需要用中立单位实现。参数（false，true，only-ignoreEnemy）（假，真，忽略敌人的友伤）

```ini
友伤: 假 / 友伤: 真 / 友伤: only-ignoreEnemy
```

### 预热不重置

**英文** `warmupNoReset` · **类型** `bool` · **权威类型** `bool` · **可用节** `炮塔`

默认为false。射击后未重设真正的预热时。与warmupCallDownRate一起使用

```ini
预热不重置: 真
```

### 允许超高频触发

**英文** `autoTriggerCooldownTime_allowDangerousHighCPU` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

自动触发冷却时间，允许高CPU占用，也就是可以使用非常高频的触发，但不建议这么做。

```ini
允许超高频触发: 真
```

### 允许多个队列

**英文** `allowMultipleInQueue` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

允许多次点击此行为形成队列，false时则只能点一次，不能累加。

```ini
允许多个队列: 真
```

### 允许中立时被AI俘虏

**英文** `allowCaptureWhenNeutralByAI` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

(推测,以前人机不能捕获中立单位)

```ini
允许中立时被AI俘虏: 真
```

### 在雾中显示

**英文** `showInFog` · **类型** `bool` · **权威类型** `bool` · **可用节** `效果`

默认为false

```ini
在雾中显示: 真
```

### 载具禁止装载其它载具

**英文** `transportUnitsBlockOtherTransports` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

默认值为true，如果为false，则此载具可以装载其它载具。比如步单位装物资，载具装步单位，运输船装载具。

```ini
载具禁止装载其它载具: 假
```

### 载具显示卸载按钮

**英文** `transportUnitsAddUnloadOption` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

是否显示卸载按钮

```ini
载具显示卸载按钮: 假
```

### 载员只占用一个槽位

**英文** `transportUnitsEachUnitAlwaysUsesSingleSlot` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

所有载员无论体积只占用一个运输槽。但体积大于载员数时仍然不能进入。

```ini
载员只占用一个槽位: 真
```

### 增加被运输单位

**英文** `addTransportedUnits` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

添加到运输单位槽中。

```ini
增加被运输单位
```

### 帧随机

**英文** `frameIndexRandom` · **类型** `bool -?` · **权威类型** `bool -?` · **可用节** `效果`

随机增减索引值，用于随机使用同组内不同的图像。

```ini
帧随机: 真
```

### 阵营色

**英文** `teamColors` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

bool. Enable team coloring on image and imageStack（布尔值。在图像和图像堆栈上启用队伍色）

```ini
阵营色: 真
```

### 阵营色色相

**英文** `teamColorsUseHue` · **类型** `bool` · **权威类型** `bool` · **可用节** `图像`

luke不建议使用。单位上的绿色像素转换为团队颜色。 若为true整个单位都带有团队色彩。 默认为false，挺难看的。

### 只在最远时保持移动

**英文** `hold_moveOnlyIfFurthest` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

保持移动仅在最远时候。

```ini
只在最远时保持移动: 真
```

### 中立时可装载任意所属单位

**英文** `whileNeutralTransportAnyTeam` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果是中立的，则这个部队可以装载任何队伍的部队。可用于占领中立单位，比如坦克需要驾驶员，步单位建筑平民建筑。

```ini
中立时可装载任意所属单位: 真
```

### 中立时转换为载员队伍

**英文** `whileNeutralConvertToTransportedTeam` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

中立时将其转换为载员队伍。与whileNeutralTransportAnyTeam一起使用，用于占领中立单位。

```ini
中立时转换为载员队伍: 真
```

### 重定向伤害到父单位

**英文** `redirectDamageToParent` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

将对附属造成的伤害重定向到父单位，而不是伤害子单位自身。

```ini
重定向伤害到父单位:
```

### 重定向伤害到父单位护盾

**英文** `redirectDamageToParent_shieldOnly` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

只将对附属造成的伤害重定向到父单位的护盾，而不是伤害子单位自身。如果盾没了那就伤害自身，如模块蜘蛛。

```ini
重定向伤害到父单位护盾:
```

### 重新瞄准在飞行时

**英文** `retargetingInFlight` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

在飞行过程中重新瞄准一个新的目标，非常适合投掷式武器和相互碰撞的抛射体

```ini
重新瞄准在飞行时: 真
```

### 转换时保留标签

**英文** `convertTo_keepCurrentTags` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

将单位转换为另一个单位。并且保留拥有的标签。

```ini
转换时保留标签: 真
```

### 转换所属时保留内部单位所属

**英文** `transportUnitsOnTeamChangeKeepCurrentTeam` · **类型** `bool` · **权威类型** `bool` · **可用节** `核心`

如果为true，则单位转换阵营时仍然将运输的单位保留在其原始队伍中。

```ini
转换所属时保留内部单位所属: 真
```

### 子单位一同旋转

**英文** `rotateWithParent` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

旋转时子单位是否一同旋转。

```ini
子单位一同旋转
```

### 自动切换目标

**英文** `autoTargetingOnDeadTarget` · **类型** `bool` · **权威类型** `bool` · **可用节** `抛射体`

如果目标死亡则自动切换单位。填true时如果有代码"targetGround:true"抛射体将会追踪距离它最近的单位。如果没有"targetGround"时将会在单位死亡后再改变目标。

```ini
自动切换目标: 真
```

### 自动停火

**英文** `stopTargetingAfterFiring` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击`

单位射击后停止瞄准。很少使用或需要。如用于您不希望胡乱攻击浪费弹药的单位。

```ini
自动停火: 真
```

### 自杀攻击

**英文** `dieOnAttack` · **类型** `bool` · **权威类型** `bool` · **可用节** `攻击`

攻击时自毁。

```ini
自杀攻击: 真
```

### 自身队伍的盟友选中时绘制

**英文** `onlyWhenSelectedByAllyNotOwnPlayer` · **类型** `bool` · **权威类型** `bool` · **可用节** `贴花`

非自身队伍的盟友选中时绘制

```ini
自身队伍的盟友选中时绘制: 真
```

### 总是按一次

**英文** `alwaysSinglePress` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

点击就执行，不需要确认。默认false,不需要在手机上确认，使用canPlayerCancel: false和allowMultipleInQueue: false也隐藏队列接口。

```ini
总是按一次: 真
```

### 总是攻击父单位的主要目标

**英文** `alwaysAllowedToAttackParentsMainTarget` · **类型** `bool` · **权威类型** `bool` · **可用节** `附属`

子单位总是跟随攻击父单位的主要目标。

```ini
总是攻击父单位的主要目标
```

### 最小移动距离检查相邻

**英文** `holdDisMin_checkNeighbours` · **类型** `bool` · **权威类型** `bool` · **可用节** `腿`

移动最小距离时检查相邻的腿脚。

```ini
最小移动距离检查相邻: 真
```

### 作为AI的全局资源

**英文** `equivalentGlobalResourceForAI` · **类型** `Boolean` · **权威类型** `Boolean` · **可用节** `资源`

用于向AI提示具有本地资源的资源节点可用于获取不同的全局资源。例如，当一台收割机卸载资源

```ini
作为AI的全局资源: 真
```

### action_#_whenBuilding_cannotMove

**类型** `bool` · **可用节** `核心`

在执行操作时停止单位移动。对于部署类似动作很有用。

```ini
action_1_whenBuilding_cannotMove: 真
```

### AI作为建筑

**英文** `ai_considerSameAsBuilding` · **类型** `bool` · **权威类型** `bool` · **可用节** `行动`

填建筑。考虑动作的优先级将会跟建筑物建优先级一样。

```ini
AI作为建筑: 真
```

### appendResourceInHUD_whenThisZero

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

默认为true。为false时附加资源（如用于显示最大值的资源）与父资源一起隐藏。

```ini
appendResourceInHUD_whenThisZero: 假
```

### builtFrom__forceNano

**类型** `bool` · **权威类型** `bool` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_forceNano

```ini
builtFrom_1_forceNano: 真
```

### canBuild_#_isLocked

**类型** `bool` · **可用节** `核心`

data.sections.core.canBuild_#_isLocked

```ini
canBuild_1_isLocked: 真
```

### displayColorUseInText

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

默认为true

```ini
displayColorUseInText: 假
```

### displayInHud

**类型** `bool` · **权威类型** `bool` · **可用节** `全局资源`

- 用于 appendResourceInHUD（在HUD中追加资源） 中的资源，而不会隐藏它们，这有其他副作用

```ini
displayInHud: 真
```

### displayNameHideWhenIconShownInHUD

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

默认为false

```ini
displayNameHideWhenIconShownInHUD: 真
```

### displayNameHideWhenIconShownInText

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

(默认为false) - 有助于缩短说明文本

```ini
displayNameHideWhenIconShownInText: 真
```

### displayTextAppendResourceWithGap

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

默认为false。将不相关的资源放在同一行时，在资源之间添加空格。

```ini
displayTextAppendResourceWithGap: 真
```

### displayWithRounding

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

将资源的小数部分用四舍五入法变成整数，此代码为真后，该资源数字一定是整数

```ini
displayWithRounding: 真
```

### dustEffect

**类型** `bool` · **权威类型** `bool` · **可用节** `图像`

在地面上产生灰尘效果。默认为false

```ini
dustEffect: 真
```

### iconImageUseInText

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

默认为true

```ini
iconImageUseInText: 假
```

### includeInStats

**可按节区分类型** · **权威类型** `Boolean` · **可用节** `全局资源` `资源`

- **全局资源**：`bool`
- **资源**：`Boolean`

包括该资源，与valueInStats类似

```ini
includeInStats: 真
```

### onConvertKeepExistingUnitInSameSlot

**类型** `bool` · **权威类型** `bool` · **可用节** `附属`

转换单位时如果有相同ID的附属槽位则子单位就会在那个槽位中

```ini
onConvertKeepExistingUnitInSameSlot
```

### refundAllQueuedItems

**类型** `boolean` · **权威类型** `boolean` · **可用节** `行动`

并返还资源

```ini
refundAllQueuedItems: 真
```

### removeAllQueuedItemsWithoutRefund

**类型** `boolean` · **权威类型** `boolean` · **可用节** `行动`

不返还资源

```ini
removeAllQueuedItemsWithoutRefund: 真
```

### showShotDelayBar

**类型** `bool` · **权威类型** `bool` · **可用节** `图像`

(默认为true) - 用于显示开火速度慢的炮塔的冷却指示条(译者注:原文calldown似乎有误，应为cooldown)

```ini
showShotDelayBar: 假
```

### stackHorizontal

**类型** `Boolean` · **权威类型** `Boolean` · **可用节** `资源`

将此代码为真的资源统一排列到同一水平线上。

```ini
stackHorizontal: 真
```

### targetHeightRelative

**类型** `bool` · **权威类型** `bool` · **可用节** `腿`

相对自身的最终高度

```ini
targetHeightRelative: 真
```

### unloadInCurrentPosition

**类型** `bool` · **权威类型** `bool` · **可用节** `附属`

卸载单位时在这个位置进行卸载

```ini
unloadInCurrentPosition
```

### valueInStats

**可按节区分类型** · **权威类型** `Number` · **可用节** `全局资源` `资源`

- **全局资源**：`bool/int`
- **资源**：`Number`

(如果全局且未隐藏，则默认为 1(true)，否则默认为 0(false)） - 用于游戏后的统计和回放的排行榜

```ini
valueInStats: 真
```

---

## 文字（70 个）

### 被运输单位需要标签

**英文** `transportUnitsRequireTag` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

仅允许运输具有这些标签之一的单位。可以用于运输类型分类，如人运物资，步单位车运人，气垫船运车辆。或是子机限制，如航母飞机只能降落到航母。

```ini
被运输单位需要标签: smallTank, soldier
```

### 别名

**英文** `altNames` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

以逗号分隔的名称列表。像名称一样，但优先级较低，对于启用多个自定义nod有用。

```ini
别名: custTank1, customTank1, cTank1
```

### 从载具删除带标签单位

**英文** `deleteNumUnitsFromTransport_onlyWithTags` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `行动`

从载具中删除具有此标签的指定数量单位。

```ini
从载具删除带标签单位: cheapStuff
```

### 地图位置产生指定单位

**英文** `onNewMapSpawn` · **类型** `string` · **权威类型** `string` · **可用节** `核心`

在地图上添加此单位。可以用于覆盖铁锈默认的资源逻辑。参数：
emptyResourcePools_asNeutral  产生位置于空资源池，并且为中立所属
emptyOrOccupiedResourcePools_asNeutral 空的或者已经占用的资源池，并且为中立所属
mapCenter_asNeutral 地图中心，且为中立所属
mapCenter_eachActiveTeam 地图中心，且给与每个玩家
spawnPoint_eachActiveTeam 玩家初始位置，给与每个玩家

```ini
地图位置产生指定单位: spawnPoint_eachActiveTeam
```

### 发送消息给所有敌人

**英文** `showMessageToAllEnemyPlayers` · **类型** `string` · **权威类型** `string` · **可用节** `行动`

发送消息给所有敌人。

```ini
发送消息给所有敌人: Team %{self.playerName} has %{自身资源.gold}
```

### 发送消息至所有

**英文** `showMessageToAllPlayers` · **类型** `string` · **权威类型** `string` · **可用节** `行动`

向所有玩家发送消息。

```ini
发送消息至所有: %{self.playerName} has captured a point
```

### 发送消息至玩家

**英文** `showMessageToPlayer` · **类型** `string` · **权威类型** `string` · **可用节** `行动`

向玩家个人发送信息。自己发给自己。

```ini
发送消息至玩家: There is a 隐藏 敌方 near your defenses
```

### 发送战争快报至玩家

**英文** `showQuickWarLogToPlayer` · **类型** `string` · **权威类型** `string` · **可用节** `行动`

向玩家个人发送战争快报。（在左下角）

```ini
发送战争快报至玩家: Unit decloaked
```

### 放置规则组

**英文** `anyRuleInGroup` · **类型** `string` · **权威类型** `string` · **可用节** `放置规则`

放置规则组名。（仅需要通过此组规则中的1条，而不是全部通过。在其他放置规则中可使用相同的组名称来创建组。）

```ini
放置规则组: struct
```

### 覆盖单位

**英文** `overrideAndReplace` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

用此单位覆盖另一个单位，地图上所有单位也将被替换，可以用来替换dex内无法修改的单位。

```ini
覆盖单位: builder, combatEngineer
```

### 更多锁定消息

**英文** `isLockedAltMessage` · **可按节区分类型** · **权威类型** `string` · **可用节** `行动` `可建造`

- **行动**：`LocaleString`
- **可建造**：`string`

显示更多锁定的原因。

```ini
更多锁定消息: Not enough factories!
```

### 更多锁定消息2

**英文** `isLockedAlt2Message` · **可按节区分类型** · **权威类型** `string` · **可用节** `行动` `可建造`

- **行动**：`LocaleString`
- **可建造**：`string`

显示更多锁定的原因。

```ini
更多锁定消息2: Explosive stockpile is heavily damaged.
```

### 攻击移动类型

**英文** `attackMovement` · **类型** `string` · **权威类型** `string` · **可用节** `攻击`

移动攻击类型。填normal/bomber。能量耗尽时，轰炸机攻击运动将后退。可填moveaway，strafing但无效。

```ini
攻击移动类型: bomber
```

### 混合所选单位所显示的行为

**英文** `showActionsWithMixedSelectionIfOtherUnitsHaveTag` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

如果选择的单位都包含此处使用的标签，则合并“行为(action)”。比如您的步单位通过部署转化成另一个单位，在混合时可以当作同一个单位处理，不再是默认的谁都无法执行操作。例子如红警的盟军大单位在混合选中后依旧可执行部署或解除。

```ini
混合所选单位所显示的行为: 真
```

### 界面显示描述

**英文** `displayDescription` · **类型** `LocaleString` · **权威类型** `LocaleString` · **可用节** `核心`

单位显示给玩家的单位描述。

```ini
界面显示描述: -Fast 运动\n-Light damage
```

### 界面显示名称

**英文** `displayText` · **类型** `LocaleString` · **权威类型** `LocaleString` · **可用节** `核心`

默认单位显示给玩家的单位名称。不填则显示core下的单位name。此条目依据语言设定不同会被下一条覆盖。

```ini
界面显示名称: Custom Tank
```

### 界面显示内部调用

**英文** `displayLocaleKey` · **类型** `string` · **权威类型** `string` · **可用节** `核心`

调用内部语言文件的单位名称和说明的翻译文件。

```ini
界面显示内部调用: units.mechArtillery
```

### 仅许带此标签单位攻击

**英文** `canOnlyBeAttackedByUnitsWithTags` · **类型** `strings(s)` · **权威类型** `strings(s)` · **可用节** `核心`

填标签，只有带有这些标签的单位才能直接定位到该单位。

```ini
仅许带此标签单位攻击: piercingTank, powerfulTank
```

### 仅允许回收特定标签单位

**英文** `canReclaimUnitsOnlyWithTags` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

仅允许此单位回收具有特定标签单位。

```ini
仅允许回收特定标签单位: reclaimable
```

### 仅允许回收特定标签资源

**英文** `canReclaimResourcesOnlyWithTags` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

仅允许回收有此标签的单位。

```ini
仅允许回收特定标签资源: foodResource, goldResource
```

### 仅允许维修特定标签单位

**英文** `canRepairUnitsOnlyWithTags` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

仅允许此单位维修具有特定标签单位。

```ini
仅允许维修特定标签单位: vulnerable
```

### 开火音效

**英文** `shoot_sound` · **类型** `string` · **权威类型** `string` · **可用节** `炮塔`

开火音效，填声音文件。有以下内置条目。attack、move、click、missile_fire、missile_hit、unit_explode、buiding_explode、
tank_firing、cannon_firing、gun_fire、lighting_burst、plasma_fire、plasma_fire2、firing3、firing4、large_gun_fire1、
large_gun_fire2、bug_die、bug_attack、interface_error、nuke_explode、nuke_launch、laser_deflect、laser_deflect2

```ini
开火音效: tank_firing | 开火音效: missile.wav | 开火音效: ROOT:audio/shoot.ogg
```

### 描述

**英文** `description` · **类型** `string` · **权威类型** `string` · **可用节** `行动`

对于的模组描述

```ini
描述: Fires shot on target area
```

### 排错信息

**英文** `debugMessage` · **可用节** `行动`

只有在调试模式开启的沙盒模式中才显示消息

```ini
排错信息: [行动 log] unit launch on %{当前动作目标.x}, %{当前动作目标.y}
```

### 升级自

**英文** `upgradedFrom` · **类型** `string` · **权威类型** `string` · **可用节** `AI`

创建到另一个单位的链接，用于保留同一单位已升级和未升级的计数。

```ini
升级自: builderLevel1
```

### 失败信息

**英文** `cannotPlaceMessage` · **类型** `LocaleString` · **权威类型** `LocaleString` · **可用节** `放置规则`

如果此放置规则失败，则会向玩家显示消息（将成为mergedRuleGroup中的第一个失败规则）

```ini
失败信息: "No factory is nearby"
```

### 死亡产生单位

**英文** `unitsSpawnedOnDeath` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

死亡时产生这些单位。逗号分隔的单位标识符。

```ini
死亡产生单位: tank*5, hoverTank
```

### 死亡音效

**英文** `soundOnDeath` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

为该单位设置自定义死亡声音。

```ini
死亡音效: tankExplosion1.ogg, tankExplosion2.ogg
```

### 锁定消息

**英文** `isLockedMessage` · **可按节区分类型** · **权威类型** `string` · **可用节** `行动` `可建造`

- **行动**：`LocaleString`
- **可建造**：`string`

显示锁定的原因。

```ini
锁定消息: Not enough ammunition
```

### 文本动态更改

**英文** `textPostFix` · **类型** `string` · **权威类型** `string` · **可用节** `行动`

显示为后缀的文本，与textAddUnitName一起用于创建文本UI

```ini
text: [  文本动态更改: ]  从单元添加名称: unitRef self.附属(slot="${slotId}")
```

### 显示短名称

**英文** `displayNameShort` · **可按节区分类型** · **权威类型** `String` · **可用节** `全局资源` `资源`

- **全局资源**：`string`
- **资源**：`String`

在较小的UI上显示的文本（如action的悬浮文本）默认为displayName

```ini
显示短名称: G
```

### 显示名称

**英文** `displayName` · **可按节区分类型** · **权威类型** `String` · **可用节** `全局资源` `资源`

- **全局资源**：`string`
- **资源**：`String`

UI中此资源的名称(如悬停在单元信息上)

```ini
显示名称: Gold
```

### 像用于此标签的单位

**英文** `similarResourcesHaveTag` · **类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

有这些标签时，资源采集单位将视为同一种资源

```ini
像用于此标签的单位: goldResource
```

### 优先级

**英文** `priority` · **可按节区分类型** · **权威类型** `float` · **可用节** `效果` `全局资源`

- **效果**：`string`
- **全局资源**：`float`

如果多个mod单元中定义了同名的资源，则使用具有最高优先级的displayName/displayColor

```ini
优先级: critical
```

### 有此标签才作为采集者

**英文** `onlyUseAsHarvester_ifBaseHasUnitTagged` · **类型** `string` · **权威类型** `string` · **可用节** `AI`

只有当基地有单位标记时才可以作为采集者使用.

```ini
有此标签才作为采集者: mineral, wood, stones
```

### 运动类型

**英文** `movementType` · **类型** `string` · **权威类型** `string` · **可用节** `运动`

定义单位将能够通过的地形以及与单位类型相关的其他属性。类型：无、陆地、建筑、空中、水面、两栖、跨悬崖、跨水和悬崖
NONE、LAND、BUILDING、AIR、WATER、HOVER、OVER_CLIFF、OVER_CLIFF_WATER

```ini
运动类型: 陆地
```

### action_#_convertTo

**类型** `string` · **可用节** `核心`

在1.13版中已弃用，请改用[action_x]节

```ini
action_1_convertTo: customTank_2
```

### action_#_description

**类型** `string` · **可用节** `核心`

动作说明。

```ini
action_1_description: -Converts the tank
```

### action_#_text

**类型** `string` · **可用节** `核心`

选择单位动作时的显示文本，用于说明其目的。

```ini
action_1_text: Upgrade to Custom Tank 2
```

### builtFrom__category

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_category

```ini
builtFrom_1_category: vehicle
```

### builtFrom__icon

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_icon

```ini
builtFrom_1_icon: custom_factory_icon.png
```

### builtFrom__isLockedMessage

**类型** `LocaleString` · **权威类型** `LocaleString` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_isLockedMessage

```ini
builtFrom_1_isLockedMessage: -Needs more population
```

### builtFrom__isLockedMessage_es

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_isLockedMessage_es

```ini
builtFrom_1_isLockedMessage_es: -Necesita más población
```

### builtFrom__name

**类型** `string(s)` · **权威类型** `string(s)` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_name

```ini
builtFrom_1_name: landFactory, airFactory
```

### builtFrom__tooltip

**类型** `LocaleString` · **权威类型** `LocaleString` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_tooltip

```ini
builtFrom_1_tooltip: Builds advanced tanks
```

### builtFrom__tooltip_es

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.builtFrom_{NUM}_tooltip_es

```ini
builtFrom_1_tooltip_es: Construye tanques avanzados
```

### canBuild_#_name

**类型** `string` · **可用节** `核心`

data.sections.core.canBuild_#_name

```ini
canBuild_1_name: tank
```

### canBuild_#_name/pos/isLocked

**类型** `string` · **可用节** `核心`

请改用canBuild部分。

```ini
canBuild_1_name: tank
```

### class

**类型** `string` · **权威类型** `string` · **可用节** `核心`

无实际用处，可以删除。Luke说：保留供将来使用，默认情况下最好为CustomUnitMetadata。

```ini
class: CustomUnitMetadata
```

### description_

**类型** `string` · **权威类型** `string` · **可用节** `行动`

描述多语言。

```ini
description_es: Disparos en el área objetivo
```

### displayDescription_de

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.displayDescription_de

```ini
displayDescription_de: -Schnelle Bewegung\n-Geringer Schaden
```

### displayDescription_es

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.displayDescription_es

```ini
displayDescription_es: -Movimiento rápido\n-Daño ligero
```

### displayDescription_fr

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.displayDescription_fr

```ini
displayDescription_fr: -Mouvement rapide\n-Dégâts légers
```

### displayPostfixInHUD

**可按节区分类型** · **权威类型** `String` · **可用节** `全局资源` `资源`

- **全局资源**：`string`
- **资源**：`String`

在资源数值之后显示的文本

```ini
displayPostfixInHUD: 123
```

### displayPrefixInHUD

**可按节区分类型** · **权威类型** `String` · **可用节** `全局资源` `资源`

- **全局资源**：`string`
- **资源**：`String`

在资源数值前显示的文本，会替换掉资源原本的名称以及冒号，和appendResourceInHUD一同使用时可以用来显示资源限制之类的内容

```ini
displayPrefixInHUD: 321
```

### displayText_de

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.displayText_de

```ini
displayText_de: Benutzerdefinierter Panzer
```

### displayText_es

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.displayText_es

```ini
displayText_es: Tanque Personalizado
```

### displayText_fr

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.displayText_fr

```ini
displayText_fr: Char personnalisé
```

### displayTextPostfix

**可按节区分类型** · **权威类型** `String` · **可用节** `全局资源` `资源`

- **全局资源**：`string`
- **资源**：`String`

与displayPostfixInHUD有着相同的作用，而且它俩不能共存

```ini
displayTextPostfix: 123
```

### displayTextPrefix

**可按节区分类型** · **权威类型** `String` · **可用节** `全局资源` `资源`

- **全局资源**：`string`
- **资源**：`String`

与displayPrefixInHUD有着相同的作用，而且它俩不能共存

```ini
displayTextPrefix: 321
```

### drawLayer

**类型** `string` · **权威类型** `string` · **可用节** `图像`

陆上单位通常默认为ground或ground2。运输船为ground2。wreaks, underwater, bottom, ground, ground2, experimentals, air, top

```ini
drawLayer: bottom
```

### id

**类型** `string` · **权威类型** `string` · **可用节** `行动`

用于标记触发。

```ini
id: fireShot
```

### isLockedMessage_es

**类型** `string` · **权威类型** `string` · **可用节** `可建造`

data.sections.canBuild.isLockedMessage_es

```ini
isLockedMessage_es: -Necesita 2 Cuarteles
```

### name

**可按节区分类型** · **权威类型** `string` · **可用节** `可建造` `核心`

- **可建造**：`string(s)`
- **核心**：`string`

该单位可以创建的单位名列表。可以是建筑物或单位。

```ini
name: setRally, tank, hoverTank, heavyTank
```

### showMessageToPlayer_

**类型** `string` · **权威类型** `string` · **可用节** `行动`

向玩家个人发送信息。自己发给自己。多语言版。注意:这种格式是支持几乎所有字符串显示给玩家，即使当引用不显示它

```ini
showMessageToPlayer_fil: May nakatagong kalaban na malapit sa mga depensa mo
```

### text

**类型** `string` · **权威类型** `string` · **可用节** `行动`

设置文本

```ini
text: Catch Fish, text: Fire: %{自身资源.ammo}
```

### text_

**类型** `string` · **权威类型** `string` · **可用节** `行动`

界面中显示的文字，多语言

```ini
text_es: Fuego %{自身资源.ammo}
```

### weaponSlot__name

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.weaponSlot_{NUM}_name

```ini
weaponSlot_1_name: mainGun
```

### weaponSlotsForAI__name

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.weaponSlotsForAI_{NUM}_name

```ini
weaponSlotsForAI_1_name: antiTank
```

### weaponSlotsForBuild__name

**类型** `string` · **权威类型** `string` · **可用节** `核心`

data.sections.core.weaponSlotsForBuild_{NUM}_name

```ini
weaponSlotsForBuild_1_name: buildTool
```

---

## 效果引用（21 个）

### 爆炸效果

**英文** `explodeEffect` · **类型** `effect ref list` · **权威类型** `effect ref list` · **可用节** `抛射体`

抛射体爆炸效果

```ini
爆炸效果: smallExplosion, CUSTOM:myExplodeEffect
```

### 产生效果

**英文** `spawnEffects` · **类型** `effect ref` · **权威类型** `effect ref` · **可用节** `行动`

产生指定效果

```ini
产生效果: CUSTOM:puff
```

### 创建时效果

**英文** `effectOnCreate` · **类型** `effect ref list` · **权威类型** `effect ref list` · **可用节** `抛射体`

创建时效果，会追随抛射体。

```ini
创建时效果: CUSTOM:puff
```

### 队列产生效果

**英文** `spawnEffectsOnQueue` · **类型** `effect ref` · **权威类型** `effect ref` · **可用节** `行动`

动作准备开始时产生的效果。

```ini
队列产生效果: CUSTOM:steam
```

### 护盾上爆炸效果

**英文** `explodeEffectOnShield` · **类型** `effect ref list` · **权威类型** `effect ref list` · **可用节** `抛射体`

如果目标上有护盾，则使用此效果

```ini
护盾上爆炸效果: CUSTOM:EMPwave
```

### 回收目标效果

**英文** `reclaimEffectAtTarget` · **类型** `effect` · **权威类型** `effect` · **可用节** `图像`

回收目标上产生的效果。

```ini
回收目标效果: CUSTOM:redsparks*2
```

### 回收效果

**英文** `reclaimEffect` · **类型** `effect` · **权威类型** `effect` · **可用节** `图像`

自定义回收效果，可以是任何东西。替换回收时的默认效果

```ini
回收效果: CUSTOM:sap*5
```

### 开火动画

**英文** `shoot_flame` · **类型** `effects` · **权威类型** `effects` · **可用节** `炮塔`

开火动画，内置参考表末尾。Luke推荐为：small, large, smoke, shockwave（小，大，烟，冲击波）。可以自定义。
其余有：medium，largeExplosion，smallExplosion，resourcePoolSmoke，noneExplosion（中等，大爆炸，小爆炸，资源池的绿色烟，没有爆炸）

```ini
eg: 开火动画: smoke, CUSTOM:lightFade, CUSTOM:pop*5
```

### 没建造完成时死亡效果

**英文** `effectOnDeathIfUnbuilt` · **类型** `effect(s) ref` · **权威类型** `effect(s) ref` · **可用节** `核心`

如果这个单位没建造完成就被毁了，则播放此效果

```ini
没建造完成时死亡效果: CUSTOM:implode
```

### 逆向运动效果

**英文** `movementEffectReverse` · **类型** `effect` · **权威类型** `effect` · **可用节** `图像`

自定义倒车效果。

```ini
逆向运动效果: smoke, CUSTOM:fastDust*2, CUSTOM:pop*5
```

### 如果失败则创建效果

**英文** `ifSpawnFailsEmitEffects` · **类型** `effect ref` · **权威类型** `effect ref` · **可用节** `效果`

如果此效果的“spawnChance”失败，则产生这些效果。

```ini
如果失败则创建效果: CUSTOM:greenSparks*4
```

### 死亡时爆炸类型

**英文** `explodeTypeOnDeath` · **类型** `preset effects` · **权威类型** `preset effects` · **可用节** `核心`

options: verysmall, small, normal, large, largeUnit, building, buildingNoShockwaveOrSmoke, verylargeBuilding
添加内置死亡效果关键字，选项：非常小，很小，正常，大，大单位，建筑物，没有冲击波和烟雾的建筑物，非常大建筑

### 死亡时创建效果

**英文** `alsoEmitEffectsOnDeath` · **类型** `effect ref` · **权威类型** `effect ref` · **可用节** `效果`

在生命耗尽的时候创造这些效果。

```ini
死亡时创建效果: CUSTOM:finalPuff
```

### 死亡效果

**英文** `effectOnDeath` · **类型** `effect(s) ref` · **权威类型** `effect(s) ref` · **可用节** `核心`

单位死亡时产生内置或自定义效果。

```ini
死亡效果: shockwave, CUSTOM:pieces*3, CUSTOM:deathSound
```

### 死亡撞击地面效果

**英文** `effectOnDeathGroundCollision` · **类型** `effect(s) ref` · **权威类型** `effect(s) ref` · **可用节** `核心`

类似于effectOnDeath，但当单位接触地面时。对飞行单位有用。

```ini
死亡撞击地面效果: CUSTOM:bigExplosion
```

### 维修目标效果

**英文** `repairEffectAtTarget` · **类型** `effect` · **权威类型** `effect` · **可用节** `图像`

维修目标上产生的效果。

```ini
维修目标效果: CUSTOM:greensparks*2
```

### 维修效果

**英文** `repairEffect` · **类型** `effect ref` · **权威类型** `effect ref` · **可用节** `图像`

自定义维修效果，可以是任何东西。替换构建时的默认效果

```ini
维修效果: CUSTOM:mist*5
```

### 修正命中效果

**英文** `mutatorX_changedExplodeEffect` · **类型** `effect` · **权威类型** `effect` · **可用节** `抛射体`

如果此修正处于有效状态，则更改爆炸效果。

```ini
修正命中效果: CUSTOM:specialBlast
```

### 也创造效果

**英文** `alsoEmitEffects` · **类型** `effect ref` · **权威类型** `effect ref` · **可用节** `效果`

创建时创建更多效果，对于多效果很有用。 注意：创建的效果上的其他“ alsoEmitEffects”将被忽略，禁止套娃。

```ini
也创造效果: CUSTOM:extraSparks*2
```

### 预热效果

**英文** `warmupStartEffect` · **类型** `effect ref` · **权威类型** `effect ref` · **可用节** `炮塔`

开火延迟时产生效果。

```ini
预热效果: CUSTOM:abosrb
```

### 运动效果

**英文** `movementEffect` · **类型** `effect` · **权威类型** `effect` · **可用节** `图像`

自定义运动效果，可以是内置的或者自定义的。

```ini
运动效果: smoke, CUSTOM:fastDust*2, CUSTOM:pop*5
```

---

## 声音引用（8 个）

### 播放声音至单位

**英文** `playSoundAtUnit` · **类型** `sound ref` · **权威类型** `sound ref` · **可用节** `行动`

动作结束时在当前位置播放声音。

```ini
播放声音至单位: engineStart.ogg
```

### 播放声音至全图

**英文** `playSoundGlobally` · **类型** `sound ref` · **权威类型** `sound ref` · **可用节** `行动`

播放全局声音，所有玩家都能听到。

```ini
播放声音至全图: hornWarn.ogg
```

### 播放声音至玩家

**英文** `playSoundToPlayer` · **类型** `sound ref` · **权威类型** `sound ref` · **可用节** `行动`

播放声音，只有自己能听到。

```ini
播放声音至玩家: confirm.wav
```

### 队列播放声音

**英文** `playSoundToPlayerOnQueue` · **类型** `sound ref` · **权威类型** `sound ref` · **可用节** `行动`

动作准备时播放给玩家的声音。

```ini
队列播放声音: eva_building.ogg
```

### 攻击指令音效

**英文** `soundOnAttackOrder` · **类型** `sound(s)` · **权威类型** `sound(s)` · **可用节** `核心`

填音效名称列表。用,隔开。每次下达攻击指令时只播放其中一个。仅支持.ogg和.wav格式，注意如果您mod用于pc端，则不要直接修改后缀名，要转换格式。

```ini
攻击指令音效: tankAttackOrder1.ogg, tankAttackOrder2.ogg
```

### 选择指令音效

**英文** `soundOnNewSelection` · **类型** `sound(s)` · **权威类型** `sound(s)` · **可用节** `核心`

填声音名称列表。用,隔开。每次下达选择指令时只播放其中一个。仅支持.ogg和.wav格式，注意不要直接修改后缀名，要转换格式。

```ini
选择指令音效: tankSelection1.ogg, tankSelection2.ogg
```

### 也播放音效

**英文** `alsoPlaySound` · **类型** `sound ref` · **权威类型** `sound ref` · **可用节** `效果`

播放音效，填文件名或内置。

```ini
也播放音效: meow.wav:0.5
```

### 移动指令音效

**英文** `soundOnMoveOrder` · **类型** `sound(s)` · **权威类型** `sound(s)` · **可用节** `核心`

填声音名称列表。用,隔开。每次下达移动指令时只播放其中一个。仅支持.ogg和.wav格式，注意不要直接修改后缀名，要转换格式。

```ini
移动指令音效: tankMoveOrder1.ogg, tankMoveOrder2.ogg
```

---

## 抛射体引用（7 个）

### 关联抛射体

**英文** `altProjectile` · **类型** `projectile ref` · **权威类型** `projectile ref` · **可用节** `炮塔`

当altProjectileCondition为true时，从此炮塔发射的抛射体。

```ini
关联抛射体: spear
```

### 抛射体

**英文** `projectile` · **类型** `projectile ref` · **权威类型** `projectile ref` · **可用节** `炮塔`

[projectile_NAME]

```ini
抛射体: torpedo
```

### 在爆炸时刷抛射体

**英文** `spawnProjectilesOnExplode` · **类型** `projectile ref` · **权威类型** `projectile ref` · **可用节** `抛射体`

填抛射体名。只在爆炸时生成抛射体，如果耗光life则不会生成。

```ini
在爆炸时刷抛射体: flachette*4
```

### 在创建时刷抛射体

**英文** `spawnProjectilesOnCreate` · **可用节** `抛射体`

填抛射体名。抛射体创建时生成抛射体。

```ini
在创建时刷抛射体: shotgun
```

### 在寿命结束时刷抛射体

**英文** `spawnProjectilesOnEndOfLife` · **类型** `projectile ref` · **权威类型** `projectile ref` · **可用节** `抛射体`

填抛射体名。在寿命(life)结束时生成抛射体。生成抛射体逻辑具体参数参考后面生成抛射体大项。这简化了过去许多需要多炮塔(如贯穿攻击)或是生成辅助单位才能实现的操作。

```ini
在寿命结束时刷抛射体: torpedo_split(偏移角度=90), torpedo_split(偏移角度=-90)
```

### 指定攻击地面抛射体

**英文** `fireTurretXAtGround_withProjectile` · **类型** `projectile ref` · **权威类型** `projectile ref` · **可用节** `行动`

设定使用的抛射体，如果不设置则为炮塔默认的抛射体。

```ini
指定攻击地面抛射体: nuke
```

### spawnUnit

**类型** `projectile ref` · **权威类型** `projectile ref` · **可用节** `抛射体`

在抛射体丸爆炸位置产生这种单位

```ini
eg: spawnUnit: heavyTank, tank*5, hoverTank(偏移量x=10)
```

---

## 炮塔引用（7 个）

### 复制与

**英文** `copyFrom` · **可按节区分类型** · **权威类型** `turret ref` · **可用节** `核心` `腿` `炮塔`

- **核心**：`file(s) (ini)`
- **腿**：`int`
- **炮塔**：`turret ref`

从另一条腿复制。仅需设置一次腿部值时有用

```ini
复制与: ROOT:defaultTanks.template, tankT1.ini
```

### 建造时转向指定炮塔

**英文** `whenBuilding_rotateTo_rotateTurretX` · **类型** `turret ref` · **权威类型** `turret ref` · **可用节** `行动`

准备此操作时转向指定炮塔。

```ini
建造时转向指定炮塔: cannon
```

### 链接到

**英文** `attachedTo` · **类型** `turret ref` · **权威类型** `turret ref` · **可用节** `炮塔`

要连接的另一个炮塔的ID，将相对于它定位，并随其旋转。

```ini
链接到: base
```

### 链接间隔

**英文** `linkDelayWithTurret` · **类型** `turret ref` · **权威类型** `turret ref` · **可用节** `炮塔`

当另一炮塔开火时，该炮塔上的冷却延迟将被重置/删除

```ini
链接间隔: 1
```

### 死亡武器

**英文** `fireTurretXAtSelfOnDeath` · **类型** `turret ref` · **权威类型** `turret ref` · **可用节** `核心`

死亡武器。单位死后，使用指定炮塔攻击自身所在位置。

```ini
死亡武器: turret_1
```

### 指定攻击地面炮塔

**英文** `fireTurretXAtGround` · **类型** `turret ref` · **权威类型** `turret ref` · **可用节** `行动`

使用此炮塔攻击玩家所指定的地面。

```ini
指定攻击地面炮塔: nukeSilo
```

### 指定炮塔为中心位置

**英文** `basePositionFromTurret` · **类型** `turret` · **权威类型** `turret` · **可用节** `贴花`

Use a turret position instead of unit body as base（使用炮塔位置而不是单元主体作为底座）

```ini
指定炮塔为中心位置: rocketLauncherBase
```

---

## 动画引用（8 个）

### 播放动画

**英文** `playAnimation` · **类型** `animation id` · **权威类型** `animation id` · **可用节** `行动`

播放[animation_xx]类型动画。

```ini
播放动画: engageThrusters
```

### 建造时播放动画

**英文** `whenBuilding_playAnimation` · **类型** `animation ref` · **权威类型** `animation ref` · **可用节** `行动`

准备此动作时，播放[animation]中自定义的动画。

```ini
建造时播放动画: firePrepare
```

### 开火播放动画

**英文** `onShoot_playAnimation` · **类型** `animation ref` · **权威类型** `animation ref` · **可用节** `炮塔`

开火后播放[Animation]中的自定义动画

```ini
开火播放动画: heatUp
```

### arm#_[time]

**可用节** `动画`

添加一个关键帧。用来创建多个动作的动画。

```ini
arm1_5s: {x: 5, dir: 90 }
```

### basePositionFromLegEnd

**类型** `leg/arm` · **权威类型** `leg/arm` · **可用节** `贴花`

填腿脚/胳膊节

```ini
basePositionFromLegEnd: leg_3, basePositionFromLegEnd: arm_5
```

### body_[time]

**可用节** `动画`

随时为主体添加关键帧。身体仅允许使用frame和scale

```ini
body_4s: {帧: 4, scale: 0.5}
```

### effect_[time]

**可用节** `动画`

在播放动画时产生效果

```ini
effect_2s: {name:CUSTOM|myExplode,  x: 0,y: 5}
```

### leg#_[time]

**可用节** `动画`

添加一个关键帧。用来创建多个动作的动画。

```ini
leg1_3s: {dir: 300}
```

---

## 附属引用（1 个）

### 附属添加于槽位

**英文** `attachments_onlyOnSlots` · **类型** `attachment ids` · **权威类型** `attachment ids` · **可用节** `行动`

只将附属物添加于特定的槽位

```ini
附属添加于槽位: drop1, drop2, drop3
```

---

## 贴花引用（1 个）

### fireTurretXAtGround_showGuideDecals

**类型** `decal refs` · **权威类型** `decal refs` · **可用节** `行动`

当选择发射位置时显示贴花(参考核抛射体选择发射地点时长按显示)

```ini
fireTurretXAtGround_showGuideDecals: strikeZone300
```

---

## 图像（23 个）

### 背景图像

**英文** `image_back` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `图像`

可选的在单位后面绘制图像，不随单位转动。可用于工厂底图，单位角标等。

```ini
背景图像: floor.png
```

### 波束开始图像

**英文** `beamImageStart` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `抛射体`

光束动画开火端效果图像

```ini
波束开始图像: beamStart.png
```

### 波束命中图像

**英文** `beamImageEnd` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `抛射体`

光束动画命中端效果图像

```ini
波束命中图像: beamEnd.png
```

### 波束图像

**英文** `beamImage` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `抛射体`

用于激光效果的主体图像，图像会被复制为多份显示。例如可用于做红警中的正弦波状辐射武器。因为本质是图像拼接,所以长度限制不能小于20

```ini
波束图像: beam.png
```

### 额外图标图像

**英文** `iconExtraImage` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `行动`

使用图像作为此动作图标。和上一个一致,但显示更靠上。绘制在图标图像上方。对升级图标等有用

```ini
额外图标图像: fireShotNo.png
```

### 护盾图像

**英文** `image_shield` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `图像`

自定义护盾所使用的图像。

```ini
护盾图像: forcefield.png
```

### 脚图像

**英文** `image_foot` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `腿`

脚图像，与image_end相同

```ini
脚图像: footR.png
```

### 脚图像阴影

**英文** `image_foot_shadow` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `腿`

脚图像阴影。

```ini
脚图像阴影: AUTO
```

### 列表图像列表

**英文** `imageStack` · **类型** `images` · **权威类型** `images` · **可用节** `贴花`

list of images recommended for 3d voxel style units as images can be batch drawn when using the same sprite sheet.（推荐用于三维立体体积元素样式单位的图像列表，因为当使用相同的子画面时，可以批量绘制图像。）(目前没有实例所以不清楚,但确定是用于3d单位)

```ini
列表图像列表 - carFloor.png, carBody.png*3, carRoof.png*2, antenna.png*10
```

### 末端图像

**英文** `image_end` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `腿`

末端图像，相当于脚。

```ini
末端图像: rotor.png
```

### 末端图像阴影

**英文** `image_end_shadow` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `腿`

末端图像阴影。

```ini
末端图像阴影: AUTO
```

### 炮塔图像

**英文** `image_turret` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `图像`

所有炮塔的默认图像，也可以为每个炮塔设置专用图像。

```ini
炮塔图像: gun.png
```

### 死亡图像

**英文** `image_wreak` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `图像`

可选的单位死亡时使用的图像。

```ini
死亡图像: ded.png
```

### 缩放图标

**英文** `icon_zoomed_out` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `图像`

缩放时显示的图标

```ini
缩放图标: unitIcon.png
```

### 图标

**英文** `iconImage` · **可按节区分类型** · **权威类型** `String` · **可用节** `行动` `全局资源` `资源`

- **行动**：`file (image)`
- **全局资源**：`file (image)`
- **资源**：`String`

自定义该资源的象征图像

```ini
图标: fireShot.png
```

### 图像集

**英文** `stripIndex` · **类型** `int/string` · **权威类型** `int/string` · **可用节** `效果`

要使用的内置图像集。不能与自定义图像一起使用。如effects 效果1对应res内文件effects.png，以此类推。
explode_big爆炸效果图、light_50闪光贴花、flame开火图火焰、dust灰尘贴花、smoke_black黑烟、shockwave冲击波、fire火焰图、lava_bubble岩浆泡、effects2效果2、plasma_shot等离子、shockwave_large大冲击波、explode_bits碎片、explode_big2大爆炸、explode_bits_bug虫碎片、projectiles抛射体、projectiles2抛射体2、effects3效果3

```ini
图像集: projectiles
```

### 腿图像

**英文** `image_leg` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `腿`

腿图像，与image_middle相同

```ini
腿图像: legR.png
```

### 中部图像

**英文** `image_middle` · **类型** `file (image)` · **权威类型** `file (image)` · **可用节** `腿`

中部图像，相当于腿。

```ini
中部图像: 无
```

### chargeEffectImage

**类型** `file (image)` · **权威类型** `file (image)` · **可用节** `炮塔`

预热时的充能效果。默认是由小变大。

```ini
chargeEffectImage: glow.png
```

### icon_build

**类型** `file (image)` · **权威类型** `file (image)` · **可用节** `图像`

填文件名,指定图像作为单位在列表中显示的图像.

```ini
icon_build: iconFish.png
```

### image

**可按节区分类型** · **权威类型** `file (image)` · **可用节** `贴花` `效果` `图像` `抛射体` `炮塔`

- **贴花**：`image`
- **效果**：`image`
- **图像**：`file (image)`
- **抛射体**：`file (image)`
- **炮塔**：`file (image)`

图像

```ini
image: fish.png
```

### image_shadow

**可按节区分类型** · **权威类型** `file (image)` · **可用节** `贴花` `图像`

- **贴花**：`image`
- **图像**：`file (image)`

阴影图像

```ini
image_shadow: 阴影.png
```

### imageShadow

**类型** `image` · **权威类型** `image` · **可用节** `效果`

用于自定义阴影的图像

```ini
imageShadow: AUTO
```

---

## 颜色（5 个）

### 额外图标颜色

**英文** `iconExtraColor` · **类型** `colour` · **权威类型** `colour` · **可用节** `行动`

将图标叠加上额外的颜色。默认为#64FFFFFF

```ini
额外图标颜色: #ff0000
```

### 开火闪光

**英文** `shoot_light` · **类型** `color` · **权威类型** `color` · **可用节** `炮塔`

开火时闪光，16进制argb格式。#AARRGGBB,透明度，红，绿，蓝

```ini
开火闪光: #fafafa
```

### 闪光颜色

**英文** `lightColor` · **类型** `color` · **权威类型** `color` · **可用节** `抛射体`

抛射体光照颜色,不决定激光颜色，激光由color决定。

```ini
闪光颜色: #ffe92b
```

### 显示颜色

**英文** `displayColor` · **类型** `color` · **权威类型** `color` · **可用节** `全局资源`

颜色，可以是十六进制，带有可选的alpha

```ini
显示颜色: #FF0000
```

### 颜色

**英文** `color` · **可按节区分类型** · **权威类型** `color` · **可用节** `贴花` `效果` `抛射体`

- **贴花**：`color hex`
- **效果**：`color`
- **抛射体**：`color`

affects image and line.（影响图像和线条。）

```ini
颜色: #ffff00
```

---

## 资源（14 个）

### 根据AI难度添加资源

**英文** `addResourcesScaledByAIHandicaps` · **类型** `resources` · **权威类型** `resources` · **可用节** `行动`

与addResources类似，但根据AI难度级别增加或减少。比例：最简单40%、简单70%、中等100%、困难140%、很困难180%、不可能370%

```ini
根据AI难度添加资源: 资金=5, hp=-100
```

### 价格

**英文** `price` · **可按节区分类型** · **权威类型** `int / price` · **可用节** `行动` `可建造` `核心`

- **行动**：`resources`
- **可建造**：`price(s)`
- **核心**：`int / price`

此行为需要的价格，可以是多种资源或自定义资源。如果不满足则行为不可用。如果没设定资源类型则默认为资金。如果价格为0，AI可能很少甚至不会使用。

```ini
价格: 资金=5, 能量=5, hp=100, 护盾=5, ammo=1
```

### 修正区域添加资源

**英文** `mutatorX_addResourcesAreaHit` · **类型** `resource` · **权威类型** `resource` · **可用节** `抛射体`

伤害修正,用于范围伤害(areaDamage)，给目标添加指定资源。

```ini
修正区域添加资源: rust=4
```

### 修正直接添加资源

**英文** `mutatorX_addResourcesDirectHit` · **类型** `resource` · **权威类型** `resource` · **可用节** `抛射体`

伤害修正,用于直接伤害(directDamage)，给目标添加指定资源。用途如给目标资源后，目标检测自身资源然后禁止移动。

```ini
修正直接添加资源: oil=1
```

### 用逻辑设置资源

**英文** `setResourcesWithLogic` · **类型** `dynamic resources` · **权威类型** `dynamic resources` · **可用节** `行动`

将目标资源设置为此值，而不是添加。小心对全局资源使用。

```ini
用逻辑设置资源: hp=self.父单位.hp - 10, 能量 = 自身能量 / 2
```

### 用逻辑添加资源

**英文** `addResourcesWithLogic` · **类型** `dynamic resources` · **权威类型** `dynamic resources` · **可用节** `行动`

与addResources类似，但允许对资源值使用逻辑

```ini
用逻辑添加资源: hp = 选择( self.父单位.能量>5, 10, 20 )
```

### 增加能量

**英文** `addEnergy` · **类型** `addEnergy` · **权威类型** `addEnergy` · **可用节** `行动`

为单位增加能量。需要设置了energyMax，否则不起作用。（与addResources相同：energy = X）

```ini
增加能量: 10
```

### 增加资源

**英文** `addResources` · **可按节区分类型** · **权威类型** `price(s)` · **可用节** `行动` `可建造`

- **行动**：`resources`
- **可建造**：`price(s)`

给生成单位这些资源，可用于设置触发动作的标志

```ini
增加资源: 资金=5, 能量=-5, hp=-100, 护盾=5, ammo=1
```

### 转换资源来源

**英文** `convertResource_from` · **类型** `customResource` · **权威类型** `customResource` · **可用节** `行动`

将此资源转换资源为别的资源。

```ini
转换资源来源: juice
```

### 转换资源至

**英文** `convertResource_to` · **类型** `customResource` · **权威类型** `customResource` · **可用节** `行动`

将资源转后为此资源，要提供的自定义资源的名称

```ini
转换资源至: sauce
```

### 资源类型

**英文** `resourceAmount` · **类型** `customResource` · **权威类型** `customResource` · **可用节** `行动`

自定义资源的名称，使用以下3个键进行设置。所有键都是可选的，可以一起使用。

```ini
资源类型: oil
```

### 资源类型添加至

**英文** `resourceAmount_addOtherResource` · **类型** `customResource` · **权威类型** `customResource` · **可用节** `行动`

添加到其中的另一个自定义资源的名称。可以不使用resourceAmount_setValue，只是添加资源。或者使用resourceAmount_setValue:0复制一个资源值。

```ini
资源类型添加至: juice
```

### appendResourceInHUD

**可按节区分类型** · **权威类型** `ResourceRef` · **可用节** `全局资源` `资源`

- **全局资源**：`resource ref`
- **资源**：`ResourceRef`

使用后，在游戏界面中这个资源值将会叠在目标资源值的后面，并且继承目标资源的颜色和图标。这个资源值后面也能继续叠加其它资源值

```ini
appendResourceInHUD: <resources:name>
```

### displayTextAppendResource

**可按节区分类型** · **权威类型** `ResourceRef` · **可用节** `全局资源` `资源`

- **全局资源**：`resource ref`
- **资源**：`ResourceRef`

在显示的UI中添加该资源

```ini
displayTextAppendResource: <资源:name>
```

---

## 时间（12 个）

### 淡出

**英文** `fadeOut` · **类型** `bool` · **权威类型** `time` · **可用节** `效果`

设置淡入效果时间，透明度从100%淡出到%0。把alpha设置为高于1可以延长淡出。

```ini
淡出: 4s
```

### 动画帧速度

**英文** `animateFrameSpeed` · **类型** `time` · **权威类型** `time` · **可用节** `效果`

动画播放速度，单位位游戏帧，一般动画帧数也不高，所以设置一般为0.x

```ini
动画帧速度: 10
```

### 动画帧随机速度

**英文** `animateFrameSpeedRandom` · **类型** `time` · **权威类型** `time` · **可用节** `效果`

按此值随机更改动画速度。

```ini
动画帧随机速度: 20
```

### 建造速度

**英文** `buildSpeed` · **可按节区分类型** · **权威类型** `float / s` · **可用节** `行动` `核心`

- **行动**：`time`
- **核心**：`float / s`

建造此动作所需要的时间，这个读条过程下面称之为准备以区分建造，机翻或是多个动作称之为队列。完成时的效果称为结果。

```ini
建造速度: 5s
```

### 切换融合

**英文** `blendOut` · **类型** `time` · **权威类型** `time` · **可用节** `动画`

与下一个动画融合时间。

```ini
切换融合: 3s
```

### 融入时间

**英文** `blendIn` · **类型** `time` · **权威类型** `time` · **可用节** `动画`

与上一个动画融合时间。

```ini
融入时间: 2s
```

### 添加路径点检索时间

**英文** `addWaypoint_maxTime` · **类型** `time` · **权威类型** `time` · **可用节** `行动`

如果此路径点这么久还无法执行完成，则取消。

```ini
添加路径点检索时间: 20s
```

### 移动延迟

**英文** `moveWarmUp` · **可用节** `腿`

腿脚移动延迟

```ini
移动延迟: 2
```

### 增加动作冷却时间

**英文** `addActionCooldownTime` · **类型** `time` · **权威类型** `time` · **可用节** `行动`

增加动作冷却时间，制作先充能再使用的action更加容易了。

```ini
增加动作冷却时间: 20s
```

### 增加所有动作冷却时间

**英文** `addAllActionCooldownsTime` · **类型** `time` · **权威类型** `time` · **可用节** `行动`

增加所有action的冷却时间。

```ini
增加所有动作冷却时间: 4s
```

### 重新瞄准在飞行时搜索延迟

**英文** `retargetingInFlightSearchDelay` · **类型** `float/time` · **权威类型** `float/time` · **可用节** `抛射体`

寻找新目标之间的时间。默认5

```ini
重新瞄准在飞行时搜索延迟: 1s
```

### 自动触发间隔

**英文** `autoTriggerCooldownTime` · **类型** `time (seconds)` · **权威类型** `time (seconds)` · **可用节** `核心`

设置自动触发动作间隔。默认为1s。警告：对于许多设备，将此值设置得太低可能会影响性能，具体取决于操作效果。

```ini
自动触发间隔: 0.05s
```

---

## 关系（5 个）

### 查看者为特定队伍时显示

**英文** `onlyTeam` · **类型** `TeamRelation` · **权威类型** `TeamRelation` · **可用节** `贴花`

Only draw when this relation between unit and viewing player，Can be filled in:own|notOwn|neutral|allyNotOwn|ally|enemy|any         (仅当单位和查看玩家之间的关系时绘制，可以填写：己方|非己方|中立方|除自己外盟友|盟友|敌方|任意)

```ini
查看者为特定队伍时显示: 所有
```

### 检索队伍

**英文** `searchTeam` · **类型** `relation` · **权威类型** `relation` · **可用节** `放置规则`

要搜索的团队可以是：自身|中立|盟友非自身|盟友|敌人|任何。  own|neutral|allyNotOwn|ally|enemy|any

```ini
检索队伍: 己方
```

### 提取资源队伍

**英文** `takeResources_includeUnitsWithinRange_team` · **类型** `TeamRelation` · **权威类型** `TeamRelation` · **可用节** `行动`

提取在此范围内队伍的资源。与“includeUnitsWithinRange”一起使用，默认为own。可以是：
己方|除自己外任何|中立|除自己外盟友|盟友|敌人|任何，own|notOwn|neutral|allyNotOwn|ally|enemy|any

```ini
提取资源队伍: 己方
```

### 添加路径点靠近队伍

**英文** `addWaypoint_target_nearestUnit_team` · **类型** `relation` · **权威类型** `relation` · **可用节** `行动`

添加的路径点目标需要的所属方：己方|除自己外任何|中立|除自己外盟友|盟友|敌人|任何，own|notOwn|neutral|allyNotOwn|ally|enemy|any

```ini
添加路径点靠近队伍: 盟友
```

### 添加指定队伍的随机单位

**英文** `addWaypoint_target_randomUnit_team` · **类型** `relation` · **权威类型** `relation` · **可用节** `行动`

添加路径点单位为指定队伍的随机单位

```ini
添加指定队伍的随机单位: 所有
```

---

## 内存（5 个）

### @memory

**可用节** `核心`

data.sections.core.@memory

```ini
@内存 fish:unit
```

### 定义单位内存

**英文** `defineUnitMemory` · **可用节** `核心`

为每个单元创建唯一的自定义存储变量。允许的类型:boolean, float/number, unit, string
格式:defineUnitMemory:类型 变量名称
"defineUnitMemory: boolean nukeActive,boolean laserReady, float experience, unit nextTarget, unit homeBase, string customText
"

```ini
定义单位内存: boolean nukeActive, boolean laserReady, float experience, unit nextTarget, unit homeBase, string customText
```

### defineUnitMemory: boolean nukeActive, boolean laserReady, float experience, unit nextTarget, unit homeBase, string customText

**类型** `defineUnitMemory` · **权威类型** `defineUnitMemory` · **可用节** `核心`

data.sections.core.defineUnitMemory

```ini
定义单位内存: boolean nukeActive, boolean laserReady, float experience, unit nextTarget, unit homeBase, string customText
```

### defineUnitMemory: unit[] squad, float[] mango

**类型** `defineUnitMemory` · **权威类型** `defineUnitMemory` · **可用节** `核心`

data.sections.core.defineUnitMemory

```ini
定义单位内存: unit[] squad, float[] mango
```

### updateUnitMemory

**类型** `memory` · **权威类型** `memory` · **可用节** `核心`

(类似[action]setUnitMemory但是性能更好)works like [action]setUnitMemory but with better performance and easier timing than triggering an action

```ini
updateUnitMemory: timeCount = (1 + 内存.timeCount)
```

---

## 枚举（9 个）

### 被运输单位类型限制

**英文** `transportUnitsRequireMovementType` · **类型** `movementTypes` · **权威类型** `movementTypes` · **可用节** `核心`

仅允许运输具有这些移动类型之一的单位。默认陆地。比如可以设定某单位可以运输空军以实现停机坪，只运输海军等。

```ini
被运输单位类型限制: 空中, 水面
```

### 动画条件

**英文** `onActions` · **类型** `enum` · **权威类型** `enum` · **可用节** `动画`

动画触发条件：移动、攻击、闲置、在建造中、建造中并将动画拉伸至建造时长、生产中、未知、修复、回收、创建时
move, attack, idle, underConstruction, underConstructionWithLinkedBuiltTime, queuedUnits、Unknown, repair, reclaim, created

```ini
动画条件: 移动
```

### 绘制图层

**英文** `layer` · **类型** `enum` · **权威类型** `enum` · **可用节** `贴花`

可以填入的值：shadow(阴影), beforeBody(在主体之下), afterBody(在主体之上), onTop(在最顶端), beforeUI(在显示界面之上)，inactive(无效)

```ini
绘制图层: beforeUI
```

### 添加路径点动作类型

**英文** `addWaypoint_type` · **类型** `enum` · **权威类型** `enum` · **可用节** `行动`

添加的路径点类型。值列表：移动，移动攻击，防守，进入载具，主动装载，攻击，回收，维修，靠近目标，建造，跟随，设置被动目标，卸载至，巡逻，防守至，触发行动，在范围内时触发行动
move, attackMove, guard, loadInto, loadUp, attack, reclaim, repair, touchTarget, build, follow，setPassiveTarget，unloadAt，patrol，guardAt，triggerAction，triggerActionWhenInRange

```ini
添加路径点动作类型: 移动
```

### 阵营色模式

**英文** `teamColoringMode` · **可用节** `图像`

设置单位着色逻辑，默认是纯绿。有四个值disabled,hueShift,hueAdd,pureGreen。(禁用，色相偏移，色相添加，纯绿)

```ini
阵营色模式: disabled
```

### 指定攻击地面类型

**英文** `fireTurretXAtGround_onlyOverPassableTileOf` · **可用节** `行动`

设置手动选择的地面需要满足这种运动方式。列表：无，陆地，建筑，空军，水。两栖，跨悬崖，跨悬崖和度水。
NONE LAND BUILDING AIR WATER HOVER OVER_CLIFF OVER_CLIFF_WATER

```ini
指定攻击地面类型: 两栖
```

### 资源数字分组

**英文** `displayDigitGrouping` · **可按节区分类型** · **权威类型** `String` · **可用节** `全局资源` `资源`

- **全局资源**：`display type`
- **资源**：`String`

none/comma/space--(空,逗号,空格)(例如资源数量10000,空格分组就是10 000)

```ini
资源数字分组: none
```

### 自动触发检查率

**英文** `autoTriggerCheckRate` · **类型** `enum` · **权威类型** `enum` · **可用节** `行动` `核心`

选项:everyFrame(默认)，every4Frames, every8Frames。
注意:所有触发器无论检查率都是在第一次创建和自动触发冷却之后检查的。
注:使用模板添加[core]autoTriggerCheckRate:every8Frames到所有单位。可以使使用了复杂autoTriggers的mod有巨大的性能提升。

```ini
自动触发检查率:每8帧
```

### 作图类型

**英文** `drawType` · **可按节区分类型** · **权威类型** `int` · **可用节** `效果` `抛射体`

- **效果**：`normal\|displacement`
- **抛射体**：`int`

效果的绘制类型

```ini
作图类型: normal
```

---

## 坐标（5 个）

### 添加路径点随机偏移

**英文** `addWaypoint_position_randomOffsetFromSelf` · **类型** `point` · **权威类型** `point` · **可用节** `行动`

添加坐标进路径点时的随机值。

```ini
添加路径点随机偏移:
```

### 添加路径点相对偏移

**英文** `addWaypoint_position_relativeOffsetFromSelf` · **类型** `point` · **权威类型** `point` · **可用节** `行动`

添加路径点位置相对自身偏移量。

```ini
添加路径点相对偏移: 10, 20
```

### 添加路径点坐标偏移

**英文** `addWaypoint_position_offsetFromSelf` · **类型** `point` · **权威类型** `point` · **可用节** `行动`

添加的路径点相对于自身偏移的坐标。

```ini
添加路径点坐标偏移:
```

### 指定攻击地面坐标

**英文** `fireTurretXAtGround_withOffset` · **类型** `point` · **权威类型** `point` · **可用节** `行动`

攻击指定坐标所在地面，不需要手动选择，

```ini
指定攻击地面坐标: 0,0
```

### 自身位置偏移

**英文** `offsetSelfAbsolute` · **类型** `point3d` · **权威类型** `point3d` · **可用节** `行动`

使用时将修改单位当前位置，以单位为中心的绝对坐标。

```ini
自身位置偏移: 0, 0, 40
```

---

## 列表（1 个）

### 显示类型

**英文** `displayType` · **类型** `list` · **权威类型** `list` · **可用节** `行动`

行动的显示类型正常模式为无(绿色)、集结(白色R)、升级(蓝色U)、单位队列（绿色）、建筑(绿色)、行为(蓝色)、仅信息(灰色)、无框信息、仅信息库存（灰色）
none, rally, upgrade, queueUnit, building, action, infoOnly, infoOnlyNoBox,infoOnlyStockpile

```ini
显示类型: upgrade
```

---

## 键值对（2 个）

### 带数据发送消息

**英文** `sendMessageWithData` · **类型** `key-value` · **权威类型** `key-value` · **可用节** `行动`

填变量=XXX

```ini
带数据发送消息: fish="nice!", cat=self.活动路径点目标, amount=4, xyz=内存.something
```

### 设置单位内存

**英文** `setUnitMemory` · **类型** `key value pairs` · **权威类型** `key value pairs` · **可用节** `行动`

键值对。改变此单元的内存，值可以用逻辑设定。内存必须首先用defineUnitMemory或@memory定义。
注：
设置内存时 需要加上数组下标
eg. setUnitMemory: numArray[0]=2
数组下标从0开始

```ini
设置单位内存: """"
  customText=内存.customText+'hello',
  nukeActive=真, 
  nextTarget=self.攻击中.接近单位(范围内=300, 需标签='x', 关系='敌方')
""""
```

---

## 字段集（3 个）

### 设置单位状态

**英文** `setUnitStats` · **类型** `fields values` · **权威类型** `fields values` · **可用节** `行动`

字段值。允许在不转换的情况下动态更改选定的字段数量。支持=/+=/-=，动态数学/逻辑。可以改变的领域:maxHp(血量上限)，hp(血量)，selfRegenRate(自身回血速度)，maxShield(护盾上限)，shield(护盾)，shieldRegen(护盾恢复速度)，maxEnergy(能量上限)，energy(能量)，armour(装甲)，mass(质量)，shootDelayMultiplier(开火间隔倍数)，shootDamageMultiplier(开火伤害倍数)，movespeed(最大移动速度)，maxTurnSpeed(最大转弯速度)，maxAttackRange(最大攻击距离)，nanoRange(修复范围)，fogOfWarSightRange(迷雾视野)，nanoFactorySpeed(工厂制造速度倍数)，targetHeight(单位最终高度)，

```ini
设置单位状态: 护盾值 += 20, 移动速度 += 0.1
```

### 转换时保留字段

**英文** `convertTo_keepCurrentFields` · **类型** `fields` · **权威类型** `fields` · **可用节** `行动`

转换时不要更改这些字段，这在setUnitStats中很有用

```ini
转换时保留字段: 装甲, maxEnergy, 生命值
```

### whenBuilding_temporarilyConvertTo_keepFields

**类型** `fields` · **权威类型** `fields` · **可用节** `行动`

不要在使用whenbuilding_temporaryconvertto时更改这些字段(无论是从或到)，这对setUnitStats很有用

```ini
whenBuilding_temporarilyConvertTo_keepFields: 生命值, maxEnergy, 移动速度
```

---

## 行动引用（11 个）

### 建造时触发动作

**英文** `whenBuilding_triggerAction` · **类型** `action ref` · **权威类型** `action ref` · **可用节** `行动`

准备时触发另一个动作

```ini
建造时触发动作: spawnMinions
```

### 开火触发行为

**英文** `onShoot_triggerActions` · **类型** `action refs` · **权威类型** `action refs` · **可用节** `炮塔`

每次这个炮塔开火时触发这些动作

```ini
开火触发行为: spawnShells
```

### 提取资源成功触发

**英文** `takeResources_triggerActionIfAnyCollected` · **类型** `action refs` · **权威类型** `action refs` · **可用节** `行动`

如果有任何收集，则触发动作。

```ini
提取资源成功触发: purify
```

### 提取资源触发行为

**英文** `takeResources_triggerActionForEach` · **类型** `action refs` · **权威类型** `action refs` · **可用节** `行动`

引用动作(找到带有资源的目标单位并提取时)

```ini
提取资源触发行为: manufacture
```

### 提取资源失败触发

**英文** `takeResources_triggerActionIfNoneCollected` · **类型** `action refs` · **权威类型** `action refs` · **可用节** `行动`

如果没有收集，则触发操作。

```ini
提取资源失败触发: callOtherTanker
```

### 添加路径点动作坐标

**英文** `addWaypoint_position_fromAction` · **类型** `bool` · **权威类型** `action ref` · **可用节** `行动`

将当前动作指定的坐标添加进路径序列中。

```ini
添加路径点动作坐标: fireShot
```

### 添加路径点匹配触发

**英文** `addWaypoint_triggerActionIfMatched` · **类型** `actions` · **权威类型** `actions` · **可用节** `行动`

如果操作添加的路径点是有效的，则执行指定操作。

```ini
添加路径点匹配触发: VIP_target
```

### 添加路径点失败触发

**英文** `addWaypoint_triggerActionIfFailed` · **类型** `actions` · **权威类型** `actions` · **可用节** `行动`

如果target_nearestUnit找不到匹配项，因此无法添加路径点，则触发此操作

```ini
添加路径点失败触发: retreat
```

### 添加指定动作冷却时间

**英文** `addActionCooldownApplyToActions` · **类型** `action ids` · **权威类型** `action ids` · **可用节** `行动`

action id，设置addActionCooldownTime的目标。默认情况下是当前操作本身。

```ini
添加指定动作冷却时间: retreat, transformFins
```

### 也添加进队列

**英文** `alsoQueueAction` · **类型** `action refs` · **权威类型** `action refs` · **可用节** `行动`

将另一个动作添加到序列中。忽略行动的要求

```ini
也添加进队列: spawnMinions
```

### 也执行动作

**英文** `alsoTriggerAction` · **类型** `action refs` · **权威类型** `action refs` · **可用节** `行动`

触发其他动作的结果。忽略行动的要求。

```ini
也执行动作: addCredits, playSound
```

---

## 事件触发

### 自动触发事件

**英文** `autoTriggerOnEvent` · **类型** `event` · **可用节** 行动

满足此条件则自动触发。参数表：创建，完成且激活，销毁，杀死任何单位，队列中单位完成，队列添加项目，队列项目取消，传送，接触目标成功，玩家指定路径，队伍变更，运输新单位，卸载或移除单位,受到伤害,进入载具,离开载具,新消息,移除附属。

### 可用事件（autoTriggerOnEvent 参数）

| 中文                                  | 英文                                      | 标签参数                  |
| ------------------------------------- | ----------------------------------------- | ------------------------- |
| 创建                                  | `created`                               | —                        |
| 完成且激活                            | `completeAndActive`                     | —                        |
| 销毁                                  | `destroyed`                             | —                        |
| 杀死任何单位                          | `killedAnyUnit`                         | —                        |
| 队列中单位完成                        | `queuedUnitFinished`                    | —                        |
| queueItemAdded(withActionTag="#")     | `queueItemAdded(withActionTag="#")`     | 行动标签（withActionTag） |
| queueItemCancelled(withActionTag="#") | `queueItemCancelled(withActionTag="#")` | 行动标签（withActionTag） |
| 传送                                  | `teleported`                            | —                        |
| 接触目标成功                          | `touchTargetSuccess`                    | —                        |
| 玩家指定新路径                        | `newWaypointGivenByPlayer`              | —                        |
| 队伍变更                              | `teamChanged`                           | —                        |
| 运输新单位                            | `transportingNewUnit`                   | —                        |
| 卸载或移除单位                        | `transportUnloadedOrRemovedUnit`        | —                        |
| tookDamage(withTag="#")               | `tookDamage(withTag="#")`               | 标签（withTag）           |
| newMessage(withTag="#")               | `newMessage(withTag="#")`               | 标签（withTag）           |
| 进入载具                              | `enteredTransport`                      | —                        |
| 离开载具                              | `leftTransport`                         | —                        |
| 移除附属                              | `attachmentRemoved`                     | —                        |

---

# 三、特定值（枚举选项）

## 添加路径点靠近队伍

- **己方**（`own`）
- **中立**（`neutral`）
- **除自己外盟友**（`allyNotOwn`）
- **盟友**（`ally`）
- **敌方**（`enemy`）
- **所有**（`any`）
- **notOwn**

## 添加路径点动作类型

- **移动**（`move`）
- **移动攻击**（`attackMove`）
- **保护**（`guard`）
- **loadInto**
- **loadUp**
- **攻击**（`attack`）：[attack]
- **reclaim**：回收建筑并返还75%资金
- **repair**：主动维修建筑
- **靠近目标**（`touchTarget`）
- **建立**（`build`）
- **跟随**（`follow`）
- **setPassiveTarget**

## 攻击移动类型

- **normal**
- **bomber**

## 自动触发检查率

- **everyFrame**
- **每4帧**（`every4Frames`）
- **每8帧**（`every8Frames`）

## bool

- **真**（`true`）：满足/是/对/为真/1，不建议使用1或0，不是所有代码都支持。
- **假**（`false`）：不满足/非/错/假/0

## 转换时保留字段

- **生命值**（`maxHp`）：单位最大生命值，默认生成时即为此值。
- **hp**：单位血量，一般血量归零时就会销毁单位。
- **护盾值**（`maxShield`）：单位最大护盾值，默认生成时即为此值。如果设置了startShieldAtZero:true，则初始为0.
- **护盾**（`shield`）：单位护盾，一般用于抵挡敌人攻击。
- **护盾恢复速度**（`shieldRegen`）：此数值决定每帧增加护盾值，游戏内一秒为60帧，所以不要写太大。可以写负值。
- **maxEnergy**
- **能量**（`energy`）：单位使用的能量，如可用于开火限制。
- **装甲**（`armour`）：抵消敌方攻击所造成的伤害。
- **质量**（`mass`）：单位的“重量”定义了它与其他单位的碰撞方式，值越大，推动就越困难。
- **开火间隔乘数**（`shootDelayMultiplier`）：默认为1。可以在setUnitStats动态改变
- **开火伤害乘数**（`shootDamageMultiplier`）：默认为1。可以在setUnitStats动态改变
- **移动速度**（`moveSpeed`）：腿脚移动速度。
- **最大转弯速度**（`maxTurnSpeed`）：最大转弯速度。
- **攻击距离**（`maxAttackRange`）：最大攻击距离。（会乘以globalScale，但不建议使用globalScale）
- **生命恢复速度**（`selfRegenRate`）：此数值决定每帧增加血量。游戏内默认速度下，一秒为60逻辑帧，而您看到的FPS帧数为渲染帧，所以电脑上几百帧和手机上60帧和省电模式下30帧并不影响计算。所以不要写太大。可以写负值用于自毁。
- **heightTarget**
- **工厂速度乘数**（`nanoFactorySpeed`）：默认值为1。如果此单位是工厂，则乘以创建单位的buildSpeed值。用于加速或者减速建造。
- **视野**（`fogOfWarSightRange`）：设置单位视野，在战争迷雾中可以看到的瓷砖数量。默认为15。

## 资源数字分组

- **none**
- **comma**
- **space**

## 显示类型

- **none**
- **rally**
- **upgrade**
- **queueUnit**
- **building**
- **action**
- **infoOnly**
- **infoOnlyNoBox**
- **infoOnlyStockpile**

## drawLayer

- **wreaks**
- **underwater**
- **bottom**
- **ground**
- **ground2**
- **experimentals**
- **air**
- **top**

## 作图类型

- **normal**
- **displacement**

## 指定攻击地面类型

- **无**（`NONE`）
- **BUILDING**
- **陆地**（`LAND`）
- **两栖**（`HOVER`）
- **空中**（`AIR`）
- **跨悬崖**（`OVER_CLIFF`）
- **OVER_WATER_CLIFF**

## 绘制图层

- **inactive**
- **阴影**（`shadow`）：true时绘制阴影。如果使用imageShadow则强制为true
- **afterBody**
- **beforeBody**
- **onTop**
- **beforeUI**

## 运动类型

- **无**（`NONE`）
- **陆地**（`LAND`）
- **空中**（`AIR`）
- **水面**（`WATER`）：水面。海军，两栖之类可通过，可建造。
- **两栖**（`HOVER`）
- **BUILDING**
- **跨悬崖**（`OVER_CLIFF`）
- **跨悬崖和水面**（`OVER_CLIFF_WATER`）

## 动画条件

- **移动**（`move`）
- **攻击**（`attack`）：[attack]
- **idle**
- **underConstruction**
- **underConstructionWithLinkedBuiltTime**
- **queuedUnits**
- **repair**：主动维修建筑

## 查看者为特定队伍时显示

- **己方**（`own`）
- **中立**（`neutral`）
- **除自己外盟友**（`allyNotOwn`）
- **盟友**（`ally`）
- **敌方**（`enemy`）
- **所有**（`any`）
- **notOwn**

## 地图位置产生指定单位

- **emptyResourcePools_asNeutral**：产生于空资源池，且为中立所属
- **emptyOrOccupiedResourcePools_asNeutral**：空的或已经占用资源池，且为中立所属
- **mapCenter_asNeutral**：地图中心，且为中立所属
- **mapCenter_eachActiveTeam**：地图中心，且给与每个玩家
- **spawnPoint_eachActiveTeam**：玩家初始位置，给与每个玩家

## Prices_Resources

- **资金**（`credits`）：设定玩家初始资金。
- **能量**（`energy`）：单位使用的能量，如可用于开火限制。
- **hp**：单位血量，一般血量归零时就会销毁单位。
- **护盾**（`shield`）：单位护盾，一般用于抵挡敌人攻击。
- **ammo**：弹药，每个单位的隐藏值，供mod使用。
- **设置标志**（`setFlag`）：设置标志，范围0-31。标志存储在每个单元中，与addResources，resourceUsage或price等一起使用。
- **取消标志**（`unsetFlag`）：取消已经设置的标志
- **拥有标志**（`hasFlag`）：拥有标志。
- **没有标志**（`hasMissingFlag`）：没有标志。
- **X**：子单位x轴位置

## 检索队伍

- **己方**（`own`）
- **中立**（`neutral`）
- **除自己外盟友**（`allyNotOwn`）
- **盟友**（`ally`）
- **敌方**（`enemy`）
- **所有**（`any`）
- **notOwn**

## 设置单位状态

- **生命值**（`maxHp`）：单位最大生命值，默认生成时即为此值。
- **hp**：单位血量，一般血量归零时就会销毁单位。
- **护盾值**（`maxShield`）：单位最大护盾值，默认生成时即为此值。如果设置了startShieldAtZero:true，则初始为0.
- **护盾**（`shield`）：单位护盾，一般用于抵挡敌人攻击。
- **护盾恢复速度**（`shieldRegen`）：此数值决定每帧增加护盾值，游戏内一秒为60帧，所以不要写太大。可以写负值。
- **maxEnergy**
- **能量**（`energy`）：单位使用的能量，如可用于开火限制。
- **装甲**（`armour`）：抵消敌方攻击所造成的伤害。
- **质量**（`mass`）：单位的“重量”定义了它与其他单位的碰撞方式，值越大，推动就越困难。
- **开火间隔乘数**（`shootDelayMultiplier`）：默认为1。可以在setUnitStats动态改变
- **开火伤害乘数**（`shootDamageMultiplier`）：默认为1。可以在setUnitStats动态改变
- **移动速度**（`moveSpeed`）：腿脚移动速度。
- **最大转弯速度**（`maxTurnSpeed`）：最大转弯速度。
- **攻击距离**（`maxAttackRange`）：最大攻击距离。（会乘以globalScale，但不建议使用globalScale）
- **生命恢复速度**（`selfRegenRate`）：此数值决定每帧增加血量。游戏内默认速度下，一秒为60逻辑帧，而您看到的FPS帧数为渲染帧，所以电脑上几百帧和手机上60帧和省电模式下30帧并不影响计算。所以不要写太大。可以写负值用于自毁。
- **heightTarget**
- **工厂速度乘数**（`nanoFactorySpeed`）：默认值为1。如果此单位是工厂，则乘以创建单位的buildSpeed值。用于加速或者减速建造。
- **视野**（`fogOfWarSightRange`）：设置单位视野，在战争迷雾中可以看到的瓷砖数量。默认为15。

## spawnProjectiles

- **产生机会**（`spawnChance`）：抛射体产生几率，默认为1
- **最大产生限制**（`maxSpawnLimit`）：抛射体的最大产生数量
- **递归限制**（`recursionLimit`）：用于限制抛射体生成抛射体自身的上限,而不会产生死循环。这对自动生成很有用，因为不会无限生成，有利于链条爆炸
- **偏移量x**（`offsetX`）：生成出的抛射体的x轴偏移量
- **偏移量y**（`offsetY`）：生成出的抛射体的y轴偏移量
- **X轴相对偏移**（`xOffsetRelative`）：(note use basePosition with createMarker/etc for logic use)（请注意，动态使用base Position（基本位置）和create Marker（创建标记）/ect（等））
- **Y轴相对偏移**（`yOffsetRelative`）：(note use basePosition with createMarker/etc for logic use)（请注意，动态使用base Position（基本位置）和create Marker（创建标记）/ect（等））
- **偏移随机X**（`offsetRandomX`）：生成出的抛射体的x轴的随机偏移量
- **偏移随机Y**（`offsetRandomY`）：生成出的抛射体的y轴的随机偏移量
- **偏移随机XY**（`offsetRandomXY`）：生成出的抛射体在两个方向的偏移随机偏移，使真正的在一个区域随机化。
- **偏移高度**（`offsetHeight`）：生成出的抛射体的z轴也就是高度偏移
- **偏移角度**（`offsetDir`）：生成出的抛射体的角度偏移
- **偏移随机角度**（`offsetRandomDir`）：生成出的抛射体的角度随机偏移量

## 产生单位

- **neutralTeam**：生成这个单位归属于中立队伍而不是所属玩家。
- **所属攻击者**（`setToTeamOfLastAttacker`）：生成单位归属于攻击者。（在[core] unitsSpawnedOnDeath上有用）
- **产生机会**（`spawnChance`）：抛射体产生几率，默认为1
- **spawnSource**：改变产生地点和产生单位的队伍到这个单位参考。
- **最大产生限制**（`maxSpawnLimit`）：抛射体的最大产生数量
- **对齐网格**（`gridAlign`）：将生成位置与网格对齐，对建筑物有用
- **跳过条件**（`skipIfOverlapping`）：如果在无效的位置忽略单位生成。 例如基于LAND的单位不会产生在水中。
- **偏移量x**（`offsetX`）：生成出的抛射体的x轴偏移量
- **偏移量y**（`offsetY`）：生成出的抛射体的y轴偏移量
- **偏移角度**（`offsetDir`）：生成出的抛射体的角度偏移
- **偏移高度**（`offsetHeight`）：生成出的抛射体的z轴也就是高度偏移
- **偏移随机X**（`offsetRandomX`）：生成出的抛射体的x轴的随机偏移量
- **偏移随机Y**（`offsetRandomY`）：生成出的抛射体的y轴的随机偏移量
- **偏移随机角度**（`offsetRandomDir`）：生成出的抛射体的角度随机偏移量
- **增加资源**（`addResources`）：给生成单位这些资源，可用于设置触发动作的标志
- **载员至载具**（`transportedUnitsToTransfer`）：将指定数量的单元放入生成出的单元的运输槽位中。
- **复制路径来自**（`copyWaypointsFrom`）：复制目标上的所有路径点到创建的单位。
- **朝向总是零度**（`alwayStartDirAtZero`）：生成出的单位朝向总是设置为零度。即朝右。
- **伤害边框**（`damagingBorder`）：此内部单位会生成一个圆形的安全区域，所有区域外的单位会不断收到伤害直到死亡。
- **zoneMarker**：此单位用于指示伤害边框移动到何处，是放大还是缩小，可使用“techLevel”控制大小。
- **科技等级**（`techLevel`）：“techLevel”单位生成参数,主要用于内置的单元和设置damagingBorder大小。

## 提取资源队伍

- **己方**（`own`）
- **中立**（`neutral`）
- **除自己外盟友**（`allyNotOwn`）
- **盟友**（`ally`）
- **敌方**（`enemy`）
- **所有**（`any`）
- **notOwn**

## 阵营色模式

- **pureGreen**
- **hueAdd**
- **hueShift**
- **disabled**

## 被运输单位类型限制

- **无**（`NONE`）
- **BUILDING**
- **陆地**（`LAND`）
- **两栖**（`HOVER`）
- **空中**（`AIR`）
- **跨悬崖**（`OVER_CLIFF`）
- **OVER_WATER_CLIFF**

## whenBuilding_temporarilyConvertTo_keepFields

- **生命值**（`maxHp`）：单位最大生命值，默认生成时即为此值。
- **hp**：单位血量，一般血量归零时就会销毁单位。
- **护盾值**（`maxShield`）：单位最大护盾值，默认生成时即为此值。如果设置了startShieldAtZero:true，则初始为0.
- **护盾**（`shield`）：单位护盾，一般用于抵挡敌人攻击。
- **护盾恢复速度**（`shieldRegen`）：此数值决定每帧增加护盾值，游戏内一秒为60帧，所以不要写太大。可以写负值。
- **maxEnergy**
- **能量**（`energy`）：单位使用的能量，如可用于开火限制。
- **装甲**（`armour`）：抵消敌方攻击所造成的伤害。
- **质量**（`mass`）：单位的“重量”定义了它与其他单位的碰撞方式，值越大，推动就越困难。
- **开火间隔乘数**（`shootDelayMultiplier`）：默认为1。可以在setUnitStats动态改变
- **开火伤害乘数**（`shootDamageMultiplier`）：默认为1。可以在setUnitStats动态改变
- **移动速度**（`moveSpeed`）：腿脚移动速度。
- **最大转弯速度**（`maxTurnSpeed`）：最大转弯速度。
- **攻击距离**（`maxAttackRange`）：最大攻击距离。（会乘以globalScale，但不建议使用globalScale）
- **生命恢复速度**（`selfRegenRate`）：此数值决定每帧增加血量。游戏内默认速度下，一秒为60逻辑帧，而您看到的FPS帧数为渲染帧，所以电脑上几百帧和手机上60帧和省电模式下30帧并不影响计算。所以不要写太大。可以写负值用于自毁。
- **heightTarget**
- **工厂速度乘数**（`nanoFactorySpeed`）：默认值为1。如果此单位是工厂，则乘以创建单位的buildSpeed值。用于加速或者减速建造。
- **视野**（`fogOfWarSightRange`）：设置单位视野，在战争迷雾中可以看到的瓷砖数量。默认为15。
