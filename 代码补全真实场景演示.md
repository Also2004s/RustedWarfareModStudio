# 代码补全真实场景演示

> 本文档用「模拟真实 MOD 项目编写过程」的方式，完整演示铁锈工坊（RustedWarfareModStudio）编辑器的所有参与补全的代码与触发场景。
> 覆盖：**节补全、属性补全、@ 指令校全、各类值补全（16 个 Provider），以及函数命名参数键值（`x=` 这种）**。
> 全部插入文本均为**中文视图**下补全棒出的样子；保存时由翻译引擎反译为英文原名。

---

## 一、补全类型总览

| 触发位置 | 补全 Server（类型） | 补充说明 |
|---|---|---|
| 行首光标紧跟 `[` | 节补全 `SECTION` | `[核心]` `[炮塔_` 等 17 类节 |
| 节内**空行** | 节体属性补全 | 推荐当前节允许分类的全部属性（`键:`） |
| 行首输入键名 | 属性名补全 `KEY` | 原生表 + 用户表 + 翻译库兜底 |
| `:` 之后 | 值补全 `VALUE` | 16 个 Provider 组合产出 |
| 行首输入 `@` | 指令补全 | `@memory / @define / @global` |
| `[]` 节中写 `@memory name:` | 内存变量名 / 类型补全 | 变量名 + `:`、类型（int/float/unit 等） |
| 函数括号内 | **函数参数键值补全** | 命名参数 `参数x=`、`参数x=` 后值建议 |
| 值中含 `${` | 变量引用 / 跨节引用补全 | `${全局变量}` `${攻击.攻击距离}` |

---

## 二、节补全（`SECTION`）

在空行输入 `[`，或在 `[` 后输入前缀，弹出节名：

| 节名（插入文本） | 对应英文 |
|---|---|
| `[核心]` | core |
| `[图像]` | graphics |
| `[攻击]` | attack |
| `[炮塔_` | turret_ |
| `[抛射体_` | projectile_ |
| `[运动]` | movement |
| `[行动_` | action_ |
| `[隐藏行动_` | hiddenAction_ |
| `[效果_` | effect_ |
| `[动画_` | animation_ |
| `[附属_` | attachment_ |
| `[可建造_` | canBuild_ |
| `[贴花]` | decal |
| `[资源_` | resource_ |
| `[全局资源_` | global_resource_ |
| `[腿]` | leg |
| `[放置规则]` | placementrule |

> 输入 `[炮` → 补 `[炮塔_`；输入 `[隐` → 补 `[隐藏行动_`。命名节以 `节基名_` 形式返回，方便继续写节名。

---

## 三、属性补全（`KEY`）

进入某个节后，空行会弹出当前节允许的属性（`键:` 形式）。也支持直接输入键名前缀。

以 `[核心]` 为例，节内空行补全会有：`名称:`（name）、`价格:`（price）、`最大生命:`（maxHp）、`半径:`（radius）、`最大视野:` 等。

属性补全来源按优先级叠加：
1. **用户自定义补全表**（置顶）
2. **原生补全表**（由 code_reference 生成）
3. **翻译库兜底**（输入前缀后，字典中命中但表里没有的 key）

> 节体属性补全只显示当前节允许分类下的属性；`[行动_xx]` 内只会给出行动节属性。

---

## 四、@ 指令补全

行首输入 `@`：

| 输入 | 插入文本 | 说明 |
|---|---|---|
| `@m` | `@memory ` | 内存变量 |
| `@d` | `@define ` | 局部变量（当前节 + 继承链同节可见） |
| `@g` | `@global ` | 全局变量（项目级，`${}` 引用） |

### 4.1 内存变量名与类型补全

输入 `@memory ` 后会分两阶段：

- 未输入 `:` → 补**变量名**（建议名带 `:`）：如 `customHp:`、`damage:`、`unitType:`、`tempVar:`
- 输入 `变量名:` → 补**类型**：`int`、`float`、`string`、`bool`、`unit`、`unit[]`、`float[]` 等

```ini
[核心_1]
@memory nukeReady:bool          ; 补 @memory → 变量名 nukeReady: → 类型 bool
价格: 1000
```

---

## 五、值补全（`VALUE`—— `:` 之后）

下面用一个真实的 MOD 编写过程，逐步走完 `:` 之后会出现的各种值补全。

### 5.1 布尔值补全（Bool）

属性类型为 bool 时（如 `隐藏:`、`锁定:`、`显示剩余库存:`、`复制但跳过节:`），`:` 后补 `真` / `假`：

```ini
[核心_1]
隐藏: 假
```

### 5.2 枚举值补全（Enum）

属性类型为枚举时（movement/drawType/layer/displayType/teamColoringMode/onActions/onlyTeam/searchTeam 等），`:` 后补枚举值：

```ini
[运动_1]
移动类型: 陆地     ; movementType 枚举（LAND/AIR/WATER/HOVER... 经翻译）

[图像]
着色模式: 队伍色    ; teamColoringMode
```

### 5.3 逻辑表达式补全（LogicBoolean）

属性类型为 LogicBoolean / LogicNumber（如 `需要条件:`、`可见性:`、`额外图标显示条件:`、`透明度:`、`帧:`），以及**动态资源** `用逻辑设置资源:`、**键值对** `设置单位内存:` 的 RHS、字符串 `%{...}` 插值内，都会弹出逻辑表达式补全。

写一个空中单位的出生条件（在 `[隐藏行动_1]` 的空行点 `自动触发:`）：

```ini
[隐藏行动_1]
自动触发: if 自身有标签(有标签=对空)          ; ① 空值补 真/假/if
自动触发: if 自身有标签(有标签=对空) and 自身弹药包括队列(超过=2)
```

`:` 后完整值上下文会给出的好几类项（按上下文类型过滤）：
- 布尔原语：`真/假/if`
- 连接符：`or / and`（仅表达式非起点）
- 比较与算术运算符：`= /= -` 等由用户手输，不参与补全
- 布尔函数：`自身有标签(...)`、`自身弹药包括队列(...)`、`可见性(...)、自身受到伤害(几秒内=)`
- 数值表达式：`自身血量`、`距离`、`选择`（多态）、`创建标记`、`获取相对偏移`
- **单位标记**：`自身`（self）、`创建标记(...)`、`当前动作目标`、`内存.重拦集结`

### 5.4 单位标记补全（UnitRef / 单位标记表达式）

属性语义是单位标记表达式（`设置自定义目标1/2`、`添加路径点来自参考`、`传送到`、`中心位置`、`绘制线条到`、`发送消息到`、`内存.xxx` 等）时，`:` 后补单位标记调用源：

```ini
[行动_召唤]
传送到: 自身
添加路径点来自参考: 创建标记(距离=)
```

---

## 六、🅸 函数命名参数键值补全（`x=` 这种）★

> 这是本项目中「除了属性 `键:` 之外的另一种补全形式」——**参数 `键=`**。
> 光标位于已知参数函数的括号内（未闭合 `(`）时触发，由 FunctionParameterCompletionProvider 处理。

### 6.1 `(` 或 `,` 后 → 补未使用参数名（`参数x=`）

光标停在 `(` 后或英文逗号 `,` 后，列出该函数还没填的**参数名**（插入文本带 `=`，label 就是插入文本）：

```ini
自动触发: if 自身有标签(有标签=对空) and 自身血量
```
在 `自身血量(` 的光标处补：
```
超过=    ; greaterThan
少于=    ; lessThan
等于=    ; equalTo
空的=    ; empty
满=      ; full        （超过/少于/等于/空的/满 为互斥组，detail 标注「条件参数（互斥）」）
```
依次填入 `少` → 再输入 `,`：补多个条件 `自身血量(少于=50, 空的=false)`。

### 6.2 各函数可补的参数一览

来自 `assets/data/param/logicboolean.json` 与 `autoTriggerOnEvent.json`：

| 函数（中文） | 可补参数（`=`） | 参数值来源 |
|---|---|---|
| `自身血量 / 自身能量 / 能量包括队列 / 弹药包括队列 / 自身高度 / 自身弹药 / 自身护盾 / 自身击杀数 / 运输数量 / 排队数量` | `超过= 少于= 等于= 空的= 满=` | 超过/少于/等于=字面量；空的/满=真/假（互斥） |
| `我方单位数量 / 敌方单位数量 / 友方单位数量 / 盟友非己方 / 中立 / 激进阵营 / 所有阵营` | `需标签= 超过= 少于= 范围内= 包含未完成的= 包含队列中的=` | 需标签=标签；数值=字面量；布尔=真/假 |
| `我方有单位 / 我方无单位` | `需标签= 范围内= 包含未完成的= 包含队列中的=` | 同上 |
| `自身受到伤害 / 存活时间 / 上次转换` | `几秒内= 几秒后=` | 字面量 |
| `自身有标签 / 全局队伍标签 / 运输的单位有标签` | `有标签=` | 标签（本地/全局） |
| `附近单位 / 全局搜索第一个单位 / 最近单位` | `范围内= 需标签= 关系=` | 关系枚举（己方/敌对/中立…经翻译） |
| `该单位有旗帜` | `id=` | 字面量（可表达式） |
| `自身排队量 / 队列项目添加 / 队列项目取消` | `withActionTag=` | **行动/隐藏行动节名** |
| `新消息(需标签=)` | `需标签=` | **消息标签**（sendMessageWithTags 用到的取值，独立命名空间） |
| `隶属槽 / 附属(slot=, 需标签=)` | `slot= 需标签=` | slot=**附属节名**（`[附属_xxx]` 后缀）；需标签=标签 |
| `自身有资源(...) / 全局资源数量(...)` | `资源名=` | **项目自定义资源名**（动态按资源名补 `=`） |
| `创建标记 / 获取相对偏移 / 获取绝对偏移` | `x= y= height= dir= 角度偏移= teamId=` | x/y/height/dir/角度偏移=**逻辑数值表达式**；teamId=字面量 |
| `读取单位内存 / 事件数据` | 首个参数自动补**内存变量名**（带引号） | `'攻击目标'`（位置参数，不弹 `name=`） |
| `读取单位内存(…, )` 后 | `type= index= default=` | type=类型枚举（unit/float/int/...）；index=字面量 |
| `资源比较` | `source= compareTarget= byMoreThan= multiplyTargetBy=` | source/compareTarget=**项目资源名**；其余字面量 |

### 6.3 `参数x=` 之后 → 补该参数的值

光标停在 `参数x=` 后，按参数类型给值建议：

```ini
: if 附近单位(范围内=, 需标签=)
```
- `范围内=` 后 → 数值，直接手填；若参数标记为**可表达式**（如 创建标记 的 `x=`），则给**逻辑数值表达式**（`之间方向(...)`、`自身.x`、`选择(...)`、`距离(...)` 等，还含单位标记 `自身`）。
- `需标签=` 后 → 项目**本地/全局标签**（带单引号）：`'对空'` `'海'`
- `newMessage(需标签=` → 项目**消息标签**
- `关系=` 后 → 关系枚举：`'己方'` `'敌对'` `'中立'` `'同盟'` `'非己方'` `'任何'`
- `type=` 后 → `'unit'` `'float'` `'int'` `'boolean'` `'string'` `'unit[]'` `'string[]'`
- `withActionTag=` 后 → 行动/隐藏行动节名

### 6.4 spawn 类属性的括号内参数（`x=`）

` 产生单位:`（spawnUnits）括号内也补参数键值：

```ini
死亡产生单位: tank*3(
```
括号内补 `neutralTeam=` `spawnChance=` `setToTeamOfLastAttacker=` `maxSpawnLimit=` `gridAlign=` `offsetX=` `offsetY=` `offsetRandomX=` `addResources=` `copyWaypointsFrom=` `techLevel=` 等（值类型各不同）。

```ini
死亡产生单位: tank*3(neutralTeam=真, spawnChance=0.5)
```

---

## 七、🅸 内存变量补全（Memory）

以下情况触发内存补全：
- 属性名含 memory（`设置单位内存:`、`更新单位内存:`、`读取单位内存:`、`定义单位内存:`）
- 值中出现 `内存.` / `memory.` 段（任意属性值，含 `%{...}` 内）

```ini
[核心_1]
@memory 攻击目标:unit
@memory 重拦集结:int

[隐藏行动_1]
自动触发: if … 之后再填
设置单位内存: 攻击目标=自身
更新单位内存: 重拦集结=选择(真, 1, 2)

用逻辑设置资源: 目标=内存.攻击目标   ; RHS 空时补「内存.变量名」不破坏资源名
```

`内存.` 后补已声明的内存变量名；`设置单位内存: 变量名=` 位置（无 `=` 的 LHS）补 `变量名=`；**unit 型内存变量**可继续 `.` 链：`内存.攻击目标.资源.总战力`。

---

## 八、🅸 资源补全（Resource）

- 属性 type 含 price/resource（`价格:`、`资源使用:`）→ 直接补资源名（内置 credits/energy/hp/shield/ammo + 项目自定义）。
- 链式前缀 `<链>资源.`（`自身资源.`、`当前动作目标.资源.`、`内存.xx.资源.`、`%{...资源.`）→ 补资源名，只替换点后。

```ini
文本: 弹药 %{自身资源.ammo}
价格: credits=100, energy=50
```

动态资源（`用逻辑设置资源:` / `用逻辑添加资源:`）的**资源名 LHS**（顶层 `=` 之前）补 `资源名=`：

```ini
用逻辑设置资源: AI阶段=自身资源.总战力   ; 在 = 前补「资源名=」；= 后补逻辑表达式
```

---

## 九、🅸 标签补全（Tag）

| 属性 | 数据源 | 说明 |
|---|---|---|
| `tags:` / `临时标签添加:` | 本地标签 | tags/tempTagAdd 的值 |
| 全局标签属性（`添加全局标签:`） | 全局标签 | addGlobalTag 等 |
| `带标签发送消息:` | 消息标签 | sendMessageWithTags 用到的值（独立命名空间） |

```ini
[核心_2]
tags: 海, 对空, 空军
带标签发送消息: 防空警报
```

之后 `自带标签(...`/`自身有标签(需标签=` 就能补到 `'海'` `'对空'` 等；`带标签发送消息:` 用到的取值只在 `newMessage(需标签=` 时补。

---

## 十、🅸 单位类型名补全（UnitName）

type 含 unitref/unitType/unitTypes，或 `名称:`/单位名列表时：补**项目单位类型名**（不是标记）。

```ini
[可建造_工厂]
可建造单位: 猛犸坦克T1, 小军工厂
```
`可建造单位:` 后补项目里 `[核心] name:` 定义的单位名。属性如 `死亡产生单位:`、`升级自:`、`覆盖单位:`、`builtFrom_1_name` 同样适用。

---

## 十一、🅸 单位生成补全（UnitSpawn）

` 产生单位:`（spawnUnits/produceUnits）、`产生抛射体:`（spawnProjectiles）括号外先补项目单位名 + `示例格式`；括号内补 `参数x=`（见 6.4）。

---

## 十二、🅸 事件触发补全（AutoTriggerOnEvent）

`自动触发事件:`（autoTriggerOnEvent）属性 `:` 后补事件名：

```ini
自动触发事件: 新的消息(需标签=防空警报)   ; 带参事件，括号内补需标签（消息标签）
自动触发事件: 受到伤害(需标签=对空)        ; 括号内补需标签（本地标签）
自动触发事件: 队列项目添加(withActionTag=行动)  ; withActionTag=行动节名
自动触发事件: 队列项目取消(withActionTag=)
```

---

## 十三、🅸 项目引用补全（ProjectRef）

按属性 type 补命名节/文件引用名：

| 属性 type | 补什么 |
|---|---|
| turret ref | 炮塔节名（`[炮塔_xx]`） |
| projectile ref / projectile... | 抛射体节名 |
| effect ref(s) | 效果节名 |
| action refs / action ids | 行动 + 隐藏行动节名 |
| animation id / ref | 动画节名 |
| decal refs | 贴花节名 |
| attachment id / ref | 附属节名 |
| canBuild(canBuild ref) | 可建造节名 |
| sound ref | 项目音频文件 |
| image / path | ROOT: 图片路径（Image 补全） |

```ini
[攻击_主炮]
炮塔: 炮塔_主炮
```

---

## 十四、🅸 跨节引用补全（CrossSectionRef：`${节名.属性}`）

值中出现未闭合 `${` 时：

```ini
攻击范围: ${攻击.攻击距离}
伤害: ${攻击.攻击伤害-15}
移动速度: ${44}   ; 值是 ${数值}，最多 44
时间: ${运动.移动速度+2}
```

- `${` + 片段 → 补节名（`攻击`/`运动`/`核心`…）；支持英文 `${core.X}` 与关键字 `${section.X}`（当前节）。
- `${节名.` + 片段 → 补该节属性名，只替换点后。
- 括号内含未闭合 `${`（`范围内=${攻击.攻`）也触发。

---

## 十五、🅸 变量引用补全（DefineVariable：`${变量名}`）

值中 `${` 后（无 `.`）补**局部变量 `@define`** 和**全局变量 `@global`**：

```ini
[核心_1]
@define 伤害加成:10
攻击伤害: ${伤害加成+5}     ;
```

局部变量仅当前节 + 继承链同节可见；全局变量项目级可见。

---

## 十六、🅸 复制节补全（CopyFromSection）

`复制节:`（@copyFromSection）后补**完整节名**（基名_节名，按当前节类型过滤）：

```ini
[抛射体_2]
复制节: 抛射体_1
```

在 `[隐藏行动_x]` 内只给 `隐藏行动_yyy`；非命名节回退全部命名节。`复制但跳过节:` 是布尔指令，补 `真/假`。

---

## 十七、一个完整的真实补全联动示例

把上面的补全串成一个完整单位节（每行注释标出触发的是哪种补全）：

```ini
[核心_坦克]                                  ; 节补全 [核心_
名称: 猛犸坦克                                 ; 属性补全 名称:（用户表/原生表）
价格: credits=1200, energy=50                ; 资源补全（价格）
最大生命: 500                                 ; 属性补全
移动类型: 陆地                                 ; 枚举补全（movementType）
图像: ROOT:units/tank.png                     ; 图片路径补全（ROOT:）

@memory 攻击目标:unit                          ; @memory → 变量名 → 类型
@memory 重拦集结:int
@global 我方经济阶段:1

[隐藏行动_导弹]
自动触发: if 自身弹药包括队列(超过=1) and 附近单位(范围内=100, 需标签=对空)   ; 逻辑表达式 + 函数参数键值
复制节: 行动_导弹                              ; 复制节补全
设置单位内存: 攻击目标=最近单位(范围内=100, 需标签=对空)                     ; 内存变量 + 单位标记
自动触发事件: 队列项目添加(withActionTag=行动)
文本: 开火 弹药=%{自身资源.ammo}               ; %{ 插值 + 资源补全
攻击距离: ${攻击.攻击距离+20}                 ; 跨节引用补全
```

---

## 十八、补全触发与返回关键点（速记）

1. `[` 后 → 节；节内空行 → 属性；`:` 后 → 值。
2. 括号内已知函数 → 参数 `x=`；`x=` 后 → 该参数的值建议。
3. `${` 无点 → 变量引用；`${节.` → 跨节引用。
4. `内存.` / `自身资源.` / `<链>资源.` → 各自链式补全。
5. `自动触发事件:` → 事件；`复制节:` → 节名；`带标签发送消息:` 取值 → 消息标签命名空间。
6. 已知参数函数括号内只补值结果，不刷全表；运算符（`+ - * /` 比较符）由用户手输不参与补全。

> 以上插入文本均为中文视图下的补全 label；保存时 `translateToEnglish` 会反译为 `自我` 链 / `selected` 等英文原名。