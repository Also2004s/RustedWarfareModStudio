# RustedWarfareModStudio 功能迭代计划表

---

## 一、文本编辑器

### 1. 翻译自动空格移入开发者模式（默认打开）

**现状**：`EditorScreen.kt:776` 的小菜单栏（DropdownMenu）中直接显示「翻译自动空格」开关。

**改动点**：
- `SettingsManager.kt`：新增 `devAutoSpace` Boolean 属性，默认值 `true`
- `DeveloperModeScreen.kt`：在「界面显示」或「解析与翻译」分组下新增「翻译自动空格」开关
- `EditorScreen.kt:776`：将该菜单项从 DropdownMenu 中移除，改为仅在开发者模式开启时显示（或在开发者模式子页面中控制）

**涉及文件**：
- `SettingsManager.kt`（新增属性）
- `DeveloperModeScreen.kt`（新增开关 UI）
- `EditorScreen.kt`（移除原菜单项、改为受开发者模式控制）

---

### 2. 复制文件名/复制相对路径默认隐藏，开发者模式可开启

**现状**：`EditorScreen.kt:815-817` 的 DropdownMenu 中始终显示「复制文件名」和「复制相对路径」两项。

**改动点**：
- `SettingsManager.kt`：新增 `devShowCopyPath` Boolean 属性，默认值 `false`
- `DeveloperModeScreen.kt`：在「界面显示」分组下新增「显示复制路径菜单」开关
- `EditorScreen.kt`：将这两项的显示条件改为 `SettingsManager.devMode && SettingsManager.devShowCopyPath`

**涉及文件**：
- `SettingsManager.kt`
- `DeveloperModeScreen.kt`
- `EditorScreen.kt`

---

### 3. 顶部 Tab 栏改为显示当前项目内最近打开的 5 个文件

**现状**：`EditorScreen.kt:243` 使用全局 `SettingsManager.recentFiles`，不限制项目范围。

**改动点**：
- `EditorScreen.kt:243`：过滤逻辑改为只保留 `projectRoot` 前缀的文件（若 `projectRoot` 非空）
- 代码示例：`SettingsManager.recentFiles.filter { projectRoot.isEmpty() || it.startsWith(projectRoot) }.take(5).filter { it != filePath }`
- 保持 `recentTick` 触发刷新机制不变

**涉及文件**：
- `EditorScreen.kt`

---

## 二、其他

### 1. 文件管理器顶部横栏加入「最近」按钮

**现状**：`FileBrowserScreen.kt` 顶部只有返回、首页和路径导航，无快捷入口。

**改动点**：
- `FileBrowserScreen.kt`：在顶部 Row 右侧（MoreVert 按钮旁或替换其位置）新增「最近」IconButton
- 新增回调参数 `onShowRecent: (() -> Unit)? = null`
- `MainApp.kt`：`FileBrowserScreen` 调用处传入 `onShowRecent = { showRecentDialog = true }`

**涉及文件**：
- `FileBrowserScreen.kt`
- `MainApp.kt`

---

### 2. 近期修改差异项新增「确认」和「回退」按钮 + 设置里调节缓存数量

#### 2a. 差异项操作按钮

**现状**：`MainApp.kt:713-755` `DiffView` 只显示差异行，带跳转按钮，无确认/回退操作。

**改动点**：
- `DiffView` 新增两个参数：`onConfirm: (() -> Unit)? = null`, `onRevert: (() -> Unit)? = null`
- UI 上：在每行差异的右侧（跳转按钮旁或下方）新增「确认」「回退」按钮
  - **确认**：将该差异对应的 `beforeContent` 更新为当前 `afterContent`（即接受修改），然后调用 `SaveHistoryManager` 更新记录，重新计算差异后该差异项消失
  - **回退**：将文件内容恢复为 `beforeContent`（首次保存前的内容），写回文件，并清空该文件的历史记录或重新记录一次回退操作
- `RecentFilesDialog` 中查看差异时（`selectedHistoryPath != null` 分支）传入实际回调

#### 2b. 缓存数量调节

**现状**：`SaveHistoryManager.kt:12` `MAX_RECORDS` 为编译期常量 `20`。

**改动点**：
- `SettingsManager.kt`：新增 `recentHistoryLimit: Int` 属性，默认 `20`，范围 `20..100`
- `SaveHistoryManager.kt`：`MAX_RECORDS` 改为运行时从 `SettingsManager` 读取（通过参数传入或全局读取）
- `SettingsScreen.kt`：在「文件」分组下新增 Slider 或下拉选择器，调节「近期修改缓存数量」（20-100）

**涉及文件**：
- `MainApp.kt`（DiffView、RecentFilesDialog）
- `SaveHistoryManager.kt`
- `SettingsManager.kt`
- `SettingsScreen.kt`

---

### 3. 「最近」按钮内部缓存（记住上次打开的子页面）

**现状**：`MainApp.kt:556` `showModified` 状态每次打开对话框都重置为 `false`（默认显示「最近打开」）。

**改动点**：
- `SettingsManager.kt`：新增 `lastRecentDialogTab: Boolean` 属性（`false`=最近打开, `true`=近期修改），持久化
- `RecentFilesDialog`：初始化 `showModified` 时从 `SettingsManager` 读取
- 切换「最近打开 / 近期修改」时同步写入 `SettingsManager`

**涉及文件**：
- `SettingsManager.kt`
- `MainApp.kt`

---

### 4. 文件管理器删除目录栏的首页按钮和返回按钮

**现状**：`FileBrowserScreen.kt:82-83` 顶部 Row 中有返回箭头和首页图标。

**改动点**：
- `FileBrowserScreen.kt`：直接删除这两行 `IconButton`（返回和首页）
- 保留 `BackHandler` 的返回逻辑不变（用户可通过系统返回键返回上级）
- 若顶部 Row 因删除按钮导致布局变化，调整剩余元素的间距

**涉及文件**：
- `FileBrowserScreen.kt`

---

### 5. 目录导航中点击当前文件夹展开同级文件夹切换

**现状**：`FileBrowserScreen.kt:96-115` 目录导航面包屑中，最后一级（当前目录）不可点击切换同级目录。

**改动点**：
- 最后一级目录项改为可点击，点击时在其下方展开 DropdownMenu，列出当前父目录下的所有子文件夹
- 用户选择后 `currentPath` 跳转到对应目录
- 展开菜单使用 `DropdownMenu` 实现，定位在对应面包屑下方
- 需要状态：`var showSiblingMenu by remember { mutableStateOf(false) }` 和当前点击的索引

**涉及文件**：
- `FileBrowserScreen.kt`

---

### 6. 新增版本对比功能（选中两个文件夹对比差异）

**现状**：无此功能。Python 参考脚本 `版本对比工具.py` 实现了基于 INI 节和属性的对比逻辑。

**改动点**：

#### 6a. 首页入口
- `HomeScreen.kt`：底部 Card 中新增「差异对比」AssistChip（或替换/新增在「浏览」旁）
- 新增回调 `onVersionCompare: (() -> Unit)? = null`

#### 6b. 版本对比页面
- 新建 `VersionCompareScreen.kt`
  - 选择「源文件夹」和「目标文件夹」两个路径
  - 使用文件选择器（复用现有路径导航逻辑或启动系统文件选择器）
  - 对比逻辑（Kotlin 移植参考 Python 脚本）：
    1. 递归扫描两个目录下的 `.ini` 和 `.template` 文件
    2. 提取每个文件的节（section）和属性键值对
    3. 逐文件对比：独有文件、独有节、共同节的属性差异（新增/删除/修改）
  - 结果展示：
    - 文件级差异列表（文件名 + 差异统计）
    - 点击进入该文件的详细差异（类似现有 DiffView 的样式）

#### 6c. 核心对比引擎
- 新建 `core/VersionComparator.kt`
  - `extractSections(lines: List<String>): Map<String, Map<String, String>>`
  - `compareFiles(fileA: File, fileB: File): FileDiffResult`
  - `compareFolders(folderA: File, folderB: File): List<FileDiffResult>`

**涉及文件**：
- `HomeScreen.kt`
- `MainApp.kt`（新增 Screen 枚举和路由）
- 新建 `ui/screens/VersionCompareScreen.kt`
- 新建 `core/VersionComparator.kt`

---

## 执行顺序建议

| 阶段 | 任务 | 预估工作量 |
|------|------|----------|
| **阶段一** | 1. 文本编辑器三项改动（翻译空格、复制路径、Tab栏过滤） | 小 |
| **阶段二** | 2. 文件管理器「最近」按钮 + 4. 删除首页/返回按钮 + 5. 目录导航展开 | 小 |
| **阶段三** | 3. 最近按钮缓存 + 2. 近期修改确认/回退 + 缓存数量调节 | 中 |
| **阶段四** | 6. 版本对比功能（新页面 + 对比引擎） | 大 |

---

## 风险与注意事项

1. **阶段三的回退操作**：回退是将文件内容写回磁盘，需要确认写入时机（建议在 IO 线程执行，完成后刷新 UI）。
2. **阶段三的确认操作**：确认后更新 `beforeContent` 意味着需要修改 `SaveHistoryManager` 的存储结构或提供更新接口。
3. **阶段六的文件夹对比**：递归扫描大文件夹时需注意性能，建议在协程中执行并显示进度条。
4. **所有修改**：涉及 `SettingsManager` 新增属性时，注意默认值兼容（旧用户升级后应有合理默认值）。
