# 计划：将 direction / directionBetween 移出「单位标记」补全源

## 问题
`direction(x1,y1,x2,y2)`、`directionBetween(unit1,unit2)` 在 `logicboolean.json` 中 `type=float`，
**返回角度(数值)**，仅**参数**接受单位标记。但它们被 `MARKER_FLOAT_FUNCTIONS` 强行并入 `unitMarkerItems`，
导致出现在 `设置单位内存:目标=`（期望 UNIT_MARKER）等上下文，属语义错误——
把一个 float 角度赋给「标记」值不合理。

同时存在内部不一致：
- 补全查看 `+号菜单` / 演示面板分类走 `classifyCallableCategory(type)` → `float` 归「数值表达式」。
- 生产补全 `unitMarkerItems` 却把它们当作单位标记。

## 方案
把这两个方向函数交回「数值/函数参数」体系（即它们的真实父级场景：如 `创建标记(dir=…)` 的 float 参数），
不再作为单位标记源头注入。

### 改动点
1. **删除常量** `MARKER_FLOAT_FUNCTIONS`（UnitRefValueCompletionProvider.kt 第 6 行）。
2. **`unitMarkerItems` 过滤**（LogicExpressionContext.kt 第 68-71 行）：移除
   `MARKER_FLOAT_FUNCTIONS` 并入分支，单位标记源仅保留 `self + unit/marker/event`。
3. **同步注释**：
   - `unitMarkerItems` 顶注（第 55 行）去掉「direction/directionBetween」。
   - UnitRefValueCompletionProvider.kt 第 62 行注释去掉「direction/directionBetween」。

### 不改动
- `logicboolean.json` / 翻译库：`direction`、`directionBetween` 保持 `type=float`，经
  `classifyLogicType` → NUMERIC 自动归「数值表达式」，无需数据改动。
- `FunctionParameterCompletionProvider`：`float` 参数的参数上下文补全不变，`创建标记(dir=…)` 仍可补 `方向/之间方向`。

## 影响
- `设置单位内存:目标=` 等 UNIT_MARKER 上下文不再出现方向函数（修正确）。
- 数值上下文、float 函数参数上下文（如 `创建标记(dir=)`）仍补全方向函数（不丢失）。
- `+号菜单`与生产补全口径统一（都按数值归类）。

## 验证
- 构建 `assembleDebug` 通过。
- 补全查看：`设置单位内存:目标=` 不再出现「方向/之间方向」；float 属性/参数上下文仍出现。